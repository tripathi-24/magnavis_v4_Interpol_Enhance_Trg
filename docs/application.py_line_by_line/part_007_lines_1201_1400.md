# Line-by-line documentation: `src/application.py`

- **File**: `src/application.py`
- **Lines covered in this document**: 1201–1400 (of 1983)

## Structure overview (file-level)

- Line 72: function `load_vtk_file()`
- Line 81: function `getFilePath()`
- Line 94: class `SessionDataManager`
- Line 98: function `update_api_df()`
- Line 124: class `Color`
- Line 126: function `__init__()`
- Line 134: class `DraggableLabel`
- Line 135: method `mousePressEvent()`
- Line 139: function `mouseMoveEvent()`
- Line 156: class `DropLabel`
- Line 157: method `__init__()`
- Line 161: function `dragEnterEvent()`
- Line 165: function `dropEvent()`
- Line 172: class `MapWidget`
- Line 173: method `__init__()`
- Line 177: function `initUI()`
- Line 206: class `PandasModel`
- Line 209: function `__init__()`
- Line 213: function `rowCount()`
- Line 223: function `columnCount()`
- Line 232: function `data()`
- Line 245: function `headerData()`
- Line 262: class `MagTimeSeriesWidget`
- Line 263: method `__init__()`
- Line 269: method `timerRefreshEvent()`
- Line 283: method `initWidget()`
- Line 350: function `on_threshold_changed()`
- Line 377: function `on_train_window_changed()`
- Line 390: function `update_train_window_status()`
- Line 403: class `ApplicationWindow`
- Line 406: method `__init__()`
- Line 432: method `connectSlots()`
- Line 436: method `createThread()`
- Line 447: method `updateData()`
- Line 455: method `startThreads()`
- Line 462: function `addTimeSeries()`
- Line 469: function `showCsvData()`
- Line 493: function `setTreeView()`
- Line 500: function `updateTreeView()`
- Line 507: function `createMailModel()`
- Line 514: function `addSource()`
- Line 521: function `update_visualisation()`
- Line 604: function `clear_point_actors()`
- Line 612: function `set_points()`
- Line 673: function `sphere_prop_to_vtkarray()`
- Line 691: function `build_scene()`
- Line 728: function `setup_point_picker_framework()`
- Line 746: function `on_pick_left()`
- Line 771: function `on_pick_right()`
- Line 777: function `loadFile()`
- Line 803: function `log()`
- Line 824: class `Source`
- Line 825: method `__init__()`
- Line 830: class `TimeSeriesSource`
- Line 831: method `__init__()`
- Line 837: class `World`
- Line 838: method `__init__()`
- Line 845: method `loadCsv()`
- Line 860: class `DataSourceManager`
- Line 861: method `__init__()`
- Line 877: method `loadCsv()`
- Line 883: method `updateUI()`
- Line 887: method `createTimeSeriesSource()`
- Line 892: class `Application`
- Line 893: method `__init__()`
- Line 981: function `showAppMaximized()`
- Line 985: function `log()`
- Line 988: function `_save_data()`
- Line 1039: function `load_plot_framework()`
- Line 1119: function `load_plot_framework_2()`
- Line 1178: function `on_add_static()`
- Line 1196: function `on_add_dynamic()`
- Line 1222: function `start_prediction_process()`
- Line 1261: function `_update_xydata()`
- Line 1290: function `_update_predictions_data()`
- Line 1387: function `_sync_threshold_spinbox()`
- Line 1401: function `_detect_anomalies()`
- Line 1575: function `_update_canvas()`
- Line 1589: function `on_add_new()`
- Line 1626: function `on_add_pred()`
- Line 1745: function `appWin()`
- Line 1748: function `initViews()`
- Line 1754: function `load_visualization_framework()`
- Line 1858: function `load_axes()`
- Line 1881: function `load_sphere_source()`
- Line 1898: function `load_world_grid()`
- Line 1922: function `load_map()`
- Line 1957: function `get_map_xy_from_latlon()`
- Line 1969: function `get_map_lat_lon_from_xy()`

## Line-by-line explanation

| Line | Code | Explanation |
|---:|---|---|
| 1201 | `            ` | Blank line used to separate logical sections and improve readability. |
| 1202 | `            self._save_data(self.x_t, self.y_mag_t)` | Executes Python statement: `self._save_data(self.x_t, self.y_mag_t)`. |
| 1203 | `            self._line_new = None` | Assigns/updates `self._line_new` with the expression on the right-hand side. |
| 1204 | `            # The below two timers must be attributes of self, so that the garbage` | Comment explaining intent/context: The below two timers must be attributes of self, so that the garbage |
| 1205 | `            # collector won't clean them after we finish with __init__...` | Comment explaining intent/context: collector won't clean them after we finish with __init__... |
| 1206 | `    ` | Blank line used to separate logical sections and improve readability. |
| 1207 | `            # The data retrieval may be fast as possible (Using QRunnable could be` | Comment explaining intent/context: The data retrieval may be fast as possible (Using QRunnable could be |
| 1208 | `            # even faster).` | Comment explaining intent/context: even faster). |
| 1209 | `            self.data_timer = dynamic_canvas.new_timer(1000*20) # update data every 20 sec` | Assigns/updates `self.data_timer` with the expression on the right-hand side. |
| 1210 | `            self.data_timer.add_callback(self._update_xydata)` | Executes Python statement: `self.data_timer.add_callback(self._update_xydata)`. |
| 1211 | `            self.data_timer.start()` | Executes Python statement: `self.data_timer.start()`. |
| 1212 | `            # Drawing at 5 Hz should be fast enough for the GUI to feel smooth, and` | Comment explaining intent/context: Drawing at 5 Hz should be fast enough for the GUI to feel smooth, and |
| 1213 | `            # not too fast for the GUI to be overloaded with events that need to be` | Comment explaining intent/context: not too fast for the GUI to be overloaded with events that need to be |
| 1214 | `            # processed while the GUI element is changed.` | Comment explaining intent/context: processed while the GUI element is changed. |
| 1215 | `            self.drawing_timer = dynamic_canvas.new_timer(200)` | Assigns/updates `self.drawing_timer` with the expression on the right-hand side. |
| 1216 | `            self.drawing_timer.add_callback(self._update_canvas)` | Executes Python statement: `self.drawing_timer.add_callback(self._update_canvas)`. |
| 1217 | `            self.drawing_timer.start()` | Executes Python statement: `self.drawing_timer.start()`. |
| 1218 | `            self.magetic_time_ser_canvas = static_canvas` | Assigns/updates `self.magetic_time_ser_canvas` with the expression on the right-hand side. |
| 1219 | `        except Exception as e:` | Starts an `except` handler: `except Exception as e:`. |
| 1220 | `            self.log('Error loading plot framework', error=e)` | Executes Python statement: `self.log('Error loading plot framework', error=e)`. |
| 1221 | `    ` | Blank line used to separate logical sections and improve readability. |
| 1222 | `    def start_prediction_process(self, input_file):` | Defines function/method `start_prediction_process` (entry point for reusable logic). (scope: def start_prediction_process) |
| 1223 | `        try:` | Starts a `try` block to catch and handle exceptions. (scope: def start_prediction_process) |
| 1224 | `            # Use the current Python interpreter (cross-platform)` | Comment explaining intent/context: Use the current Python interpreter (cross-platform) (scope: def start_prediction_process) |
| 1225 | `            python_exe = sys.executable` | Assigns/updates `python_exe` with the expression on the right-hand side. (scope: def start_prediction_process) |
| 1226 | `            python_app_file = os.path.join(APP_BASE, 'predictor_ai.py')` | Assigns/updates `python_app_file` with the expression on the right-hand side. (scope: def start_prediction_process) |
| 1227 | `` | Blank line used to separate logical sections and improve readability. |
| 1228 | `            # Use session folder as working directory so relative paths resolve correctly` | Comment explaining intent/context: Use session folder as working directory so relative paths resolve correctly |
| 1229 | `            work_dir = os.path.dirname(input_file) if input_file else APP_BASE` | Assigns/updates `work_dir` with the expression on the right-hand side. |
| 1230 | `            ` | Blank line used to separate logical sections and improve readability. |
| 1231 | `            # Construct command as a list for better cross-platform compatibility` | Comment explaining intent/context: Construct command as a list for better cross-platform compatibility |
| 1232 | `            command = [python_exe, python_app_file, input_file]` | Assigns/updates `command` with the expression on the right-hand side. |
| 1233 | `            ` | Blank line used to separate logical sections and improve readability. |
| 1234 | `            self.log(f'Starting prediction process: {" ".join(command)} (cwd={work_dir})', level='Info')` | Executes Python statement: `self.log(f'Starting prediction process: {" ".join(command)} (cwd={work_dir})', level='Info')`. |
| 1235 | `            self.predictor_input_file = input_file` | Assigns/updates `self.predictor_input_file` with the expression on the right-hand side. |
| 1236 | `            ` | Blank line used to separate logical sections and improve readability. |
| 1237 | `            # Capture stdout/stderr to files in the session folder for debugging` | Comment explaining intent/context: Capture stdout/stderr to files in the session folder for debugging |
| 1238 | `            stdout_path = os.path.join(work_dir, 'predict_stdout.log')` | Assigns/updates `stdout_path` with the expression on the right-hand side. |
| 1239 | `            stderr_path = os.path.join(work_dir, 'predict_stderr.log')` | Assigns/updates `stderr_path` with the expression on the right-hand side. |
| 1240 | `            stdout_f = open(stdout_path, 'w')` | Assigns/updates `stdout_f` with the expression on the right-hand side. |
| 1241 | `            stderr_f = open(stderr_path, 'w')` | Assigns/updates `stderr_f` with the expression on the right-hand side. |
| 1242 | `` | Blank line used to separate logical sections and improve readability. |
| 1243 | `            # Pass training window to predictor via environment variable if set` | Comment explaining intent/context: Pass training window to predictor via environment variable if set |
| 1244 | `            env = os.environ.copy()` | Assigns/updates `env` with the expression on the right-hand side. |
| 1245 | `            if self.train_window_minutes:` | Starts an `if` block: conditional control flow based on `self.train_window_minutes`. |
| 1246 | `                env["TRAIN_WINDOW_MINUTES"] = str(self.train_window_minutes)` | Assigns/updates `env["TRAIN_WINDOW_MINUTES"]` with the expression on the right-hand side. |
| 1247 | `            else:` | Starts an `else` branch for the preceding conditional. |
| 1248 | `                env.pop("TRAIN_WINDOW_MINUTES", None)` | Executes Python statement: `env.pop("TRAIN_WINDOW_MINUTES", None)`. |
| 1249 | `` | Blank line used to separate logical sections and improve readability. |
| 1250 | `            self.prediction_process = subprocess.Popen(` | Starts an external process (here: the predictor script) and captures its output for later use. |
| 1251 | `                command,` | Executes Python statement: `command,`. |
| 1252 | `                stdout=stdout_f,` | Assigns/updates `stdout` with the expression on the right-hand side. |
| 1253 | `                stderr=stderr_f,` | Assigns/updates `stderr` with the expression on the right-hand side. |
| 1254 | `                cwd=work_dir,  # Set working directory to session dir` | Assigns/updates `cwd` with the expression on the right-hand side. |
| 1255 | `                env=env` | Assigns/updates `env` with the expression on the right-hand side. |
| 1256 | `            )` | Executes Python statement: `)`. |
| 1257 | `            self.log(f'Prediction process started (PID: {self.prediction_process.pid})', level='Info')` | Executes Python statement: `self.log(f'Prediction process started (PID: {self.prediction_process.pid})', level='Info')`. |
| 1258 | `        except Exception as e:` | Starts an `except` handler: `except Exception as e:`. |
| 1259 | `            self.log(f'Error starting prediction process: {e}', error=e, level='Error')` | Executes Python statement: `self.log(f'Error starting prediction process: {e}', error=e, level='Error')`. |
| 1260 | `    ` | Blank line used to separate logical sections and improve readability. |
| 1261 | `    def _update_xydata(self, force = False):` | Defines function/method `_update_xydata` (entry point for reusable logic). (scope: def _update_xydata) |
| 1262 | `        # Shift the sinusoid as a function of time.` | Comment explaining intent/context: Shift the sinusoid as a function of time. (scope: def _update_xydata) |
| 1263 | `        # self.ydata = np.sin(self.xdata + time.time())` | Comment explaining intent/context: self.ydata = np.sin(self.xdata + time.time()) (scope: def _update_xydata) |
| 1264 | `        ` | Blank line used to separate logical sections and improve readability. (scope: def _update_xydata) |
| 1265 | `        # update logic:` | Comment explaining intent/context: update logic: (scope: def _update_xydata) |
| 1266 | `        if self.new_x_t:` | Starts an `if` block: conditional control flow based on `self.new_x_t`. (scope: def _update_xydata) |
| 1267 | `            # mag_t_df = #get_timeseries_magnetic_data(start_time=self.new_x_t[-1])` | Comment explaining intent/context: mag_t_df = #get_timeseries_magnetic_data(start_time=self.new_x_t[-1]) (scope: def _update_xydata) |
| 1268 | `            start_time=self.new_x_t[-1]` | Assigns/updates `start_time` with the expression on the right-hand side. (scope: def _update_xydata) |
| 1269 | `        elif self.x_t:` | Starts an `elif` branch: additional conditional `self.x_t`. (scope: def _update_xydata) |
| 1270 | `            # mag_t_df = #get_timeseries_magnetic_data(start_time=self.x_t[-1])` | Comment explaining intent/context: mag_t_df = #get_timeseries_magnetic_data(start_time=self.x_t[-1]) (scope: def _update_xydata) |
| 1271 | `            start_time=self.x_t[-1]` | Assigns/updates `start_time` with the expression on the right-hand side. (scope: def _update_xydata) |
| 1272 | `        ` | Blank line used to separate logical sections and improve readability. (scope: def _update_xydata) |
| 1273 | `        if not force:` | Starts an `if` block: conditional control flow based on `not force`. (scope: def _update_xydata) |
| 1274 | `            self.appWin.startThreads(hours=None, start_time=start_time, new=True)` | Executes Python statement: `self.appWin.startThreads(hours=None, start_time=start_time, new=True)`. (scope: def _update_xydata) |
| 1275 | `            print('after starting thread..')` | Executes Python statement: `print('after starting thread..')`. (scope: def _update_xydata) |
| 1276 | `        else:` | Starts an `else` branch for the preceding conditional. (scope: def _update_xydata) |
| 1277 | `            mag_t_df = api_df_new` | Assigns/updates `mag_t_df` with the expression on the right-hand side. (scope: def _update_xydata) |
| 1278 | `            if mag_t_df is not None and not mag_t_df.empty and 'time_H' in mag_t_df.columns and 'mag_H_nT' in mag_t_df.columns:` | Starts an `if` block: conditional control flow based on `mag_t_df is not None and not mag_t_df.empty and 'time_H' in mag_t_df.columns and 'mag_H_nT' in mag_t_df.columns`. (scope: def _update_xydata) |
| 1279 | `                new_x_t = mag_t_df['time_H'].tolist()` | Assigns/updates `new_x_t` with the expression on the right-hand side. (scope: def _update_xydata) |
| 1280 | `                new_y_mag_t = mag_t_df['mag_H_nT'].tolist()` | Assigns/updates `new_y_mag_t` with the expression on the right-hand side. (scope: def _update_xydata) |
| 1281 | `                if new_x_t and new_y_mag_t and len(new_x_t)>1 and len(new_y_mag_t)>1:` | Starts an `if` block: conditional control flow based on `new_x_t and new_y_mag_t and len(new_x_t)>1 and len(new_y_mag_t)>1`. (scope: def _update_xydata) |
| 1282 | `                    print('got new data', new_x_t[1:])` | Executes Python statement: `print('got new data', new_x_t[1:])`. (scope: def _update_xydata) |
| 1283 | `                    self.new_x_t = self.new_x_t + new_x_t[1:]` | Assigns/updates `self.new_x_t` with the expression on the right-hand side. (scope: def _update_xydata) |
| 1284 | `                    self.new_y_mag_t = self.new_y_mag_t + new_y_mag_t[1:]` | Assigns/updates `self.new_y_mag_t` with the expression on the right-hand side. (scope: def _update_xydata) |
| 1285 | `                    self.needs_update_lims = True` | Assigns/updates `self.needs_update_lims` with the expression on the right-hand side. (scope: def _update_xydata) |
| 1286 | `            # else:` | Comment explaining intent/context: else: (scope: def _update_xydata) |
| 1287 | `            #     print('no new data')` | Comment explaining intent/context: print('no new data') (scope: def _update_xydata) |
| 1288 | `        self._update_predictions_data()` | Executes Python statement: `self._update_predictions_data()`. (scope: def _update_xydata) |
| 1289 | `` | Blank line used to separate logical sections and improve readability. |
| 1290 | `    def _update_predictions_data(self):` | Defines function/method `_update_predictions_data` (entry point for reusable logic). (scope: def _update_predictions_data) |
| 1291 | `        try:` | Starts a `try` block to catch and handle exceptions. (scope: def _update_predictions_data) |
| 1292 | `            predict_input = self.predictor_input_file` | Assigns/updates `predict_input` with the expression on the right-hand side. (scope: def _update_predictions_data) |
| 1293 | `            if not predict_input:` | Starts an `if` block: conditional control flow based on `not predict_input`. (scope: def _update_predictions_data) |
| 1294 | `                return  # No prediction input file set yet` | Returns value(s) from the current function: `# No prediction input file set yet`. (scope: def _update_predictions_data) |
| 1295 | `            ` | Blank line used to separate logical sections and improve readability. (scope: def _update_predictions_data) |
| 1296 | `            predict_output_file = os.path.join(os.path.dirname(predict_input), 'predict_out.csv')` | Assigns/updates `predict_output_file` with the expression on the right-hand side. (scope: def _update_predictions_data) |
| 1297 | `            ` | Blank line used to separate logical sections and improve readability. (scope: def _update_predictions_data) |
| 1298 | `            # Check if prediction process is still running` | Comment explaining intent/context: Check if prediction process is still running (scope: def _update_predictions_data) |
| 1299 | `            if self.prediction_process:` | Starts an `if` block: conditional control flow based on `self.prediction_process`. (scope: def _update_predictions_data) |
| 1300 | `                return_code = self.prediction_process.poll()` | Returns value(s) from the current function: `_code = self.prediction_process.poll()`. (scope: def _update_predictions_data) |
| 1301 | `                if return_code is None:` | Starts an `if` block: conditional control flow based on `return_code is None`. (scope: def _update_predictions_data) |
| 1302 | `                    # Process is still running` | Comment explaining intent/context: Process is still running (scope: def _update_predictions_data) |
| 1303 | `                    self.log(f'Prediction process still running (PID: {self.prediction_process.pid})', level='Debug')` | Executes Python statement: `self.log(f'Prediction process still running (PID: {self.prediction_process.pid})', level='Debug')`. (scope: def _update_predictions_data) |
| 1304 | `                    return` | Returns from the current function (no explicit value, so `None`). (scope: def _update_predictions_data) |
| 1305 | `                elif return_code != 0:` | Starts an `elif` branch: additional conditional `return_code != 0`. (scope: def _update_predictions_data) |
| 1306 | `                    # Process finished with error` | Comment explaining intent/context: Process finished with error (scope: def _update_predictions_data) |
| 1307 | `                    work_dir = os.path.dirname(predict_input)` | Assigns/updates `work_dir` with the expression on the right-hand side. (scope: def _update_predictions_data) |
| 1308 | `                    stderr_log = os.path.join(work_dir, 'predict_stderr.log')` | Assigns/updates `stderr_log` with the expression on the right-hand side. (scope: def _update_predictions_data) |
| 1309 | `                    stderr_output = ''` | Assigns/updates `stderr_output` with the expression on the right-hand side. (scope: def _update_predictions_data) |
| 1310 | `                    try:` | Starts a `try` block to catch and handle exceptions. (scope: def _update_predictions_data) |
| 1311 | `                        if os.path.exists(stderr_log):` | Starts an `if` block: conditional control flow based on `os.path.exists(stderr_log)`. (scope: def _update_predictions_data) |
| 1312 | `                            with open(stderr_log, 'r') as ef:` | Starts a `with` block (context manager) for `open(stderr_log, 'r') as ef`. (scope: def _update_predictions_data) |
| 1313 | `                                stderr_output = ef.read()` | Assigns/updates `stderr_output` with the expression on the right-hand side. (scope: def _update_predictions_data) |
| 1314 | `                    except Exception as e_read:` | Starts an `except` handler: `except Exception as e_read:`. (scope: def _update_predictions_data) |
| 1315 | `                        stderr_output = f'Failed to read stderr log: {e_read}'` | Assigns/updates `stderr_output` with the expression on the right-hand side. (scope: def _update_predictions_data) |
| 1316 | `                    self.log(f'Prediction process failed with return code {return_code}. Error: {stderr_output}', level='Error')` | Executes Python statement: `self.log(f'Prediction process failed with return code {return_code}. Error: {stderr_output}', level='Error')`. (scope: def _update_predictions_data) |
| 1317 | `                    self.prediction_process = None` | Assigns/updates `self.prediction_process` with the expression on the right-hand side. (scope: def _update_predictions_data) |
| 1318 | `                    self.predict_app_started = False` | Assigns/updates `self.predict_app_started` with the expression on the right-hand side. (scope: def _update_predictions_data) |
| 1319 | `                    return` | Returns from the current function (no explicit value, so `None`). (scope: def _update_predictions_data) |
| 1320 | `                else:` | Starts an `else` branch for the preceding conditional. (scope: def _update_predictions_data) |
| 1321 | `                    # Process finished successfully` | Comment explaining intent/context: Process finished successfully (scope: def _update_predictions_data) |
| 1322 | `                    self.log(f'Prediction process completed successfully (PID: {self.prediction_process.pid})', level='Info')` | Executes Python statement: `self.log(f'Prediction process completed successfully (PID: {self.prediction_process.pid})', level='Info')`. (scope: def _update_predictions_data) |
| 1323 | `            ` | Blank line used to separate logical sections and improve readability. (scope: def _update_predictions_data) |
| 1324 | `            if os.path.exists(predict_output_file):` | Starts an `if` block: conditional control flow based on `os.path.exists(predict_output_file)`. (scope: def _update_predictions_data) |
| 1325 | `                predictions = pd.read_csv(predict_output_file)` | Assigns/updates `predictions` with the expression on the right-hand side. (scope: def _update_predictions_data) |
| 1326 | `                predictions['x'] = pd.to_datetime(predictions['x'])` | Assigns/updates `predictions['x']` with the expression on the right-hand side. (scope: def _update_predictions_data) |
| 1327 | `                __x = predictions['x'].to_list()` | Assigns/updates `__x` with the expression on the right-hand side. (scope: def _update_predictions_data) |
| 1328 | `                __y = predictions['y'].to_list()` | Assigns/updates `__y` with the expression on the right-hand side. (scope: def _update_predictions_data) |
| 1329 | `                if len(__x)>0:` | Starts an `if` block: conditional control flow based on `len(__x)>0`. (scope: def _update_predictions_data) |
| 1330 | `                    # Merge with existing predictions to keep a continuous, deduped time series` | Comment explaining intent/context: Merge with existing predictions to keep a continuous, deduped time series (scope: def _update_predictions_data) |
| 1331 | `                    existing_times = []` | Assigns/updates `existing_times` with the expression on the right-hand side. (scope: def _update_predictions_data) |
| 1332 | `                    existing_values = []` | Assigns/updates `existing_values` with the expression on the right-hand side. (scope: def _update_predictions_data) |
| 1333 | `                    if self.predict_x_t and self.predict_y_t:` | Starts an `if` block: conditional control flow based on `self.predict_x_t and self.predict_y_t`. (scope: def _update_predictions_data) |
| 1334 | `                        for pt, pv in zip(self.predict_x_t, self.predict_y_t):` | Starts a `for` loop: iterates as described by `pt, pv in zip(self.predict_x_t, self.predict_y_t)`. (scope: def _update_predictions_data) |
| 1335 | `                            existing_times.extend(pt)` | Executes Python statement: `existing_times.extend(pt)`. (scope: def _update_predictions_data) |
| 1336 | `                            existing_values.extend(pv)` | Executes Python statement: `existing_values.extend(pv)`. (scope: def _update_predictions_data) |
| 1337 | `` | Blank line used to separate logical sections and improve readability. |
| 1338 | `                    # Preserve earlier predictions: do not overwrite existing timestamps` | Comment explaining intent/context: Preserve earlier predictions: do not overwrite existing timestamps |
| 1339 | `                    merged = {}` | Assigns/updates `merged` with the expression on the right-hand side. |
| 1340 | `                    for t, v in zip(existing_times, existing_values):` | Starts a `for` loop: iterates as described by `t, v in zip(existing_times, existing_values)`. |
| 1341 | `                        merged[t] = v` | Assigns/updates `merged[t]` with the expression on the right-hand side. |
| 1342 | `                    skipped_dupes = 0` | Assigns/updates `skipped_dupes` with the expression on the right-hand side. |
| 1343 | `                    for t, v in zip(__x, __y):` | Starts a `for` loop: iterates as described by `t, v in zip(__x, __y)`. |
| 1344 | `                        if t in merged:` | Starts an `if` block: conditional control flow based on `t in merged`. |
| 1345 | `                            skipped_dupes += 1` | Updates `skipped_dupes` using augmented assignment `+=` (reads current value, applies operation, stores back). |
| 1346 | `                            continue` | Skips to the next iteration of the current loop. |
| 1347 | `                        merged[t] = v` | Assigns/updates `merged[t]` with the expression on the right-hand side. |
| 1348 | `` | Blank line used to separate logical sections and improve readability. |
| 1349 | `                    merged_times = sorted(merged.keys())` | Assigns/updates `merged_times` with the expression on the right-hand side. |
| 1350 | `                    merged_values = [merged[t] for t in merged_times]` | Assigns/updates `merged_values` with the expression on the right-hand side. |
| 1351 | `` | Blank line used to separate logical sections and improve readability. |
| 1352 | `                    self.predict_x_t = [merged_times]` | Assigns/updates `self.predict_x_t` with the expression on the right-hand side. |
| 1353 | `                    self.predict_y_t = [merged_values]` | Assigns/updates `self.predict_y_t` with the expression on the right-hand side. |
| 1354 | `                    msg = f'Loaded latest prediction: {len(__x)} points. Combined prediction series now {len(merged_times)} points.'` | Assigns/updates `msg` with the expression on the right-hand side. |
| 1355 | `                    if skipped_dupes:` | Starts an `if` block: conditional control flow based on `skipped_dupes`. |
| 1356 | `                        msg += f' Preserved {skipped_dupes} existing timestamps (no overwrites).'` | Updates `msg` using augmented assignment `+=` (reads current value, applies operation, stores back). |
| 1357 | `                    self.log(msg, level='Info')` | Executes Python statement: `self.log(msg, level='Info')`. |
| 1358 | `                    # Force redraw of prediction line on next canvas update` | Comment explaining intent/context: Force redraw of prediction line on next canvas update |
| 1359 | `                    self.predict_last_t_in_plot = None` | Assigns/updates `self.predict_last_t_in_plot` with the expression on the right-hand side. |
| 1360 | `                    if self.prediction_process:` | Starts an `if` block: conditional control flow based on `self.prediction_process`. |
| 1361 | `                        self.prediction_process = None` | Assigns/updates `self.prediction_process` with the expression on the right-hand side. |
| 1362 | `                        self.predict_app_started = False` | Assigns/updates `self.predict_app_started` with the expression on the right-hand side. |
| 1363 | `                    ` | Blank line used to separate logical sections and improve readability. |
| 1364 | `                    # Perform anomaly detection when we have both actual and predicted data` | Comment explaining intent/context: Perform anomaly detection when we have both actual and predicted data |
| 1365 | `                    self._detect_anomalies()` | Executes Python statement: `self._detect_anomalies()`. |
| 1366 | `                    # Trigger immediate canvas update to show new predictions` | Comment explaining intent/context: Trigger immediate canvas update to show new predictions |
| 1367 | `                    self._update_canvas()` | Executes Python statement: `self._update_canvas()`. |
| 1368 | `            else:` | Starts an `else` branch for the preceding conditional. |
| 1369 | `                # Output file doesn't exist yet - process might still be running or failed` | Comment explaining intent/context: Output file doesn't exist yet - process might still be running or failed |
| 1370 | `                if self.prediction_process and self.prediction_process.poll() is None:` | Starts an `if` block: conditional control flow based on `self.prediction_process and self.prediction_process.poll() is None`. |
| 1371 | `                    self.log(f'Waiting for prediction output file: {predict_output_file}', level='Debug')` | Executes Python statement: `self.log(f'Waiting for prediction output file: {predict_output_file}', level='Debug')`. |
| 1372 | `                else:` | Starts an `else` branch for the preceding conditional. |
| 1373 | `                    work_dir = os.path.dirname(predict_input) if predict_input else APP_BASE` | Assigns/updates `work_dir` with the expression on the right-hand side. |
| 1374 | `                    stderr_log = os.path.join(work_dir, 'predict_stderr.log')` | Assigns/updates `stderr_log` with the expression on the right-hand side. |
| 1375 | `                    stderr_output = ''` | Assigns/updates `stderr_output` with the expression on the right-hand side. |
| 1376 | `                    try:` | Starts a `try` block to catch and handle exceptions. |
| 1377 | `                        if os.path.exists(stderr_log):` | Starts an `if` block: conditional control flow based on `os.path.exists(stderr_log)`. |
| 1378 | `                            with open(stderr_log, 'r') as ef:` | Starts a `with` block (context manager) for `open(stderr_log, 'r') as ef`. |
| 1379 | `                                stderr_output = ef.read()` | Assigns/updates `stderr_output` with the expression on the right-hand side. |
| 1380 | `                    except Exception as e_read:` | Starts an `except` handler: `except Exception as e_read:`. |
| 1381 | `                        stderr_output = f'Failed to read stderr log: {e_read}'` | Assigns/updates `stderr_output` with the expression on the right-hand side. |
| 1382 | `                    self.log(f'Prediction output file not found: {predict_output_file}. Process may have failed. Stderr: {stderr_output}', level='Warning')` | Executes Python statement: `self.log(f'Prediction output file not found: {predict_output_file}. Process may have failed. Stderr: {stderr_output}', level='Warning')`. |
| 1383 | `                            ` | Blank line used to separate logical sections and improve readability. |
| 1384 | `        except Exception as e:` | Starts an `except` handler: `except Exception as e:`. |
| 1385 | `            self.log('Error updating predictions data', error=e, level='Error')` | Executes Python statement: `self.log('Error updating predictions data', error=e, level='Error')`. |
| 1386 | `    ` | Blank line used to separate logical sections and improve readability. |
| 1387 | `    def _sync_threshold_spinbox(self):` | Defines function/method `_sync_threshold_spinbox` (entry point for reusable logic). (scope: def _sync_threshold_spinbox) |
| 1388 | `        """Sync the threshold spinbox with the current anomaly detector value"""` | Single-line triple-quoted string (docstring or multiline literal expressed on one line). (scope: def _sync_threshold_spinbox) |
| 1389 | `        try:` | Starts a `try` block to catch and handle exceptions. (scope: def _sync_threshold_spinbox) |
| 1390 | `            if hasattr(self, 'timeSerWidget') and hasattr(self.timeSerWidget, 'threshold_spinbox') and hasattr(self, 'anomaly_detector'):` | Starts an `if` block: conditional control flow based on `hasattr(self, 'timeSerWidget') and hasattr(self.timeSerWidget, 'threshold_spinbox') and hasattr(self, 'anomaly_detector')`. (scope: def _sync_threshold_spinbox) |
| 1391 | `                self.timeSerWidget.threshold_spinbox.setValue(self.anomaly_detector.threshold_multiplier)` | Executes Python statement: `self.timeSerWidget.threshold_spinbox.setValue(self.anomaly_detector.threshold_multiplier)`. (scope: def _sync_threshold_spinbox) |
| 1392 | `                self.log(f'Anomaly Detection: Threshold control initialized with value {self.anomaly_detector.threshold_multiplier:.2f}', level='Debug')` | Executes Python statement: `self.log(f'Anomaly Detection: Threshold control initialized with value {self.anomaly_detector.threshold_multiplier:.2f}', level='Debug')`. (scope: def _sync_threshold_spinbox) |
| 1393 | `            if hasattr(self, 'timeSerWidget') and hasattr(self.timeSerWidget, 'train_window_spinbox'):` | Starts an `if` block: conditional control flow based on `hasattr(self, 'timeSerWidget') and hasattr(self.timeSerWidget, 'train_window_spinbox')`. (scope: def _sync_threshold_spinbox) |
| 1394 | `                if self.train_window_minutes:` | Starts an `if` block: conditional control flow based on `self.train_window_minutes`. (scope: def _sync_threshold_spinbox) |
| 1395 | `                    self.timeSerWidget.train_window_spinbox.setValue(self.train_window_minutes)` | Executes Python statement: `self.timeSerWidget.train_window_spinbox.setValue(self.train_window_minutes)`. (scope: def _sync_threshold_spinbox) |
| 1396 | `                else:` | Starts an `else` branch for the preceding conditional. (scope: def _sync_threshold_spinbox) |
| 1397 | `                    self.timeSerWidget.train_window_spinbox.setValue(0)` | Executes Python statement: `self.timeSerWidget.train_window_spinbox.setValue(0)`. (scope: def _sync_threshold_spinbox) |
| 1398 | `        except Exception as e:` | Starts an `except` handler: `except Exception as e:`. (scope: def _sync_threshold_spinbox) |
| 1399 | `            self.log(f'Error syncing threshold spinbox: {e}', level='Error')` | Executes Python statement: `self.log(f'Error syncing threshold spinbox: {e}', level='Error')`. (scope: def _sync_threshold_spinbox) |
| 1400 | `    ` | Blank line used to separate logical sections and improve readability. |
