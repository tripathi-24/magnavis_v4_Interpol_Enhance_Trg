# Line-by-line documentation: `src/application.py`

- **File**: `src/application.py`
- **Lines covered in this document**: 1401–1600 (of 1983)

## Structure overview (file-level)

- Line 72: function `load_vtk_file()`
- Line 81: function `getFilePath()`
- Line 94: class `SessionDataManager`
- Line 98: function `update_api_df()`
- Line 124: class `Color`
- Line 126: function `__init__()`
- Line 134: class `DraggableLabel`
- Line 135: method `mousePressEvent()`
- Line 139: function `mouseMoveEvent()`
- Line 156: class `DropLabel`
- Line 157: method `__init__()`
- Line 161: function `dragEnterEvent()`
- Line 165: function `dropEvent()`
- Line 172: class `MapWidget`
- Line 173: method `__init__()`
- Line 177: function `initUI()`
- Line 206: class `PandasModel`
- Line 209: function `__init__()`
- Line 213: function `rowCount()`
- Line 223: function `columnCount()`
- Line 232: function `data()`
- Line 245: function `headerData()`
- Line 262: class `MagTimeSeriesWidget`
- Line 263: method `__init__()`
- Line 269: method `timerRefreshEvent()`
- Line 283: method `initWidget()`
- Line 350: function `on_threshold_changed()`
- Line 377: function `on_train_window_changed()`
- Line 390: function `update_train_window_status()`
- Line 403: class `ApplicationWindow`
- Line 406: method `__init__()`
- Line 432: method `connectSlots()`
- Line 436: method `createThread()`
- Line 447: method `updateData()`
- Line 455: method `startThreads()`
- Line 462: function `addTimeSeries()`
- Line 469: function `showCsvData()`
- Line 493: function `setTreeView()`
- Line 500: function `updateTreeView()`
- Line 507: function `createMailModel()`
- Line 514: function `addSource()`
- Line 521: function `update_visualisation()`
- Line 604: function `clear_point_actors()`
- Line 612: function `set_points()`
- Line 673: function `sphere_prop_to_vtkarray()`
- Line 691: function `build_scene()`
- Line 728: function `setup_point_picker_framework()`
- Line 746: function `on_pick_left()`
- Line 771: function `on_pick_right()`
- Line 777: function `loadFile()`
- Line 803: function `log()`
- Line 824: class `Source`
- Line 825: method `__init__()`
- Line 830: class `TimeSeriesSource`
- Line 831: method `__init__()`
- Line 837: class `World`
- Line 838: method `__init__()`
- Line 845: method `loadCsv()`
- Line 860: class `DataSourceManager`
- Line 861: method `__init__()`
- Line 877: method `loadCsv()`
- Line 883: method `updateUI()`
- Line 887: method `createTimeSeriesSource()`
- Line 892: class `Application`
- Line 893: method `__init__()`
- Line 981: function `showAppMaximized()`
- Line 985: function `log()`
- Line 988: function `_save_data()`
- Line 1039: function `load_plot_framework()`
- Line 1119: function `load_plot_framework_2()`
- Line 1178: function `on_add_static()`
- Line 1196: function `on_add_dynamic()`
- Line 1222: function `start_prediction_process()`
- Line 1261: function `_update_xydata()`
- Line 1290: function `_update_predictions_data()`
- Line 1387: function `_sync_threshold_spinbox()`
- Line 1401: function `_detect_anomalies()`
- Line 1575: function `_update_canvas()`
- Line 1589: function `on_add_new()`
- Line 1626: function `on_add_pred()`
- Line 1745: function `appWin()`
- Line 1748: function `initViews()`
- Line 1754: function `load_visualization_framework()`
- Line 1858: function `load_axes()`
- Line 1881: function `load_sphere_source()`
- Line 1898: function `load_world_grid()`
- Line 1922: function `load_map()`
- Line 1957: function `get_map_xy_from_latlon()`
- Line 1969: function `get_map_lat_lon_from_xy()`

## Line-by-line explanation

| Line | Code | Explanation |
|---:|---|---|
| 1401 | `    def _detect_anomalies(self):` | Defines function/method `_detect_anomalies` (entry point for reusable logic). (scope: def _detect_anomalies) |
| 1402 | `        """` | Ends a triple-quoted string (closing a docstring/multiline literal). (scope: def _detect_anomalies) |
| 1403 | `        Detect anomalies by comparing real-time actual data with predictions.` | Executes Python statement: `Detect anomalies by comparing real-time actual data with predictions.`. (scope: def _detect_anomalies) |
| 1404 | `        This method is called whenever new predictions are available.` | Executes Python statement: `This method is called whenever new predictions are available.`. (scope: def _detect_anomalies) |
| 1405 | `        Only compares new real-time data (green line) with predictions (purple line), not historic data.` | Executes Python statement: `Only compares new real-time data (green line) with predictions (purple line), not historic data.`. (scope: def _detect_anomalies) |
| 1406 | `        """` | Ends a triple-quoted string (closing a docstring/multiline literal). (scope: def _detect_anomalies) |
| 1407 | `        try:` | Starts a `try` block to catch and handle exceptions. (scope: def _detect_anomalies) |
| 1408 | `            # Only use real-time data (new_x_t, new_y_mag_t) for anomaly detection` | Comment explaining intent/context: Only use real-time data (new_x_t, new_y_mag_t) for anomaly detection (scope: def _detect_anomalies) |
| 1409 | `            # This is the green line - real-time data that should be compared with predictions` | Comment explaining intent/context: This is the green line - real-time data that should be compared with predictions (scope: def _detect_anomalies) |
| 1410 | `            realtime_actual_times = self.new_x_t if self.new_x_t else []` | Assigns/updates `realtime_actual_times` with the expression on the right-hand side. (scope: def _detect_anomalies) |
| 1411 | `            realtime_actual_values = self.new_y_mag_t if self.new_y_mag_t else []` | Assigns/updates `realtime_actual_values` with the expression on the right-hand side. (scope: def _detect_anomalies) |
| 1412 | `            ` | Blank line used to separate logical sections and improve readability. (scope: def _detect_anomalies) |
| 1413 | `            # Get all predictions (flatten the list of lists)` | Comment explaining intent/context: Get all predictions (flatten the list of lists) (scope: def _detect_anomalies) |
| 1414 | `            all_predicted_times = []` | Assigns/updates `all_predicted_times` with the expression on the right-hand side. (scope: def _detect_anomalies) |
| 1415 | `            all_predicted_values = []` | Assigns/updates `all_predicted_values` with the expression on the right-hand side. (scope: def _detect_anomalies) |
| 1416 | `            if self.predict_x_t and self.predict_y_t:` | Starts an `if` block: conditional control flow based on `self.predict_x_t and self.predict_y_t`. (scope: def _detect_anomalies) |
| 1417 | `                for pred_times, pred_values in zip(self.predict_x_t, self.predict_y_t):` | Starts a `for` loop: iterates as described by `pred_times, pred_values in zip(self.predict_x_t, self.predict_y_t)`. (scope: def _detect_anomalies) |
| 1418 | `                    all_predicted_times.extend(pred_times)` | Executes Python statement: `all_predicted_times.extend(pred_times)`. (scope: def _detect_anomalies) |
| 1419 | `                    all_predicted_values.extend(pred_values)` | Executes Python statement: `all_predicted_values.extend(pred_values)`. (scope: def _detect_anomalies) |
| 1420 | `            ` | Blank line used to separate logical sections and improve readability. (scope: def _detect_anomalies) |
| 1421 | `            # Only detect anomalies if we have both real-time actual and predicted data` | Comment explaining intent/context: Only detect anomalies if we have both real-time actual and predicted data (scope: def _detect_anomalies) |
| 1422 | `            if len(realtime_actual_times) > 0 and len(all_predicted_times) > 0:` | Starts an `if` block: conditional control flow based on `len(realtime_actual_times) > 0 and len(all_predicted_times) > 0`. (scope: def _detect_anomalies) |
| 1423 | `                self.log(f'Anomaly Detection: Starting detection with {len(realtime_actual_times)} real-time actual points and {len(all_predicted_times)} predicted points', level='Info')` | Executes Python statement: `self.log(f'Anomaly Detection: Starting detection with {len(realtime_actual_times)} real-time actual points and {len(all_predicted_times)} predicted points', level='Info')`. (scope: def _detect_anomalies) |
| 1424 | `                self.log(f'Anomaly Detection: Current threshold multiplier: {self.anomaly_detector.threshold_multiplier:.2f}', level='Info')` | Executes Python statement: `self.log(f'Anomaly Detection: Current threshold multiplier: {self.anomaly_detector.threshold_multiplier:.2f}', level='Info')`. (scope: def _detect_anomalies) |
| 1425 | `                self.log(f'Anomaly Detection: Before detection - prediction_errors count: {len(self.anomaly_detector.prediction_errors)}', level='Debug')` | Executes Python statement: `self.log(f'Anomaly Detection: Before detection - prediction_errors count: {len(self.anomaly_detector.prediction_errors)}', level='Debug')`. (scope: def _detect_anomalies) |
| 1426 | `                ` | Blank line used to separate logical sections and improve readability. (scope: def _detect_anomalies) |
| 1427 | `                # Detect anomalies using the AnomalyDetector` | Comment explaining intent/context: Detect anomalies using the AnomalyDetector (scope: def _detect_anomalies) |
| 1428 | `                # Only compare real-time data with predictions` | Comment explaining intent/context: Only compare real-time data with predictions (scope: def _detect_anomalies) |
| 1429 | `                # This will recalculate the threshold using the current threshold_multiplier` | Comment explaining intent/context: This will recalculate the threshold using the current threshold_multiplier (scope: def _detect_anomalies) |
| 1430 | `                anomalies_df, threshold = self.anomaly_detector.detect_anomalies(` | Assigns/updates `anomalies_df, threshold` with the expression on the right-hand side. (scope: def _detect_anomalies) |
| 1431 | `                    actual_times=realtime_actual_times,` | Assigns/updates `actual_times` with the expression on the right-hand side. (scope: def _detect_anomalies) |
| 1432 | `                    actual_values=realtime_actual_values,` | Assigns/updates `actual_values` with the expression on the right-hand side. (scope: def _detect_anomalies) |
| 1433 | `                    predicted_times=all_predicted_times,` | Assigns/updates `predicted_times` with the expression on the right-hand side. (scope: def _detect_anomalies) |
| 1434 | `                    predicted_values=all_predicted_values` | Assigns/updates `predicted_values` with the expression on the right-hand side. (scope: def _detect_anomalies) |
| 1435 | `                )` | Executes Python statement: `)`. (scope: def _detect_anomalies) |
| 1436 | `                ` | Blank line used to separate logical sections and improve readability. (scope: def _detect_anomalies) |
| 1437 | `                # Verify threshold was calculated correctly` | Comment explaining intent/context: Verify threshold was calculated correctly (scope: def _detect_anomalies) |
| 1438 | `                expected_threshold = None` | Assigns/updates `expected_threshold` with the expression on the right-hand side. (scope: def _detect_anomalies) |
| 1439 | `                if len(self.anomaly_detector.prediction_errors) >= self.anomaly_detector.min_samples_for_threshold:` | Starts an `if` block: conditional control flow based on `len(self.anomaly_detector.prediction_errors) >= self.anomaly_detector.min_samples_for_threshold`. (scope: def _detect_anomalies) |
| 1440 | `                    mean_err = np.mean(self.anomaly_detector.prediction_errors)` | Assigns/updates `mean_err` with the expression on the right-hand side. (scope: def _detect_anomalies) |
| 1441 | `                    std_err = np.std(self.anomaly_detector.prediction_errors)` | Assigns/updates `std_err` with the expression on the right-hand side. (scope: def _detect_anomalies) |
| 1442 | `                    expected_threshold = mean_err + self.anomaly_detector.threshold_multiplier * std_err` | Assigns/updates `expected_threshold` with the expression on the right-hand side. (scope: def _detect_anomalies) |
| 1443 | `                    if abs(threshold - expected_threshold) > 0.01:` | Starts an `if` block: conditional control flow based on `abs(threshold - expected_threshold) > 0.01`. (scope: def _detect_anomalies) |
| 1444 | `                        self.log(f'Anomaly Detection: WARNING - Threshold mismatch! Expected: {expected_threshold:.2f}, Got: {threshold:.2f}', level='Warning')` | Executes Python statement: `self.log(f'Anomaly Detection: WARNING - Threshold mismatch! Expected: {expected_threshold:.2f}, Got: {threshold:.2f}', level='Warning')`. (scope: def _detect_anomalies) |
| 1445 | `                ` | Blank line used to separate logical sections and improve readability. (scope: def _detect_anomalies) |
| 1446 | `                self.log(f'Anomaly Detection: Detection completed. Found {len(anomalies_df)} anomalies. Threshold used: {threshold:.2f} nT (multiplier: {self.anomaly_detector.threshold_multiplier:.2f})', level='Info')` | Executes Python statement: `self.log(f'Anomaly Detection: Detection completed. Found {len(anomalies_df)} anomalies. Threshold used: {threshold:.2f} nT (multiplier: {self.anomaly_detector.threshold_multiplier:.2f})', level='Info')`. (scope: def _detect_anomalies) |
| 1447 | `                ` | Blank line used to separate logical sections and improve readability. (scope: def _detect_anomalies) |
| 1448 | `                # Log detailed statistics for debugging` | Comment explaining intent/context: Log detailed statistics for debugging (scope: def _detect_anomalies) |
| 1449 | `                stats = self.anomaly_detector.get_statistics()` | Assigns/updates `stats` with the expression on the right-hand side. (scope: def _detect_anomalies) |
| 1450 | `                ` | Blank line used to separate logical sections and improve readability. (scope: def _detect_anomalies) |
| 1451 | `                # First, check how many matched pairs we have` | Comment explaining intent/context: First, check how many matched pairs we have (scope: def _detect_anomalies) |
| 1452 | `                differences_df = self.anomaly_detector.calculate_differences(` | Assigns/updates `differences_df` with the expression on the right-hand side. (scope: def _detect_anomalies) |
| 1453 | `                    realtime_actual_times, realtime_actual_values, all_predicted_times, all_predicted_values` | Executes Python statement: `realtime_actual_times, realtime_actual_values, all_predicted_times, all_predicted_values`. (scope: def _detect_anomalies) |
| 1454 | `                )` | Executes Python statement: `)`. (scope: def _detect_anomalies) |
| 1455 | `                ` | Blank line used to separate logical sections and improve readability. (scope: def _detect_anomalies) |
| 1456 | `                if len(differences_df) > 0:` | Starts an `if` block: conditional control flow based on `len(differences_df) > 0`. (scope: def _detect_anomalies) |
| 1457 | `                    self.log(f'Anomaly Detection: Matched {len(differences_df)} pairs between actual and predicted data', level='Info')` | Executes Python statement: `self.log(f'Anomaly Detection: Matched {len(differences_df)} pairs between actual and predicted data', level='Info')`. (scope: def _detect_anomalies) |
| 1458 | `                    max_diff = differences_df['difference'].abs().max()` | Assigns/updates `max_diff` with the expression on the right-hand side. (scope: def _detect_anomalies) |
| 1459 | `                    min_diff = differences_df['difference'].abs().min()` | Assigns/updates `min_diff` with the expression on the right-hand side. (scope: def _detect_anomalies) |
| 1460 | `                    mean_diff = differences_df['difference'].abs().mean()` | Assigns/updates `mean_diff` with the expression on the right-hand side. (scope: def _detect_anomalies) |
| 1461 | `                    std_diff = differences_df['difference'].abs().std()` | Assigns/updates `std_diff` with the expression on the right-hand side. (scope: def _detect_anomalies) |
| 1462 | `                    self.log(f'Anomaly Detection: Differences - Max: {max_diff:.2f} nT, Min: {min_diff:.2f} nT, Mean: {mean_diff:.2f} nT, Std: {std_diff:.2f} nT', level='Info')` | Executes Python statement: `self.log(f'Anomaly Detection: Differences - Max: {max_diff:.2f} nT, Min: {min_diff:.2f} nT, Mean: {mean_diff:.2f} nT, Std: {std_diff:.2f} nT', level='Info')`. (scope: def _detect_anomalies) |
| 1463 | `                    ` | Blank line used to separate logical sections and improve readability. (scope: def _detect_anomalies) |
| 1464 | `                    # Show how many would be anomalies at current threshold` | Comment explaining intent/context: Show how many would be anomalies at current threshold (scope: def _detect_anomalies) |
| 1465 | `                    if threshold is not None:` | Starts an `if` block: conditional control flow based on `threshold is not None`. (scope: def _detect_anomalies) |
| 1466 | `                        potential_anomalies = (differences_df['difference'].abs() > threshold).sum()` | Assigns/updates `potential_anomalies` with the expression on the right-hand side. (scope: def _detect_anomalies) |
| 1467 | `                        self.log(f'Anomaly Detection: {potential_anomalies} out of {len(differences_df)} matched pairs exceed threshold of {threshold:.2f} nT', level='Info')` | Executes Python statement: `self.log(f'Anomaly Detection: {potential_anomalies} out of {len(differences_df)} matched pairs exceed threshold of {threshold:.2f} nT', level='Info')`. (scope: def _detect_anomalies) |
| 1468 | `                else:` | Starts an `else` branch for the preceding conditional. (scope: def _detect_anomalies) |
| 1469 | `                    # No matches found - this is the problem!` | Comment explaining intent/context: No matches found - this is the problem! (scope: def _detect_anomalies) |
| 1470 | `                    self.log(f'Anomaly Detection: WARNING - No matched pairs found between actual and predicted data!', level='Warning')` | Executes Python statement: `self.log(f'Anomaly Detection: WARNING - No matched pairs found between actual and predicted data!', level='Warning')`. (scope: def _detect_anomalies) |
| 1471 | `                    if len(realtime_actual_times) > 0 and len(all_predicted_times) > 0:` | Starts an `if` block: conditional control flow based on `len(realtime_actual_times) > 0 and len(all_predicted_times) > 0`. (scope: def _detect_anomalies) |
| 1472 | `                        actual_start = pd.to_datetime(realtime_actual_times[0])` | Assigns/updates `actual_start` with the expression on the right-hand side. (scope: def _detect_anomalies) |
| 1473 | `                        actual_end = pd.to_datetime(realtime_actual_times[-1])` | Assigns/updates `actual_end` with the expression on the right-hand side. (scope: def _detect_anomalies) |
| 1474 | `                        pred_start = pd.to_datetime(all_predicted_times[0])` | Assigns/updates `pred_start` with the expression on the right-hand side. (scope: def _detect_anomalies) |
| 1475 | `                        pred_end = pd.to_datetime(all_predicted_times[-1])` | Assigns/updates `pred_end` with the expression on the right-hand side. (scope: def _detect_anomalies) |
| 1476 | `                        self.log(f'Anomaly Detection: Real-time actual data range: {actual_start} to {actual_end}', level='Warning')` | Executes Python statement: `self.log(f'Anomaly Detection: Real-time actual data range: {actual_start} to {actual_end}', level='Warning')`. (scope: def _detect_anomalies) |
| 1477 | `                        self.log(f'Anomaly Detection: Predicted data range: {pred_start} to {pred_end}', level='Warning')` | Executes Python statement: `self.log(f'Anomaly Detection: Predicted data range: {pred_start} to {pred_end}', level='Warning')`. (scope: def _detect_anomalies) |
| 1478 | `                        time_gap = abs((pred_start - actual_end).total_seconds() / 60)  # gap in minutes` | Assigns/updates `time_gap` with the expression on the right-hand side. (scope: def _detect_anomalies) |
| 1479 | `                        self.log(f'Anomaly Detection: Time gap between actual end and predicted start: {time_gap:.1f} minutes (tolerance: 10 minutes)', level='Warning')` | Executes Python statement: `self.log(f'Anomaly Detection: Time gap between actual end and predicted start: {time_gap:.1f} minutes (tolerance: 10 minutes)', level='Warning')`. (scope: def _detect_anomalies) |
| 1480 | `                ` | Blank line used to separate logical sections and improve readability. (scope: def _detect_anomalies) |
| 1481 | `                if stats['total_samples'] > 0:` | Starts an `if` block: conditional control flow based on `stats['total_samples'] > 0`. (scope: def _detect_anomalies) |
| 1482 | `                    self.log(f'Anomaly Detection: Statistics - Mean error: {stats["mean_error"]:.2f} nT, Std error: {stats["std_error"]:.2f} nT, Samples: {stats["total_samples"]}', level='Info')` | Executes Python statement: `self.log(f'Anomaly Detection: Statistics - Mean error: {stats["mean_error"]:.2f} nT, Std error: {stats["std_error"]:.2f} nT, Samples: {stats["total_samples"]}', level='Info')`. (scope: def _detect_anomalies) |
| 1483 | `                    self.log(f'Anomaly Detection: Current threshold multiplier: {self.anomaly_detector.threshold_multiplier:.2f}', level='Info')` | Executes Python statement: `self.log(f'Anomaly Detection: Current threshold multiplier: {self.anomaly_detector.threshold_multiplier:.2f}', level='Info')`. (scope: def _detect_anomalies) |
| 1484 | `                    self.log(f'Anomaly Detection: Threshold calculation - Mean + ({self.anomaly_detector.threshold_multiplier:.2f} × Std) = {stats["mean_error"]:.2f} + ({self.anomaly_detector.threshold_multiplier:.2f} × {stats["std_error"]:.2f}) = {threshold:.2f} nT', level='Info')` | Executes Python statement: `self.log(f'Anomaly Detection: Threshold calculation - Mean + ({self.anomaly_detector.threshold_multiplier:.2f} × Std) = {stats["mean_error"]:.2f} + ({self.anomaly_detector.threshold_multiplier:.2f} × {stats["std_error"]:.2f}) = {threshold:.2f} nT', level='Info')`. (scope: def _detect_anomalies) |
| 1485 | `                    ` | Blank line used to separate logical sections and improve readability. (scope: def _detect_anomalies) |
| 1486 | `                    # Show detailed comparison of differences vs threshold` | Comment explaining intent/context: Show detailed comparison of differences vs threshold (scope: def _detect_anomalies) |
| 1487 | `                    if len(differences_df) > 0:` | Starts an `if` block: conditional control flow based on `len(differences_df) > 0`. (scope: def _detect_anomalies) |
| 1488 | `                        max_diff = differences_df['difference'].abs().max()` | Assigns/updates `max_diff` with the expression on the right-hand side. (scope: def _detect_anomalies) |
| 1489 | `                        min_diff = differences_df['difference'].abs().min()` | Assigns/updates `min_diff` with the expression on the right-hand side. (scope: def _detect_anomalies) |
| 1490 | `                        # Count how many exceed threshold` | Comment explaining intent/context: Count how many exceed threshold (scope: def _detect_anomalies) |
| 1491 | `                        exceeding_threshold = (differences_df['difference'].abs() > threshold).sum()` | Assigns/updates `exceeding_threshold` with the expression on the right-hand side. (scope: def _detect_anomalies) |
| 1492 | `                        self.log(f'Anomaly Detection: Differences vs Threshold - Max: {max_diff:.2f} nT, Min: {min_diff:.2f} nT, Threshold: {threshold:.2f} nT', level='Info')` | Executes Python statement: `self.log(f'Anomaly Detection: Differences vs Threshold - Max: {max_diff:.2f} nT, Min: {min_diff:.2f} nT, Threshold: {threshold:.2f} nT', level='Info')`. (scope: def _detect_anomalies) |
| 1493 | `                        self.log(f'Anomaly Detection: {exceeding_threshold} out of {len(differences_df)} differences exceed threshold', level='Info')` | Executes Python statement: `self.log(f'Anomaly Detection: {exceeding_threshold} out of {len(differences_df)} differences exceed threshold', level='Info')`. (scope: def _detect_anomalies) |
| 1494 | `                        ` | Blank line used to separate logical sections and improve readability. (scope: def _detect_anomalies) |
| 1495 | `                        if len(anomalies_df) == 0 and exceeding_threshold > 0:` | Starts an `if` block: conditional control flow based on `len(anomalies_df) == 0 and exceeding_threshold > 0`. (scope: def _detect_anomalies) |
| 1496 | `                            self.log(f'Anomaly Detection: WARNING - {exceeding_threshold} differences exceed threshold but no anomalies in result! This may indicate a filtering issue.', level='Warning')` | Executes Python statement: `self.log(f'Anomaly Detection: WARNING - {exceeding_threshold} differences exceed threshold but no anomalies in result! This may indicate a filtering issue.', level='Warning')`. (scope: def _detect_anomalies) |
| 1497 | `                        elif len(anomalies_df) == 0:` | Starts an `elif` branch: additional conditional `len(anomalies_df) == 0`. (scope: def _detect_anomalies) |
| 1498 | `                            self.log(f'Anomaly Detection: No anomalies detected because all differences ({max_diff:.2f} nT max) are below threshold ({threshold:.2f} nT)', level='Info')` | Executes Python statement: `self.log(f'Anomaly Detection: No anomalies detected because all differences ({max_diff:.2f} nT max) are below threshold ({threshold:.2f} nT)', level='Info')`. (scope: def _detect_anomalies) |
| 1499 | `                else:` | Starts an `else` branch for the preceding conditional. (scope: def _detect_anomalies) |
| 1500 | `                    self.log(f'Anomaly Detection: Not enough samples for threshold calculation. Need {self.anomaly_detector.min_samples_for_threshold}, have {len(self.anomaly_detector.prediction_errors)}', level='Warning')` | Executes Python statement: `self.log(f'Anomaly Detection: Not enough samples for threshold calculation. Need {self.anomaly_detector.min_samples_for_threshold}, have {len(self.anomaly_detector.prediction_errors)}', level='Warning')`. (scope: def _detect_anomalies) |
| 1501 | `                    self.log(f'Anomaly Detection: Using default threshold: {threshold:.2f} nT (multiplier {self.anomaly_detector.threshold_multiplier:.2f} not used until enough samples)', level='Warning')` | Executes Python statement: `self.log(f'Anomaly Detection: Using default threshold: {threshold:.2f} nT (multiplier {self.anomaly_detector.threshold_multiplier:.2f} not used until enough samples)', level='Warning')`. (scope: def _detect_anomalies) |
| 1502 | `                ` | Blank line used to separate logical sections and improve readability. (scope: def _detect_anomalies) |
| 1503 | `                # Update anomaly data for visualization` | Comment explaining intent/context: Update anomaly data for visualization (scope: def _detect_anomalies) |
| 1504 | `                if not anomalies_df.empty:` | Starts an `if` block: conditional control flow based on `not anomalies_df.empty`. (scope: def _detect_anomalies) |
| 1505 | `                    # IMPORTANT: Only show anomalies where we have BOTH actual and predicted data` | Comment explaining intent/context: IMPORTANT: Only show anomalies where we have BOTH actual and predicted data (scope: def _detect_anomalies) |
| 1506 | `                    # Filter to ensure 'predicted' column is not NaN (valid match exists)` | Comment explaining intent/context: Filter to ensure 'predicted' column is not NaN (valid match exists) (scope: def _detect_anomalies) |
| 1507 | `                    # The AnomalyDetector already filtered for valid matches, so we can trust these` | Comment explaining intent/context: The AnomalyDetector already filtered for valid matches, so we can trust these (scope: def _detect_anomalies) |
| 1508 | `                    valid_anomalies = anomalies_df.dropna(subset=['predicted', 'actual'])` | Assigns/updates `valid_anomalies` with the expression on the right-hand side. (scope: def _detect_anomalies) |
| 1509 | `                    ` | Blank line used to separate logical sections and improve readability. (scope: def _detect_anomalies) |
| 1510 | `                    if len(valid_anomalies) > 0:` | Starts an `if` block: conditional control flow based on `len(valid_anomalies) > 0`. (scope: def _detect_anomalies) |
| 1511 | `                        # Since AnomalyDetector already validated matches, we can use these directly` | Comment explaining intent/context: Since AnomalyDetector already validated matches, we can use these directly (scope: def _detect_anomalies) |
| 1512 | `                        new_anomaly_times = pd.to_datetime(valid_anomalies['time']).tolist()` | Assigns/updates `new_anomaly_times` with the expression on the right-hand side. (scope: def _detect_anomalies) |
| 1513 | `                        new_anomaly_values = valid_anomalies['actual'].tolist()` | Assigns/updates `new_anomaly_values` with the expression on the right-hand side. (scope: def _detect_anomalies) |
| 1514 | `                        ` | Blank line used to separate logical sections and improve readability. (scope: def _detect_anomalies) |
| 1515 | `                        # FIXED: Accumulate anomalies instead of replacing (retain across cycles)` | Comment explaining intent/context: FIXED: Accumulate anomalies instead of replacing (retain across cycles) (scope: def _detect_anomalies) |
| 1516 | `                        # Convert existing times to datetime for comparison` | Comment explaining intent/context: Convert existing times to datetime for comparison (scope: def _detect_anomalies) |
| 1517 | `                        existing_times_set = set()` | Assigns/updates `existing_times_set` with the expression on the right-hand side. (scope: def _detect_anomalies) |
| 1518 | `                        if self.anomaly_times:` | Starts an `if` block: conditional control flow based on `self.anomaly_times`. (scope: def _detect_anomalies) |
| 1519 | `                            existing_times_set = {pd.to_datetime(t) if not isinstance(t, (datetime, pd.Timestamp)) else t for t in self.anomaly_times}` | Assigns/updates `existing_times_set` with the expression on the right-hand side. (scope: def _detect_anomalies) |
| 1520 | `                        ` | Blank line used to separate logical sections and improve readability. (scope: def _detect_anomalies) |
| 1521 | `                        # Add new anomalies, avoiding duplicates (same timestamp)` | Comment explaining intent/context: Add new anomalies, avoiding duplicates (same timestamp) (scope: def _detect_anomalies) |
| 1522 | `                        new_count = 0` | Assigns/updates `new_count` with the expression on the right-hand side. (scope: def _detect_anomalies) |
| 1523 | `                        max_anomalies = 1000  # Reasonable limit to prevent memory issues` | Assigns/updates `max_anomalies` with the expression on the right-hand side. (scope: def _detect_anomalies) |
| 1524 | `                        for new_time, new_value in zip(new_anomaly_times, new_anomaly_values):` | Starts a `for` loop: iterates as described by `new_time, new_value in zip(new_anomaly_times, new_anomaly_values)`. (scope: def _detect_anomalies) |
| 1525 | `                            new_time_dt = pd.to_datetime(new_time) if not isinstance(new_time, (datetime, pd.Timestamp)) else new_time` | Assigns/updates `new_time_dt` with the expression on the right-hand side. (scope: def _detect_anomalies) |
| 1526 | `                            if new_time_dt not in existing_times_set:` | Starts an `if` block: conditional control flow based on `new_time_dt not in existing_times_set`. (scope: def _detect_anomalies) |
| 1527 | `                                # If we're at the limit, remove oldest anomaly (FIFO)` | Comment explaining intent/context: If we're at the limit, remove oldest anomaly (FIFO) (scope: def _detect_anomalies) |
| 1528 | `                                if len(self.anomaly_times) >= max_anomalies:` | Starts an `if` block: conditional control flow based on `len(self.anomaly_times) >= max_anomalies`. (scope: def _detect_anomalies) |
| 1529 | `                                    self.anomaly_times.pop(0)` | Executes Python statement: `self.anomaly_times.pop(0)`. (scope: def _detect_anomalies) |
| 1530 | `                                    self.anomaly_values.pop(0)` | Executes Python statement: `self.anomaly_values.pop(0)`. (scope: def _detect_anomalies) |
| 1531 | `                                    # Remove from set as well` | Comment explaining intent/context: Remove from set as well (scope: def _detect_anomalies) |
| 1532 | `                                    if self.anomaly_times:` | Starts an `if` block: conditional control flow based on `self.anomaly_times`. (scope: def _detect_anomalies) |
| 1533 | `                                        oldest_time = pd.to_datetime(self.anomaly_times[0]) if not isinstance(self.anomaly_times[0], (datetime, pd.Timestamp)) else self.anomaly_times[0]` | Assigns/updates `oldest_time` with the expression on the right-hand side. (scope: def _detect_anomalies) |
| 1534 | `                                        existing_times_set.discard(oldest_time)` | Executes Python statement: `existing_times_set.discard(oldest_time)`. (scope: def _detect_anomalies) |
| 1535 | `                                ` | Blank line used to separate logical sections and improve readability. (scope: def _detect_anomalies) |
| 1536 | `                                self.anomaly_times.append(new_time_dt)` | Executes Python statement: `self.anomaly_times.append(new_time_dt)`. (scope: def _detect_anomalies) |
| 1537 | `                                self.anomaly_values.append(new_value)` | Executes Python statement: `self.anomaly_values.append(new_value)`. (scope: def _detect_anomalies) |
| 1538 | `                                existing_times_set.add(new_time_dt)` | Executes Python statement: `existing_times_set.add(new_time_dt)`. (scope: def _detect_anomalies) |
| 1539 | `                                new_count += 1` | Updates `new_count` using augmented assignment `+=` (reads current value, applies operation, stores back). (scope: def _detect_anomalies) |
| 1540 | `                        ` | Blank line used to separate logical sections and improve readability. (scope: def _detect_anomalies) |
| 1541 | `                        self.log(f'Anomaly Detection: Added {new_count} new anomalies (total: {len(self.anomaly_times)}). {len(new_anomaly_times) - new_count} duplicates skipped.', level='Info')` | Executes Python statement: `self.log(f'Anomaly Detection: Added {new_count} new anomalies (total: {len(self.anomaly_times)}). {len(new_anomaly_times) - new_count} duplicates skipped.', level='Info')`. (scope: def _detect_anomalies) |
| 1542 | `                        if len(anomalies_df) > len(valid_anomalies):` | Starts an `if` block: conditional control flow based on `len(anomalies_df) > len(valid_anomalies)`. (scope: def _detect_anomalies) |
| 1543 | `                            self.log(f'Anomaly Detection: Filtered out {len(anomalies_df) - len(valid_anomalies)} anomalies that had NaN values', level='Debug')` | Executes Python statement: `self.log(f'Anomaly Detection: Filtered out {len(anomalies_df) - len(valid_anomalies)} anomalies that had NaN values', level='Debug')`. (scope: def _detect_anomalies) |
| 1544 | `                        # Force immediate canvas update to show anomalies` | Comment explaining intent/context: Force immediate canvas update to show anomalies (scope: def _detect_anomalies) |
| 1545 | `                        QTimer.singleShot(100, self._update_canvas)` | Configures/starts a timer to periodically trigger updates without blocking the UI thread. (scope: def _detect_anomalies) |
| 1546 | `                    else:` | Starts an `else` branch for the preceding conditional. (scope: def _detect_anomalies) |
| 1547 | `                        # FIXED: Don't clear existing anomalies if no new valid anomalies found` | Comment explaining intent/context: FIXED: Don't clear existing anomalies if no new valid anomalies found (scope: def _detect_anomalies) |
| 1548 | `                        # Keep previous anomalies visible` | Comment explaining intent/context: Keep previous anomalies visible (scope: def _detect_anomalies) |
| 1549 | `                        self.log(f'Anomaly Detection: No valid anomalies found in this cycle (all had NaN values after matching). Retaining {len(self.anomaly_times)} previous anomalies.', level='Debug')` | Executes Python statement: `self.log(f'Anomaly Detection: No valid anomalies found in this cycle (all had NaN values after matching). Retaining {len(self.anomaly_times)} previous anomalies.', level='Debug')`. (scope: def _detect_anomalies) |
| 1550 | `                    ` | Blank line used to separate logical sections and improve readability. (scope: def _detect_anomalies) |
| 1551 | `                    # Log detected anomalies with detailed information` | Comment explaining intent/context: Log detected anomalies with detailed information (scope: def _detect_anomalies) |
| 1552 | `                    num_anomalies = len(anomalies_df)` | Assigns/updates `num_anomalies` with the expression on the right-hand side. (scope: def _detect_anomalies) |
| 1553 | `                    stats = self.anomaly_detector.get_statistics()` | Assigns/updates `stats` with the expression on the right-hand side. (scope: def _detect_anomalies) |
| 1554 | `                    max_diff = anomalies_df['difference'].abs().max()` | Assigns/updates `max_diff` with the expression on the right-hand side. (scope: def _detect_anomalies) |
| 1555 | `                    min_diff = anomalies_df['difference'].abs().min()` | Assigns/updates `min_diff` with the expression on the right-hand side. (scope: def _detect_anomalies) |
| 1556 | `                    avg_diff = anomalies_df['difference'].abs().mean()` | Assigns/updates `avg_diff` with the expression on the right-hand side. (scope: def _detect_anomalies) |
| 1557 | `                    ` | Blank line used to separate logical sections and improve readability. (scope: def _detect_anomalies) |
| 1558 | `                    self.log(f'Anomaly Detection: {num_anomalies} anomalies detected. Threshold: {threshold:.2f} nT. Multiplier: {self.anomaly_detector.threshold_multiplier:.2f}', level='Info')` | Executes Python statement: `self.log(f'Anomaly Detection: {num_anomalies} anomalies detected. Threshold: {threshold:.2f} nT. Multiplier: {self.anomaly_detector.threshold_multiplier:.2f}', level='Info')`. (scope: def _detect_anomalies) |
| 1559 | `                    self.log(f'Anomaly Detection: Statistics - Mean error: {stats["mean_error"]:.2f} nT, Std error: {stats["std_error"]:.2f} nT, Samples: {stats["total_samples"]}', level='Info')` | Executes Python statement: `self.log(f'Anomaly Detection: Statistics - Mean error: {stats["mean_error"]:.2f} nT, Std error: {stats["std_error"]:.2f} nT, Samples: {stats["total_samples"]}', level='Info')`. (scope: def _detect_anomalies) |
| 1560 | `                    self.log(f'Anomaly Detection: Anomaly differences - Max: {max_diff:.2f} nT, Min: {min_diff:.2f} nT, Avg: {avg_diff:.2f} nT', level='Info')` | Executes Python statement: `self.log(f'Anomaly Detection: Anomaly differences - Max: {max_diff:.2f} nT, Min: {min_diff:.2f} nT, Avg: {avg_diff:.2f} nT', level='Info')`. (scope: def _detect_anomalies) |
| 1561 | `                else:` | Starts an `else` branch for the preceding conditional. (scope: def _detect_anomalies) |
| 1562 | `                    # FIXED: Don't clear existing anomalies if no anomalies detected in this cycle` | Comment explaining intent/context: FIXED: Don't clear existing anomalies if no anomalies detected in this cycle (scope: def _detect_anomalies) |
| 1563 | `                    # Keep previous anomalies visible across cycles` | Comment explaining intent/context: Keep previous anomalies visible across cycles (scope: def _detect_anomalies) |
| 1564 | `                    stats = self.anomaly_detector.get_statistics()` | Assigns/updates `stats` with the expression on the right-hand side. (scope: def _detect_anomalies) |
| 1565 | `                    self.log(f'Anomaly Detection: No anomalies detected in this cycle. Threshold: {threshold:.2f} nT. Mean error: {stats["mean_error"]:.2f} nT. Retaining {len(self.anomaly_times)} previous anomalies.', level='Debug')` | Executes Python statement: `self.log(f'Anomaly Detection: No anomalies detected in this cycle. Threshold: {threshold:.2f} nT. Mean error: {stats["mean_error"]:.2f} nT. Retaining {len(self.anomaly_times)} previous anomalies.', level='Debug')`. (scope: def _detect_anomalies) |
| 1566 | `            else:` | Starts an `else` branch for the preceding conditional. (scope: def _detect_anomalies) |
| 1567 | `                if len(realtime_actual_times) == 0:` | Starts an `if` block: conditional control flow based on `len(realtime_actual_times) == 0`. (scope: def _detect_anomalies) |
| 1568 | `                    self.log('Anomaly Detection: No real-time actual data available for comparison', level='Debug')` | Executes Python statement: `self.log('Anomaly Detection: No real-time actual data available for comparison', level='Debug')`. (scope: def _detect_anomalies) |
| 1569 | `                if len(all_predicted_times) == 0:` | Starts an `if` block: conditional control flow based on `len(all_predicted_times) == 0`. (scope: def _detect_anomalies) |
| 1570 | `                    self.log('Anomaly Detection: No predicted data available for comparison', level='Debug')` | Executes Python statement: `self.log('Anomaly Detection: No predicted data available for comparison', level='Debug')`. (scope: def _detect_anomalies) |
| 1571 | `                    ` | Blank line used to separate logical sections and improve readability. (scope: def _detect_anomalies) |
| 1572 | `        except Exception as e:` | Starts an `except` handler: `except Exception as e:`. (scope: def _detect_anomalies) |
| 1573 | `            self.log('Error detecting anomalies', error=e, level='Error')` | Executes Python statement: `self.log('Error detecting anomalies', error=e, level='Error')`. (scope: def _detect_anomalies) |
| 1574 | `                ` | Blank line used to separate logical sections and improve readability. (scope: def _detect_anomalies) |
| 1575 | `    def _update_canvas(self):` | Defines function/method `_update_canvas` (entry point for reusable logic). (scope: def _update_canvas) |
| 1576 | `        # print('updating canvas.. with new line', self._line_new)` | Comment explaining intent/context: print('updating canvas.. with new line', self._line_new) (scope: def _update_canvas) |
| 1577 | `        try:` | Starts a `try` block to catch and handle exceptions. (scope: def _update_canvas) |
| 1578 | `            if not hasattr(self, '_new_predictions_line'):` | Starts an `if` block: conditional control flow based on `not hasattr(self, '_new_predictions_line')`. (scope: def _update_canvas) |
| 1579 | `                self._new_predictions_line = None` | Assigns/updates `self._new_predictions_line` with the expression on the right-hand side. (scope: def _update_canvas) |
| 1580 | `            if not self._line_new:` | Starts an `if` block: conditional control flow based on `not self._line_new`. (scope: def _update_canvas) |
| 1581 | `                if self.new_x_t and self.new_y_mag_t:` | Starts an `if` block: conditional control flow based on `self.new_x_t and self.new_y_mag_t`. (scope: def _update_canvas) |
| 1582 | `                    self._line_new, = self._dynamic_ax.plot(self.new_x_t, self.new_y_mag_t, color=[0.1, 0.7, 0.2])` | Assigns/updates `self._line_new,` with the expression on the right-hand side. (scope: def _update_canvas) |
| 1583 | `                    print('green line created')` | Executes Python statement: `print('green line created')`. (scope: def _update_canvas) |
| 1584 | `                    print('with data', self.new_x_t, self.new_y_mag_t)` | Executes Python statement: `print('with data', self.new_x_t, self.new_y_mag_t)`. (scope: def _update_canvas) |
| 1585 | `                    # Add cursor tooltips for new data line` | Comment explaining intent/context: Add cursor tooltips for new data line (scope: def _update_canvas) |
| 1586 | `                    if MPLCURSORS_AVAILABLE:` | Starts an `if` block: conditional control flow based on `MPLCURSORS_AVAILABLE`. (scope: def _update_canvas) |
| 1587 | `                        cursor_new = mplcursors.cursor(self._line_new, hover=True)` | Assigns/updates `cursor_new` with the expression on the right-hand side. Hint: Interactive plot tooltips via mplcursors. (scope: def _update_canvas) |
| 1588 | `                        @cursor_new.connect("add")` | Connects a Qt signal to a slot/callback so UI events trigger Python code. (scope: def _update_canvas) |
| 1589 | `                        def on_add_new(sel):` | Defines function/method `on_add_new` (entry point for reusable logic). (scope: def _update_canvas → def on_add_new) |
| 1590 | `                            idx = sel.target.index` | Assigns/updates `idx` with the expression on the right-hand side. (scope: def _update_canvas → def on_add_new) |
| 1591 | `                            if idx < len(self.new_x_t) and idx < len(self.new_y_mag_t):` | Starts an `if` block: conditional control flow based on `idx < len(self.new_x_t) and idx < len(self.new_y_mag_t)`. (scope: def _update_canvas → def on_add_new) |
| 1592 | `                                time_str = pd.to_datetime(self.new_x_t[idx]).strftime('%Y-%m-%d %H:%M:%S') if isinstance(self.new_x_t[idx], (datetime, pd.Timestamp)) else str(self.new_x_t[idx])` | Assigns/updates `time_str` with the expression on the right-hand side. (scope: def _update_canvas → def on_add_new) |
| 1593 | `                                sel.annotation.set_text(f'Time: {time_str}\\nValue: {self.new_y_mag_t[idx]:.2f} nT')` | Executes Python statement: `sel.annotation.set_text(f'Time: {time_str}\nValue: {self.new_y_mag_t[idx]:.2f} nT')`. (scope: def _update_canvas → def on_add_new) |
| 1594 | `                    self._save_data(self.x_t + self.new_x_t, self.y_mag_t + self.new_y_mag_t)` | Executes Python statement: `self._save_data(self.x_t + self.new_x_t, self.y_mag_t + self.new_y_mag_t)`. (scope: def _update_canvas) |
| 1595 | `            else:` | Starts an `else` branch for the preceding conditional. (scope: def _update_canvas) |
| 1596 | `                # self._line.set_data(self.xdata, self.ydata)` | Comment explaining intent/context: self._line.set_data(self.xdata, self.ydata) (scope: def _update_canvas) |
| 1597 | `                self._line_new.set_data(self.new_x_t, self.new_y_mag_t)` | Executes Python statement: `self._line_new.set_data(self.new_x_t, self.new_y_mag_t)`. (scope: def _update_canvas) |
| 1598 | `                self._save_data(self.x_t + self.new_x_t, self.y_mag_t + self.new_y_mag_t)` | Executes Python statement: `self._save_data(self.x_t + self.new_x_t, self.y_mag_t + self.new_y_mag_t)`. (scope: def _update_canvas) |
| 1599 | `                ` | Blank line used to separate logical sections and improve readability. (scope: def _update_canvas) |
| 1600 | `                if self.needs_update_lims:` | Starts an `if` block: conditional control flow based on `self.needs_update_lims`. (scope: def _update_canvas) |
