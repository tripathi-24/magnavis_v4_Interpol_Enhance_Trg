# Line-by-line documentation: `src/application.py`

- **File**: `src/application.py`
- **Lines covered in this document**: 1–200 (of 1983)

## Structure overview (file-level)

- Line 72: function `load_vtk_file()`
- Line 81: function `getFilePath()`
- Line 94: class `SessionDataManager`
- Line 98: function `update_api_df()`
- Line 124: class `Color`
- Line 126: function `__init__()`
- Line 134: class `DraggableLabel`
- Line 135: method `mousePressEvent()`
- Line 139: function `mouseMoveEvent()`
- Line 156: class `DropLabel`
- Line 157: method `__init__()`
- Line 161: function `dragEnterEvent()`
- Line 165: function `dropEvent()`
- Line 172: class `MapWidget`
- Line 173: method `__init__()`
- Line 177: function `initUI()`
- Line 206: class `PandasModel`
- Line 209: function `__init__()`
- Line 213: function `rowCount()`
- Line 223: function `columnCount()`
- Line 232: function `data()`
- Line 245: function `headerData()`
- Line 262: class `MagTimeSeriesWidget`
- Line 263: method `__init__()`
- Line 269: method `timerRefreshEvent()`
- Line 283: method `initWidget()`
- Line 350: function `on_threshold_changed()`
- Line 377: function `on_train_window_changed()`
- Line 390: function `update_train_window_status()`
- Line 403: class `ApplicationWindow`
- Line 406: method `__init__()`
- Line 432: method `connectSlots()`
- Line 436: method `createThread()`
- Line 447: method `updateData()`
- Line 455: method `startThreads()`
- Line 462: function `addTimeSeries()`
- Line 469: function `showCsvData()`
- Line 493: function `setTreeView()`
- Line 500: function `updateTreeView()`
- Line 507: function `createMailModel()`
- Line 514: function `addSource()`
- Line 521: function `update_visualisation()`
- Line 604: function `clear_point_actors()`
- Line 612: function `set_points()`
- Line 673: function `sphere_prop_to_vtkarray()`
- Line 691: function `build_scene()`
- Line 728: function `setup_point_picker_framework()`
- Line 746: function `on_pick_left()`
- Line 771: function `on_pick_right()`
- Line 777: function `loadFile()`
- Line 803: function `log()`
- Line 824: class `Source`
- Line 825: method `__init__()`
- Line 830: class `TimeSeriesSource`
- Line 831: method `__init__()`
- Line 837: class `World`
- Line 838: method `__init__()`
- Line 845: method `loadCsv()`
- Line 860: class `DataSourceManager`
- Line 861: method `__init__()`
- Line 877: method `loadCsv()`
- Line 883: method `updateUI()`
- Line 887: method `createTimeSeriesSource()`
- Line 892: class `Application`
- Line 893: method `__init__()`
- Line 981: function `showAppMaximized()`
- Line 985: function `log()`
- Line 988: function `_save_data()`
- Line 1039: function `load_plot_framework()`
- Line 1119: function `load_plot_framework_2()`
- Line 1178: function `on_add_static()`
- Line 1196: function `on_add_dynamic()`
- Line 1222: function `start_prediction_process()`
- Line 1261: function `_update_xydata()`
- Line 1290: function `_update_predictions_data()`
- Line 1387: function `_sync_threshold_spinbox()`
- Line 1401: function `_detect_anomalies()`
- Line 1575: function `_update_canvas()`
- Line 1589: function `on_add_new()`
- Line 1626: function `on_add_pred()`
- Line 1745: function `appWin()`
- Line 1748: function `initViews()`
- Line 1754: function `load_visualization_framework()`
- Line 1858: function `load_axes()`
- Line 1881: function `load_sphere_source()`
- Line 1898: function `load_world_grid()`
- Line 1922: function `load_map()`
- Line 1957: function `get_map_xy_from_latlon()`
- Line 1969: function `get_map_lat_lon_from_xy()`

## Line-by-line explanation

| Line | Code | Explanation |
|---:|---|---|
| 1 | `'''` | Begins a triple-quoted string (often used as a module/class/function docstring). |
| 2 | `Created on Jan 6, 2025` | Executes Python statement: `Created on Jan 6, 2025`. |
| 3 | `` | Blank line used to separate logical sections and improve readability. |
| 4 | `@author: Admin` | Executes Python statement: `@author: Admin`. |
| 5 | `'''` | Ends a triple-quoted string (closing a docstring/multiline literal). |
| 6 | `` | Blank line used to separate logical sections and improve readability. |
| 7 | `import sys, os` | Imports module(s) into this namespace: `sys, os`. |
| 8 | `import time` | Imports module(s) into this namespace: `time`. |
| 9 | `import logging` | Imports module(s) into this namespace: `logging`. |
| 10 | `from datetime import datetime, date` | Imports specific symbols using a `from … import …` statement: `from datetime import datetime, date`. |
| 11 | `` | Blank line used to separate logical sections and improve readability. |
| 12 | `import numpy as np` | Imports module(s) into this namespace: `numpy as np`. |
| 13 | `` | Blank line used to separate logical sections and improve readability. |
| 14 | `import vtk` | Imports module(s) into this namespace: `vtk`. |
| 15 | `from PyQt5 import QtCore, QtGui` | Imports specific symbols using a `from … import …` statement: `from PyQt5 import QtCore, QtGui`. |
| 16 | `from PyQt5.QtWidgets import QApplication, QMainWindow, QFileDialog, QTableView, QLabel, QSpacerItem, QSizePolicy, QSplashScreen, QWidget, QDoubleSpinBox, QHBoxLayout, QTextEdit, QSplitter` | Imports specific symbols using a `from … import …` statement: `from PyQt5.QtWidgets import QApplication, QMainWindow, QFileDialog, QTableView, QLabel, QSpacerItem, QSizePolicy, QSplashScreen, QWidget, QDoubleSpinBox, QHBoxLayout, QTextEdit, QSplitter`. |
| 17 | `from PyQt5 import Qt, uic` | Imports specific symbols using a `from … import …` statement: `from PyQt5 import Qt, uic`. |
| 18 | `from PyQt5.QtCore import pyqtSignal, QAbstractTableModel, QModelIndex, QTimer, QDateTime, QMutex, QObject, QThread` | Imports specific symbols using a `from … import …` statement: `from PyQt5.QtCore import pyqtSignal, QAbstractTableModel, QModelIndex, QTimer, QDateTime, QMutex, QObject, QThread`. |
| 19 | `from PyQt5.QtGui import QStandardItemModel, QPixmap, QPalette, QColor, QTextCursor` | Imports specific symbols using a `from … import …` statement: `from PyQt5.QtGui import QStandardItemModel, QPixmap, QPalette, QColor, QTextCursor`. |
| 20 | `from PyQt5.QtGui import QDrag, QPixmap, QPainter, QCursor` | Imports specific symbols using a `from … import …` statement: `from PyQt5.QtGui import QDrag, QPixmap, QPainter, QCursor`. |
| 21 | `from PyQt5.QtCore import QMimeData, Qt as CoreQt` | Imports specific symbols using a `from … import …` statement: `from PyQt5.QtCore import QMimeData, Qt as CoreQt`. |
| 22 | `` | Blank line used to separate logical sections and improve readability. |
| 23 | `import numpy as np` | Imports module(s) into this namespace: `numpy as np`. |
| 24 | `from vtk.qt.QVTKRenderWindowInteractor import QVTKRenderWindowInteractor` | Imports specific symbols using a `from … import …` statement: `from vtk.qt.QVTKRenderWindowInteractor import QVTKRenderWindowInteractor`. |
| 25 | `from vtkmodules.vtkCommonColor import vtkNamedColors` | Imports specific symbols using a `from … import …` statement: `from vtkmodules.vtkCommonColor import vtkNamedColors`. |
| 26 | `from vtkmodules.vtkRenderingCore import (` | Imports specific symbols using a `from … import …` statement: `from vtkmodules.vtkRenderingCore import (`. |
| 27 | `    vtkActor,` | Executes statement related to: VTK (3D visualization) usage: `vtkActor,`. |
| 28 | `    vtkPolyDataMapper,` | Executes statement related to: VTK (3D visualization) usage: `vtkPolyDataMapper,`. |
| 29 | `    vtkRenderWindow,` | Executes statement related to: VTK (3D visualization) usage: `vtkRenderWindow,`. |
| 30 | `    vtkRenderWindowInteractor,` | Executes statement related to: VTK (3D visualization) usage: `vtkRenderWindowInteractor,`. |
| 31 | `    vtkRenderer` | Executes statement related to: VTK (3D visualization) usage: `vtkRenderer`. |
| 32 | `)` | Executes Python statement: `)`. |
| 33 | `from vtkmodules.vtkInteractionWidgets import vtkOrientationMarkerWidget` | Imports specific symbols using a `from … import …` statement: `from vtkmodules.vtkInteractionWidgets import vtkOrientationMarkerWidget`. |
| 34 | `from vtkmodules.vtkRenderingAnnotation import vtkAxesActor, vtkCubeAxesActor` | Imports specific symbols using a `from … import …` statement: `from vtkmodules.vtkRenderingAnnotation import vtkAxesActor, vtkCubeAxesActor`. |
| 35 | `` | Blank line used to separate logical sections and improve readability. |
| 36 | `import matplotlib.dates as md` | Imports module(s) into this namespace: `matplotlib.dates as md`. |
| 37 | `from matplotlib.backends.backend_qtagg import FigureCanvas` | Imports specific symbols using a `from … import …` statement: `from matplotlib.backends.backend_qtagg import FigureCanvas`. |
| 38 | `from matplotlib.backends.backend_qtagg import \\` | Imports specific symbols using a `from … import …` statement: `from matplotlib.backends.backend_qtagg import \`. |
| 39 | `    NavigationToolbar2QT as NavigationToolbar` | Executes statement related to: Matplotlib plotting (Qt backend) usage: `NavigationToolbar2QT as NavigationToolbar`. |
| 40 | `# from matplotlib.backends.qt_compat import QtWidgets` | Comment explaining intent/context: from matplotlib.backends.qt_compat import QtWidgets |
| 41 | `from matplotlib.figure import Figure` | Imports specific symbols using a `from … import …` statement: `from matplotlib.figure import Figure`. |
| 42 | `# import matplotlib.pyplot as plt` | Comment explaining intent/context: import matplotlib.pyplot as plt |
| 43 | `try:` | Starts a `try` block to catch and handle exceptions. |
| 44 | `    import mplcursors` | Imports module(s) into this namespace: `mplcursors`. |
| 45 | `    MPLCURSORS_AVAILABLE = True` | Assigns/updates `MPLCURSORS_AVAILABLE` with the expression on the right-hand side. |
| 46 | `except ImportError:` | Starts an `except` handler: `except ImportError:`. |
| 47 | `    MPLCURSORS_AVAILABLE = False` | Assigns/updates `MPLCURSORS_AVAILABLE` with the expression on the right-hand side. |
| 48 | `    print('mplcursors not available - tooltips will not work. Install with: pip install mplcursors')` | Executes statement related to: Interactive plot tooltips via mplcursors: `print('mplcursors not available - tooltips will not work. Install with: pip install mplcursors')`. |
| 49 | `` | Blank line used to separate logical sections and improve readability. |
| 50 | `import pandas as pd` | Imports module(s) into this namespace: `pandas as pd`. |
| 51 | `# import libraries` | Comment explaining intent/context: import libraries |
| 52 | `import geopandas as gpd` | Imports module(s) into this namespace: `geopandas as gpd`. |
| 53 | `from shapely.geometry import Point, Polygon` | Imports specific symbols using a `from … import …` statement: `from shapely.geometry import Point, Polygon`. |
| 54 | `from pygeomag import GeoMag` | Imports specific symbols using a `from … import …` statement: `from pygeomag import GeoMag`. |
| 55 | `` | Blank line used to separate logical sections and improve readability. |
| 56 | `import uuid` | Imports module(s) into this namespace: `uuid`. |
| 57 | `import subprocess` | Imports module(s) into this namespace: `subprocess`. |
| 58 | `` | Blank line used to separate logical sections and improve readability. |
| 59 | `from pickle import NONE` | Imports specific symbols using a `from … import …` statement: `from pickle import NONE`. |
| 60 | `from PyQt5.uic.Compiler.qtproxies import QtWidgets` | Imports specific symbols using a `from … import …` statement: `from PyQt5.uic.Compiler.qtproxies import QtWidgets`. |
| 61 | `` | Blank line used to separate logical sections and improve readability. |
| 62 | `from data_convert_now import get_timeseries_magnetic_data` | Imports specific symbols using a `from … import …` statement: `from data_convert_now import get_timeseries_magnetic_data`. |
| 63 | `# from predictor_ai import LSTMPredictor` | Comment explaining intent/context: from predictor_ai import LSTMPredictor |
| 64 | `from Anomaly_detector import AnomalyDetector` | Imports specific symbols using a `from … import …` statement: `from Anomaly_detector import AnomalyDetector`. |
| 65 | `` | Blank line used to separate logical sections and improve readability. |
| 66 | `APP_BASE = os.path.dirname(__file__)` | Assigns/updates `APP_BASE` with the expression on the right-hand side. |
| 67 | `` | Blank line used to separate logical sections and improve readability. |
| 68 | `# print(APP_BASE)` | Comment explaining intent/context: print(APP_BASE) |
| 69 | `` | Blank line used to separate logical sections and improve readability. |
| 70 | `colors = vtkNamedColors()` | Assigns/updates `colors` with the expression on the right-hand side. Hint: VTK (3D visualization) usage. |
| 71 | `` | Blank line used to separate logical sections and improve readability. |
| 72 | `def load_vtk_file(path: str):` | Defines function/method `load_vtk_file` (entry point for reusable logic). (scope: def load_vtk_file) |
| 73 | `    # reader = vtk.vtkPolyDataReader()` | Comment explaining intent/context: reader = vtk.vtkPolyDataReader() (scope: def load_vtk_file) |
| 74 | `    reader = vtk.vtkUnstructuredGridReader()` | Assigns/updates `reader` with the expression on the right-hand side. Hint: VTK (3D visualization) usage. (scope: def load_vtk_file) |
| 75 | `    reader.SetFileName(path)` | Executes Python statement: `reader.SetFileName(path)`. (scope: def load_vtk_file) |
| 76 | `    reader.ReadAllScalarsOn()` | Executes Python statement: `reader.ReadAllScalarsOn()`. (scope: def load_vtk_file) |
| 77 | `    reader.ReadAllVectorsOn()` | Executes Python statement: `reader.ReadAllVectorsOn()`. (scope: def load_vtk_file) |
| 78 | `    reader.Update()` | Executes Python statement: `reader.Update()`. (scope: def load_vtk_file) |
| 79 | `    return reader` | Returns value(s) from the current function: `reader`. (scope: def load_vtk_file) |
| 80 | `` | Blank line used to separate logical sections and improve readability. |
| 81 | `def getFilePath(title):` | Defines function/method `getFilePath` (entry point for reusable logic). (scope: def getFilePath) |
| 82 | `    qfd = QFileDialog()` | Assigns/updates `qfd` with the expression on the right-hand side. Hint: PyQt5/Qt GUI framework usage. (scope: def getFilePath) |
| 83 | `    # Use current working directory or home directory as default (cross-platform)` | Comment explaining intent/context: Use current working directory or home directory as default (cross-platform) (scope: def getFilePath) |
| 84 | `    path = os.path.expanduser("~")  # Default to user's home directory` | Assigns/updates `path` with the expression on the right-hand side. (scope: def getFilePath) |
| 85 | `    f_type = "csv(*.csv)"` | Assigns/updates `f_type` with the expression on the right-hand side. (scope: def getFilePath) |
| 86 | `    f = QFileDialog.getOpenFileName(qfd, title, path, f_type)` | Assigns/updates `f` with the expression on the right-hand side. Hint: PyQt5/Qt GUI framework usage. (scope: def getFilePath) |
| 87 | `    return f` | Returns value(s) from the current function: `f`. (scope: def getFilePath) |
| 88 | `` | Blank line used to separate logical sections and improve readability. |
| 89 | `api_df = pd.DataFrame() #columns=['time_H', 'mag_H_nT'])` | Assigns/updates `api_df` with the expression on the right-hand side. |
| 90 | `api_df_new = pd.DataFrame() #columns=['time_H', 'mag_H_nT'])` | Assigns/updates `api_df_new` with the expression on the right-hand side. |
| 91 | `mutex = QMutex()` | Assigns/updates `mutex` with the expression on the right-hand side. Hint: PyQt5/Qt GUI framework usage. |
| 92 | `` | Blank line used to separate logical sections and improve readability. |
| 93 | `` | Blank line used to separate logical sections and improve readability. |
| 94 | `class SessionDataManager(QObject):` | Defines class `SessionDataManager` (starts a new type/namespace for related behavior). (scope: class SessionDataManager) |
| 95 | `    finished = pyqtSignal()` | Declares a Qt signal (used for thread-safe event notifications between objects/threads). (scope: class SessionDataManager) |
| 96 | `    updatedData = pyqtSignal()` | Declares a Qt signal (used for thread-safe event notifications between objects/threads). (scope: class SessionDataManager) |
| 97 | `` | Blank line used to separate logical sections and improve readability. |
| 98 | `    def update_api_df(self, session_id, hours=None, start_time=None, new=False):` | Defines function/method `update_api_df` (entry point for reusable logic). (scope: def update_api_df) |
| 99 | `        logging.info(f'session {session_id} want to fetch new data')` | Executes Python statement: `logging.info(f'session {session_id} want to fetch new data')`. (scope: def update_api_df) |
| 100 | `        global api_df` | Executes Python statement: `global api_df`. (scope: def update_api_df) |
| 101 | `        global api_df_new` | Executes Python statement: `global api_df_new`. (scope: def update_api_df) |
| 102 | `        mutex.lock()` | Executes Python statement: `mutex.lock()`. (scope: def update_api_df) |
| 103 | `        try:` | Starts a `try` block to catch and handle exceptions. (scope: def update_api_df) |
| 104 | `            if new:` | Starts an `if` block: conditional control flow based on `new`. (scope: def update_api_df) |
| 105 | `                # print('new ...., getting data')` | Comment explaining intent/context: print('new ...., getting data') (scope: def update_api_df) |
| 106 | `                api_df_new = get_timeseries_magnetic_data(session_id, hours=hours, start_time=start_time)` | Assigns/updates `api_df_new` with the expression on the right-hand side. (scope: def update_api_df) |
| 107 | `                if api_df_new is None or api_df_new.empty:` | Starts an `if` block: conditional control flow based on `api_df_new is None or api_df_new.empty`. (scope: def update_api_df) |
| 108 | `                    api_df_new = pd.DataFrame(columns=['time_H', 'mag_H_nT'])` | Assigns/updates `api_df_new` with the expression on the right-hand side. (scope: def update_api_df) |
| 109 | `            else:` | Starts an `else` branch for the preceding conditional. (scope: def update_api_df) |
| 110 | `                api_df = get_timeseries_magnetic_data(session_id, hours=hours, start_time=start_time)` | Assigns/updates `api_df` with the expression on the right-hand side. (scope: def update_api_df) |
| 111 | `                if api_df is None or api_df.empty:` | Starts an `if` block: conditional control flow based on `api_df is None or api_df.empty`. (scope: def update_api_df) |
| 112 | `                    api_df = pd.DataFrame(columns=['time_H', 'mag_H_nT'])` | Assigns/updates `api_df` with the expression on the right-hand side. (scope: def update_api_df) |
| 113 | `            logging.info('data fetched from api in non blocking mode')` | Executes Python statement: `logging.info('data fetched from api in non blocking mode')`. (scope: def update_api_df) |
| 114 | `        except Exception as e:` | Starts an `except` handler: `except Exception as e:`. (scope: def update_api_df) |
| 115 | `            logging.error(f'issue fetching api data: {e}')` | Executes Python statement: `logging.error(f'issue fetching api data: {e}')`. (scope: def update_api_df) |
| 116 | `            if new:` | Starts an `if` block: conditional control flow based on `new`. (scope: def update_api_df) |
| 117 | `                api_df_new = pd.DataFrame(columns=['time_H', 'mag_H_nT'])` | Assigns/updates `api_df_new` with the expression on the right-hand side. (scope: def update_api_df) |
| 118 | `            else:` | Starts an `else` branch for the preceding conditional. (scope: def update_api_df) |
| 119 | `                api_df = pd.DataFrame(columns=['time_H', 'mag_H_nT'])` | Assigns/updates `api_df` with the expression on the right-hand side. (scope: def update_api_df) |
| 120 | `        self.updatedData.emit()` | Executes Python statement: `self.updatedData.emit()`. (scope: def update_api_df) |
| 121 | `        mutex.unlock()` | Executes Python statement: `mutex.unlock()`. (scope: def update_api_df) |
| 122 | `        self.finished.emit()` | Executes Python statement: `self.finished.emit()`. (scope: def update_api_df) |
| 123 | `` | Blank line used to separate logical sections and improve readability. |
| 124 | `class Color(QWidget):` | Defines class `Color` (starts a new type/namespace for related behavior). (scope: class Color) |
| 125 | `` | Blank line used to separate logical sections and improve readability. |
| 126 | `    def __init__(self, color):` | Defines function/method `__init__` (entry point for reusable logic). (scope: def __init__) |
| 127 | `        super().__init__()` | Executes Python statement: `super().__init__()`. (scope: def __init__) |
| 128 | `        self.setAutoFillBackground(True)` | Executes Python statement: `self.setAutoFillBackground(True)`. (scope: def __init__) |
| 129 | `` | Blank line used to separate logical sections and improve readability. |
| 130 | `        palette = self.palette()` | Assigns/updates `palette` with the expression on the right-hand side. |
| 131 | `        palette.setColor(QPalette.Window, QColor(color))` | Executes Python statement: `palette.setColor(QPalette.Window, QColor(color))`. |
| 132 | `        self.setPalette(palette)` | Executes Python statement: `self.setPalette(palette)`. |
| 133 | `` | Blank line used to separate logical sections and improve readability. |
| 134 | `class DraggableLabel(QLabel):` | Defines class `DraggableLabel` (starts a new type/namespace for related behavior). (scope: class DraggableLabel) |
| 135 | `    def mousePressEvent(self, event):` | Defines function/method `mousePressEvent` (entry point for reusable logic). (scope: class DraggableLabel → def mousePressEvent) |
| 136 | `        if event.button() == CoreQt.LeftButton:` | Starts an `if` block: conditional control flow based on `event.button() == CoreQt.LeftButton`. (scope: class DraggableLabel → def mousePressEvent) |
| 137 | `            self.drag_start_position = event.pos()` | Assigns/updates `self.drag_start_position` with the expression on the right-hand side. (scope: class DraggableLabel → def mousePressEvent) |
| 138 | `` | Blank line used to separate logical sections and improve readability. |
| 139 | `    def mouseMoveEvent(self, event):` | Defines function/method `mouseMoveEvent` (entry point for reusable logic). (scope: def mouseMoveEvent) |
| 140 | `        if not (event.buttons() & CoreQt.LeftButton):` | Starts an `if` block: conditional control flow based on `not (event.buttons() & CoreQt.LeftButton)`. (scope: def mouseMoveEvent) |
| 141 | `            return` | Returns from the current function (no explicit value, so `None`). (scope: def mouseMoveEvent) |
| 142 | `        if (event.pos() - self.drag_start_position).manhattanLength() < QApplication.startDragDistance():` | Starts an `if` block: conditional control flow based on `(event.pos() - self.drag_start_position).manhattanLength() < QApplication.startDragDistance()`. (scope: def mouseMoveEvent) |
| 143 | `            return` | Returns from the current function (no explicit value, so `None`). (scope: def mouseMoveEvent) |
| 144 | `        drag = QDrag(self)` | Assigns/updates `drag` with the expression on the right-hand side. (scope: def mouseMoveEvent) |
| 145 | `        mimedata = QMimeData()` | Assigns/updates `mimedata` with the expression on the right-hand side. (scope: def mouseMoveEvent) |
| 146 | `        mimedata.setText(self.text())` | Executes Python statement: `mimedata.setText(self.text())`. (scope: def mouseMoveEvent) |
| 147 | `        drag.setMimeData(mimedata)` | Executes Python statement: `drag.setMimeData(mimedata)`. (scope: def mouseMoveEvent) |
| 148 | `        pixmap = QPixmap(self.size())` | Assigns/updates `pixmap` with the expression on the right-hand side. (scope: def mouseMoveEvent) |
| 149 | `        painter = QPainter(pixmap)` | Assigns/updates `painter` with the expression on the right-hand side. (scope: def mouseMoveEvent) |
| 150 | `        painter.drawPixmap(self.rect(), self.grab())` | Executes Python statement: `painter.drawPixmap(self.rect(), self.grab())`. (scope: def mouseMoveEvent) |
| 151 | `        painter.end()` | Executes Python statement: `painter.end()`. (scope: def mouseMoveEvent) |
| 152 | `        drag.setPixmap(pixmap)` | Executes Python statement: `drag.setPixmap(pixmap)`. (scope: def mouseMoveEvent) |
| 153 | `        drag.setHotSpot(event.pos())` | Executes Python statement: `drag.setHotSpot(event.pos())`. (scope: def mouseMoveEvent) |
| 154 | `        drag.exec_(CoreQt.CopyAction \| CoreQt.MoveAction)` | Executes Python statement: `drag.exec_(CoreQt.CopyAction | CoreQt.MoveAction)`. (scope: def mouseMoveEvent) |
| 155 | `` | Blank line used to separate logical sections and improve readability. |
| 156 | `class DropLabel(QLabel):` | Defines class `DropLabel` (starts a new type/namespace for related behavior). (scope: class DropLabel) |
| 157 | `    def __init__(self, *args, **kwargs):` | Defines function/method `__init__` (entry point for reusable logic). (scope: class DropLabel → def __init__) |
| 158 | `        QLabel.__init__(self, *args, **kwargs)` | Executes statement related to: PyQt5/Qt GUI framework usage: `QLabel.__init__(self, *args, **kwargs)`. (scope: class DropLabel → def __init__) |
| 159 | `        self.setAcceptDrops(True)` | Executes Python statement: `self.setAcceptDrops(True)`. (scope: class DropLabel → def __init__) |
| 160 | `` | Blank line used to separate logical sections and improve readability. |
| 161 | `    def dragEnterEvent(self, event):` | Defines function/method `dragEnterEvent` (entry point for reusable logic). (scope: def dragEnterEvent) |
| 162 | `        if event.mimeData().hasText():` | Starts an `if` block: conditional control flow based on `event.mimeData().hasText()`. (scope: def dragEnterEvent) |
| 163 | `            event.acceptProposedAction()` | Executes Python statement: `event.acceptProposedAction()`. (scope: def dragEnterEvent) |
| 164 | `` | Blank line used to separate logical sections and improve readability. |
| 165 | `    def dropEvent(self, event):` | Defines function/method `dropEvent` (entry point for reusable logic). (scope: def dropEvent) |
| 166 | `        pos = event.pos()` | Assigns/updates `pos` with the expression on the right-hand side. (scope: def dropEvent) |
| 167 | `        text = event.mimeData().text()` | Assigns/updates `text` with the expression on the right-hand side. (scope: def dropEvent) |
| 168 | `        self.setText(text)` | Executes Python statement: `self.setText(text)`. (scope: def dropEvent) |
| 169 | `        event.acceptProposedAction()` | Executes Python statement: `event.acceptProposedAction()`. (scope: def dropEvent) |
| 170 | `` | Blank line used to separate logical sections and improve readability. |
| 171 | `` | Blank line used to separate logical sections and improve readability. |
| 172 | `class MapWidget(QWidget):` | Defines class `MapWidget` (starts a new type/namespace for related behavior). (scope: class MapWidget) |
| 173 | `    def __init__(self):` | Defines function/method `__init__` (entry point for reusable logic). (scope: class MapWidget → def __init__) |
| 174 | `        super().__init__()` | Executes Python statement: `super().__init__()`. (scope: class MapWidget → def __init__) |
| 175 | `        self.initUI()` | Executes Python statement: `self.initUI()`. (scope: class MapWidget → def __init__) |
| 176 | `` | Blank line used to separate logical sections and improve readability. |
| 177 | `    def initUI(self):` | Defines function/method `initUI` (entry point for reusable logic). (scope: def initUI) |
| 178 | `        label = DropLabel("drop there",self)` | Assigns/updates `label` with the expression on the right-hand side. (scope: def initUI) |
| 179 | `        # label.setGeometry(190, 65, 100, 100)` | Comment explaining intent/context: label.setGeometry(190, 65, 100, 100) (scope: def initUI) |
| 180 | `        # layout = QVBoxLayout()` | Comment explaining intent/context: layout = QVBoxLayout() (scope: def initUI) |
| 181 | `        label_to_drag1 = DraggableLabel("drag this 1",self)` | Assigns/updates `label_to_drag1` with the expression on the right-hand side. (scope: def initUI) |
| 182 | `        # label_to_drag1.setGeometry(10, 10, 100,100)` | Comment explaining intent/context: label_to_drag1.setGeometry(10, 10, 100,100) (scope: def initUI) |
| 183 | `        label_to_drag2 = DraggableLabel("drag this 2",self)` | Assigns/updates `label_to_drag2` with the expression on the right-hand side. (scope: def initUI) |
| 184 | `        ` | Blank line used to separate logical sections and improve readability. (scope: def initUI) |
| 185 | `        layout1 = Qt.QHBoxLayout()` | Assigns/updates `layout1` with the expression on the right-hand side. (scope: def initUI) |
| 186 | `        layout2 = Qt.QVBoxLayout()` | Assigns/updates `layout2` with the expression on the right-hand side. (scope: def initUI) |
| 187 | `        layout3 = Qt.QVBoxLayout()` | Assigns/updates `layout3` with the expression on the right-hand side. (scope: def initUI) |
| 188 | `` | Blank line used to separate logical sections and improve readability. |
| 189 | `        layout2.addWidget(Color('red'))` | Executes Python statement: `layout2.addWidget(Color('red'))`. |
| 190 | `        layout2.addWidget(Color('yellow'))` | Executes Python statement: `layout2.addWidget(Color('yellow'))`. |
| 191 | `        layout2.addWidget(Color('purple'))` | Executes Python statement: `layout2.addWidget(Color('purple'))`. |
| 192 | `        layout2.addWidget(label_to_drag1)` | Executes Python statement: `layout2.addWidget(label_to_drag1)`. |
| 193 | `        layout2.addWidget(label_to_drag2)` | Executes Python statement: `layout2.addWidget(label_to_drag2)`. |
| 194 | `` | Blank line used to separate logical sections and improve readability. |
| 195 | `        layout1.addLayout( layout2 )` | Executes Python statement: `layout1.addLayout( layout2 )`. |
| 196 | `        layout1.addLayout( layout3 )` | Executes Python statement: `layout1.addLayout( layout3 )`. |
| 197 | `` | Blank line used to separate logical sections and improve readability. |
| 198 | `        # layout1.addWidget(Color('green'))` | Comment explaining intent/context: layout1.addWidget(Color('green')) |
| 199 | `` | Blank line used to separate logical sections and improve readability. |
| 200 | `        layout3.addWidget(Color('orange'))` | Executes Python statement: `layout3.addWidget(Color('orange'))`. |
