# Line-by-line documentation: `src/application.py`

- **File**: `src/application.py`
- **Lines covered in this document**: 1801–1983 (of 1983)

## Structure overview (file-level)

- Line 72: function `load_vtk_file()`
- Line 81: function `getFilePath()`
- Line 94: class `SessionDataManager`
- Line 98: function `update_api_df()`
- Line 124: class `Color`
- Line 126: function `__init__()`
- Line 134: class `DraggableLabel`
- Line 135: method `mousePressEvent()`
- Line 139: function `mouseMoveEvent()`
- Line 156: class `DropLabel`
- Line 157: method `__init__()`
- Line 161: function `dragEnterEvent()`
- Line 165: function `dropEvent()`
- Line 172: class `MapWidget`
- Line 173: method `__init__()`
- Line 177: function `initUI()`
- Line 206: class `PandasModel`
- Line 209: function `__init__()`
- Line 213: function `rowCount()`
- Line 223: function `columnCount()`
- Line 232: function `data()`
- Line 245: function `headerData()`
- Line 262: class `MagTimeSeriesWidget`
- Line 263: method `__init__()`
- Line 269: method `timerRefreshEvent()`
- Line 283: method `initWidget()`
- Line 350: function `on_threshold_changed()`
- Line 377: function `on_train_window_changed()`
- Line 390: function `update_train_window_status()`
- Line 403: class `ApplicationWindow`
- Line 406: method `__init__()`
- Line 432: method `connectSlots()`
- Line 436: method `createThread()`
- Line 447: method `updateData()`
- Line 455: method `startThreads()`
- Line 462: function `addTimeSeries()`
- Line 469: function `showCsvData()`
- Line 493: function `setTreeView()`
- Line 500: function `updateTreeView()`
- Line 507: function `createMailModel()`
- Line 514: function `addSource()`
- Line 521: function `update_visualisation()`
- Line 604: function `clear_point_actors()`
- Line 612: function `set_points()`
- Line 673: function `sphere_prop_to_vtkarray()`
- Line 691: function `build_scene()`
- Line 728: function `setup_point_picker_framework()`
- Line 746: function `on_pick_left()`
- Line 771: function `on_pick_right()`
- Line 777: function `loadFile()`
- Line 803: function `log()`
- Line 824: class `Source`
- Line 825: method `__init__()`
- Line 830: class `TimeSeriesSource`
- Line 831: method `__init__()`
- Line 837: class `World`
- Line 838: method `__init__()`
- Line 845: method `loadCsv()`
- Line 860: class `DataSourceManager`
- Line 861: method `__init__()`
- Line 877: method `loadCsv()`
- Line 883: method `updateUI()`
- Line 887: method `createTimeSeriesSource()`
- Line 892: class `Application`
- Line 893: method `__init__()`
- Line 981: function `showAppMaximized()`
- Line 985: function `log()`
- Line 988: function `_save_data()`
- Line 1039: function `load_plot_framework()`
- Line 1119: function `load_plot_framework_2()`
- Line 1178: function `on_add_static()`
- Line 1196: function `on_add_dynamic()`
- Line 1222: function `start_prediction_process()`
- Line 1261: function `_update_xydata()`
- Line 1290: function `_update_predictions_data()`
- Line 1387: function `_sync_threshold_spinbox()`
- Line 1401: function `_detect_anomalies()`
- Line 1575: function `_update_canvas()`
- Line 1589: function `on_add_new()`
- Line 1626: function `on_add_pred()`
- Line 1745: function `appWin()`
- Line 1748: function `initViews()`
- Line 1754: function `load_visualization_framework()`
- Line 1858: function `load_axes()`
- Line 1881: function `load_sphere_source()`
- Line 1898: function `load_world_grid()`
- Line 1922: function `load_map()`
- Line 1957: function `get_map_xy_from_latlon()`
- Line 1969: function `get_map_lat_lon_from_xy()`

## Line-by-line explanation

| Line | Code | Explanation |
|---:|---|---|
| 1801 | `        # class HoverCallback:` | Comment explaining intent/context: class HoverCallback: |
| 1802 | `        #` | Comment line used as a visual separator. |
| 1803 | `        #     def __call__(self, caller, ev):` | Comment explaining intent/context: def __call__(self, caller, ev): |
| 1804 | `        #         if ev == 'TimerEvent':` | Comment explaining intent/context: if ev == 'TimerEvent': |
| 1805 | `        #             print('TimerEvent -> The mouse stopped moving and the widget hovered.')` | Comment explaining intent/context: print('TimerEvent -> The mouse stopped moving and the widget hovered.') |
| 1806 | `        #         if ev == 'EndInteractionEvent':` | Comment explaining intent/context: if ev == 'EndInteractionEvent': |
| 1807 | `        #             print('EndInteractionEvent -> The mouse started to move.')` | Comment explaining intent/context: print('EndInteractionEvent -> The mouse started to move.') |
| 1808 | `        #         x, y = appself.iren.GetEventPosition()` | Comment explaining intent/context: x, y = appself.iren.GetEventPosition() |
| 1809 | `        #         renderer = appself.iren.FindPokedRenderer(x, y)` | Comment explaining intent/context: renderer = appself.iren.FindPokedRenderer(x, y) |
| 1810 | `        #         print(x, y)` | Comment explaining intent/context: print(x, y) |
| 1811 | `        #         picker = vtk.vtkPointPicker()` | Comment explaining intent/context: picker = vtk.vtkPointPicker() |
| 1812 | `        #         picker.SetTolerance(0.5)` | Comment explaining intent/context: picker.SetTolerance(0.5) |
| 1813 | `        #         picker.Pick(x, y, 0, renderer) #appself.iren.GetRenderWindow().GetRenderers().GetFirstRenderer())` | Comment explaining intent/context: picker.Pick(x, y, 0, renderer) #appself.iren.GetRenderWindow().GetRenderers().GetFirstRenderer()) |
| 1814 | `        #` | Comment line used as a visual separator. |
| 1815 | `        #         point_idx = picker.GetPointId()` | Comment explaining intent/context: point_idx = picker.GetPointId() |
| 1816 | `        #         print(point_idx)` | Comment explaining intent/context: print(point_idx) |
| 1817 | `        #         if point_idx != -1:` | Comment explaining intent/context: if point_idx != -1: |
| 1818 | `        #             mesh = picker.GetDataSet()` | Comment explaining intent/context: mesh = picker.GetDataSet() |
| 1819 | `        #             print('picked', mesh)` | Comment explaining intent/context: print('picked', mesh) |
| 1820 | `        #         # picker.Pick(x, y, 0, renderer)` | Comment explaining intent/context: # picker.Pick(x, y, 0, renderer) |
| 1821 | `        #         # point_idx = picker.GetPointId()` | Comment explaining intent/context: # point_idx = picker.GetPointId() |
| 1822 | `        #         #` | Comment explaining intent/context: # |
| 1823 | `        #         # if point_idx != -1:` | Comment explaining intent/context: # if point_idx != -1: |
| 1824 | `        #         #     mesh = picker.GetDataSet()` | Comment explaining intent/context: #     mesh = picker.GetDataSet() |
| 1825 | `        #         #     print(mesh.field_data['name'][0], event_name, point_idx, mesh.point_data['data'][point_idx])` | Comment explaining intent/context: #     print(mesh.field_data['name'][0], event_name, point_idx, mesh.point_data['data'][point_idx]) |
| 1826 | `        #` | Comment line used as a visual separator. |
| 1827 | `        # hoverCallback = HoverCallback()` | Comment explaining intent/context: hoverCallback = HoverCallback() |
| 1828 | `        # hover_widget.AddObserver('TimerEvent', hoverCallback)` | Comment explaining intent/context: hover_widget.AddObserver('TimerEvent', hoverCallback) |
| 1829 | `        # hover_widget.AddObserver('EndInteractionEvent', hoverCallback)` | Comment explaining intent/context: hover_widget.AddObserver('EndInteractionEvent', hoverCallback) |
| 1830 | `        # ##################` | Comment explaining intent/context: ################## |
| 1831 | `        ` | Blank line used to separate logical sections and improve readability. |
| 1832 | `        ` | Blank line used to separate logical sections and improve readability. |
| 1833 | `        ` | Blank line used to separate logical sections and improve readability. |
| 1834 | `        # self.load_sphere_source()` | Comment explaining intent/context: self.load_sphere_source() |
| 1835 | `        ` | Blank line used to separate logical sections and improve readability. |
| 1836 | `        temp_blocked_for_other_analysis = True` | Assigns/updates `temp_blocked_for_other_analysis` with the expression on the right-hand side. |
| 1837 | `        if not temp_blocked_for_other_analysis:` | Starts an `if` block: conditional control flow based on `not temp_blocked_for_other_analysis`. |
| 1838 | `            self.load_world_grid()` | Executes Python statement: `self.load_world_grid()`. |
| 1839 | `            self.load_map()` | Executes Python statement: `self.load_map()`. |
| 1840 | `        else:` | Starts an `else` branch for the preceding conditional. |
| 1841 | `            pass # code related to other temporary analysis` | Executes Python statement: `pass # code related to other temporary analysis`. |
| 1842 | `        ` | Blank line used to separate logical sections and improve readability. |
| 1843 | `        ` | Blank line used to separate logical sections and improve readability. |
| 1844 | `        ` | Blank line used to separate logical sections and improve readability. |
| 1845 | `        # hover_widget.On()` | Comment explaining intent/context: hover_widget.On() |
| 1846 | `        self.load_axes()` | Executes Python statement: `self.load_axes()`. |
| 1847 | `        self.ren.ResetCamera()` | Executes Python statement: `self.ren.ResetCamera()`. |
| 1848 | `        self.iren.Initialize()` | Executes Python statement: `self.iren.Initialize()`. |
| 1849 | `        self.iren.Start()` | Executes Python statement: `self.iren.Start()`. |
| 1850 | `        self.ren.Render()` | Executes Python statement: `self.ren.Render()`. |
| 1851 | `        self.appWin.setup_point_picker_framework()` | Executes Python statement: `self.appWin.setup_point_picker_framework()`. |
| 1852 | `        # self.hover_widget= hover_widget` | Comment explaining intent/context: self.hover_widget= hover_widget |
| 1853 | `        # hover_widget.On()` | Comment explaining intent/context: hover_widget.On() |
| 1854 | `    ` | Blank line used to separate logical sections and improve readability. |
| 1855 | `` | Blank line used to separate logical sections and improve readability. |
| 1856 | `        ` | Blank line used to separate logical sections and improve readability. |
| 1857 | `        ` | Blank line used to separate logical sections and improve readability. |
| 1858 | `    def load_axes(self):` | Defines function/method `load_axes` (entry point for reusable logic). (scope: def load_axes) |
| 1859 | `        # ########################` | Comment explaining intent/context: ######################## (scope: def load_axes) |
| 1860 | `        axesActor1 = vtk.vtkAxesActor()` | Assigns/updates `axesActor1` with the expression on the right-hand side. Hint: VTK (3D visualization) usage. (scope: def load_axes) |
| 1861 | `        self.axes = vtk.vtkOrientationMarkerWidget()` | Assigns/updates `self.axes` with the expression on the right-hand side. Hint: VTK (3D visualization) usage. (scope: def load_axes) |
| 1862 | `        self.axes.SetOrientationMarker(axesActor1)` | Executes Python statement: `self.axes.SetOrientationMarker(axesActor1)`. (scope: def load_axes) |
| 1863 | `        self.axes.SetInteractor(self.iren)` | Executes Python statement: `self.axes.SetInteractor(self.iren)`. (scope: def load_axes) |
| 1864 | `        ` | Blank line used to separate logical sections and improve readability. (scope: def load_axes) |
| 1865 | `        ` | Blank line used to separate logical sections and improve readability. (scope: def load_axes) |
| 1866 | `        axesActor2 = vtk.vtkAxesActor()` | Assigns/updates `axesActor2` with the expression on the right-hand side. Hint: VTK (3D visualization) usage. (scope: def load_axes) |
| 1867 | `        axesActor2.AxisLabelsOff()` | Executes Python statement: `axesActor2.AxisLabelsOff()`. (scope: def load_axes) |
| 1868 | `        self.ren.AddActor(axesActor2)` | Executes Python statement: `self.ren.AddActor(axesActor2)`. (scope: def load_axes) |
| 1869 | `        # ########################` | Comment explaining intent/context: ######################## (scope: def load_axes) |
| 1870 | `        # not so good looking so far` | Comment explaining intent/context: not so good looking so far (scope: def load_axes) |
| 1871 | `        # axesActor3 = vtkCubeAxesActor();` | Comment explaining intent/context: axesActor3 = vtkCubeAxesActor(); (scope: def load_axes) |
| 1872 | `        # axesActor3.SetBounds(0, 1, 0, 1, 0, 1)` | Comment explaining intent/context: axesActor3.SetBounds(0, 1, 0, 1, 0, 1) (scope: def load_axes) |
| 1873 | `        # axesActor3.SetCamera(self.cam)` | Comment explaining intent/context: axesActor3.SetCamera(self.cam) (scope: def load_axes) |
| 1874 | `        # self.ren.AddActor(axesActor3)` | Comment explaining intent/context: self.ren.AddActor(axesActor3) (scope: def load_axes) |
| 1875 | `        ` | Blank line used to separate logical sections and improve readability. (scope: def load_axes) |
| 1876 | `        self.axes.EnabledOn() # <== application freeze-crash?` | Executes Python statement: `self.axes.EnabledOn() # <== application freeze-crash?`. (scope: def load_axes) |
| 1877 | `        self.axes.InteractiveOn()` | Executes Python statement: `self.axes.InteractiveOn()`. (scope: def load_axes) |
| 1878 | `        ` | Blank line used to separate logical sections and improve readability. (scope: def load_axes) |
| 1879 | `        # print(self.iren.__class__, self.iren.GetInteractorStyle(), dir(self.iren))` | Comment explaining intent/context: print(self.iren.__class__, self.iren.GetInteractorStyle(), dir(self.iren)) (scope: def load_axes) |
| 1880 | `    ` | Blank line used to separate logical sections and improve readability. |
| 1881 | `    def load_sphere_source(self):` | Defines function/method `load_sphere_source` (entry point for reusable logic). (scope: def load_sphere_source) |
| 1882 | `        # Create source` | Comment explaining intent/context: Create source (scope: def load_sphere_source) |
| 1883 | `        source = vtk.vtkSphereSource()` | Assigns/updates `source` with the expression on the right-hand side. Hint: VTK (3D visualization) usage. (scope: def load_sphere_source) |
| 1884 | `        source.SetCenter(0, 0, 0)` | Executes Python statement: `source.SetCenter(0, 0, 0)`. (scope: def load_sphere_source) |
| 1885 | `        source.SetRadius(5.0)` | Executes Python statement: `source.SetRadius(5.0)`. (scope: def load_sphere_source) |
| 1886 | `        ` | Blank line used to separate logical sections and improve readability. (scope: def load_sphere_source) |
| 1887 | `        # Create a mapper` | Comment explaining intent/context: Create a mapper (scope: def load_sphere_source) |
| 1888 | `        mapper = vtk.vtkPolyDataMapper()` | Assigns/updates `mapper` with the expression on the right-hand side. Hint: VTK (3D visualization) usage. (scope: def load_sphere_source) |
| 1889 | `        mapper.SetInputConnection(source.GetOutputPort())` | Executes Python statement: `mapper.SetInputConnection(source.GetOutputPort())`. (scope: def load_sphere_source) |
| 1890 | `        ` | Blank line used to separate logical sections and improve readability. (scope: def load_sphere_source) |
| 1891 | `        # # Create an actor` | Comment explaining intent/context: # Create an actor (scope: def load_sphere_source) |
| 1892 | `        actor = vtk.vtkActor()` | Assigns/updates `actor` with the expression on the right-hand side. Hint: VTK (3D visualization) usage. (scope: def load_sphere_source) |
| 1893 | `        actor.SetMapper(mapper)` | Executes Python statement: `actor.SetMapper(mapper)`. (scope: def load_sphere_source) |
| 1894 | `        actor.GetProperty().SetOpacity(0.2)` | Executes Python statement: `actor.GetProperty().SetOpacity(0.2)`. (scope: def load_sphere_source) |
| 1895 | `        #` | Comment line used as a visual separator. (scope: def load_sphere_source) |
| 1896 | `        self.ren.AddActor(actor)` | Executes Python statement: `self.ren.AddActor(actor)`. (scope: def load_sphere_source) |
| 1897 | `    ` | Blank line used to separate logical sections and improve readability. |
| 1898 | `    def load_world_grid(self):` | Defines function/method `load_world_grid` (entry point for reusable logic). (scope: def load_world_grid) |
| 1899 | `        reader = load_vtk_file('salome_cube_world_res_10_with_magneticfield.vtk')` | Assigns/updates `reader` with the expression on the right-hand side. Hint: VTK (3D visualization) usage. (scope: def load_world_grid) |
| 1900 | `        output = reader.GetOutput()` | Assigns/updates `output` with the expression on the right-hand side. (scope: def load_world_grid) |
| 1901 | `        self.world_extent = output.GetBounds()` | Assigns/updates `self.world_extent` with the expression on the right-hand side. (scope: def load_world_grid) |
| 1902 | `        # print('world extent', output.GetBounds())` | Comment explaining intent/context: print('world extent', output.GetBounds()) (scope: def load_world_grid) |
| 1903 | `        # scalar_range = output.GetScalarRange()` | Comment explaining intent/context: scalar_range = output.GetScalarRange() (scope: def load_world_grid) |
| 1904 | `        # print(scalar_range)` | Comment explaining intent/context: print(scalar_range) (scope: def load_world_grid) |
| 1905 | `    ` | Blank line used to separate logical sections and improve readability. |
| 1906 | `        # Create the mapper that corresponds the objects of the vtk.vtk file` | Comment explaining intent/context: Create the mapper that corresponds the objects of the vtk.vtk file |
| 1907 | `        # into graphics elements` | Comment explaining intent/context: into graphics elements |
| 1908 | `        mapper = vtk.vtkDataSetMapper()` | Assigns/updates `mapper` with the expression on the right-hand side. Hint: VTK (3D visualization) usage. |
| 1909 | `        mapper.SetInputData(output)` | Executes Python statement: `mapper.SetInputData(output)`. |
| 1910 | `        # mapper.SetScalarRange(scalar_range)` | Comment explaining intent/context: mapper.SetScalarRange(scalar_range) |
| 1911 | `        # mapper.ScalarVisibilityOff()` | Comment explaining intent/context: mapper.ScalarVisibilityOff() |
| 1912 | `    ` | Blank line used to separate logical sections and improve readability. |
| 1913 | `        # Create the Actor` | Comment explaining intent/context: Create the Actor |
| 1914 | `        actor = vtk.vtkActor()` | Assigns/updates `actor` with the expression on the right-hand side. Hint: VTK (3D visualization) usage. |
| 1915 | `        actor.SetMapper(mapper)` | Executes Python statement: `actor.SetMapper(mapper)`. |
| 1916 | `        # actor.GetProperty().EdgeVisibilityOn()` | Comment explaining intent/context: actor.GetProperty().EdgeVisibilityOn() |
| 1917 | `        actor.GetProperty().SetLineWidth(2.0)` | Executes Python statement: `actor.GetProperty().SetLineWidth(2.0)`. |
| 1918 | `        actor.GetProperty().SetColor(colors.GetColor3d("MistyRose"))` | Executes Python statement: `actor.GetProperty().SetColor(colors.GetColor3d("MistyRose"))`. |
| 1919 | `        actor.GetProperty().SetOpacity(0.05)` | Executes Python statement: `actor.GetProperty().SetOpacity(0.05)`. |
| 1920 | `        self.ren.AddActor(actor)` | Executes Python statement: `self.ren.AddActor(actor)`. |
| 1921 | `        ` | Blank line used to separate logical sections and improve readability. |
| 1922 | `    def load_map(self):` | Defines function/method `load_map` (entry point for reusable logic). (scope: def load_map) |
| 1923 | `        # India Map` | Comment explaining intent/context: India Map (scope: def load_map) |
| 1924 | `        map_figure = os.path.join(APP_BASE, *r'data\\india_India_Country_Boundary_MAPOG\\code_generated_Figure_1_India_cropped.png'.split('\\\\'))` | Assigns/updates `map_figure` with the expression on the right-hand side. (scope: def load_map) |
| 1925 | `        reader_map = vtk.vtkPNGReader()` | Assigns/updates `reader_map` with the expression on the right-hand side. Hint: VTK (3D visualization) usage. (scope: def load_map) |
| 1926 | `        reader_map.SetFileName(map_figure)` | Executes Python statement: `reader_map.SetFileName(map_figure)`. (scope: def load_map) |
| 1927 | `        reader_map.Update()` | Executes Python statement: `reader_map.Update()`. (scope: def load_map) |
| 1928 | `        ` | Blank line used to separate logical sections and improve readability. (scope: def load_map) |
| 1929 | `        self.map_extent = reader_map.GetDataExtent()` | Assigns/updates `self.map_extent` with the expression on the right-hand side. (scope: def load_map) |
| 1930 | `        x_min, x_max, y_min, y_max, z_min, z_max = self.map_extent` | Assigns/updates `x_min, x_max, y_min, y_max, z_min, z_max` with the expression on the right-hand side. (scope: def load_map) |
| 1931 | `        # print('map extent', x_min, x_max, y_min, y_max)` | Comment explaining intent/context: print('map extent', x_min, x_max, y_min, y_max) (scope: def load_map) |
| 1932 | `        ` | Blank line used to separate logical sections and improve readability. (scope: def load_map) |
| 1933 | `        actor_map = vtk.vtkImageActor()` | Assigns/updates `actor_map` with the expression on the right-hand side. Hint: VTK (3D visualization) usage. (scope: def load_map) |
| 1934 | `        actor_map.GetMapper().SetInputConnection(reader_map.GetOutputPort())` | Executes Python statement: `actor_map.GetMapper().SetInputConnection(reader_map.GetOutputPort())`. (scope: def load_map) |
| 1935 | `        actor_map.GetProperty().SetOpacity(0.2)` | Executes Python statement: `actor_map.GetProperty().SetOpacity(0.2)`. (scope: def load_map) |
| 1936 | `        if z_min == z_max == 0:` | Starts an `if` block: conditional control flow based on `z_min == z_max == 0`. (scope: def load_map) |
| 1937 | `            # reposition_actor(actor_map, self.map_extent, self.world_extent) # make a function to handle transformation` | Comment explaining intent/context: reposition_actor(actor_map, self.map_extent, self.world_extent) # make a function to handle transformation (scope: def load_map) |
| 1938 | `            transform = vtk.vtkTransform()` | Assigns/updates `transform` with the expression on the right-hand side. Hint: VTK (3D visualization) usage. (scope: def load_map) |
| 1939 | `            transform.PostMultiply() #; // this is the key line` | Executes Python statement: `transform.PostMultiply() #; // this is the key line`. (scope: def load_map) |
| 1940 | `            w_x_min, w_x_max, w_y_min, w_y_max, w_z_in, w_z_max = self.world_extent` | Assigns/updates `w_x_min, w_x_max, w_y_min, w_y_max, w_z_in, w_z_max` with the expression on the right-hand side. (scope: def load_map) |
| 1941 | `            transform.Scale((w_x_max-w_x_min)*1/(x_max-x_min), (w_y_max-w_y_min)*1/(y_max-y_min), 1)` | Executes Python statement: `transform.Scale((w_x_max-w_x_min)*1/(x_max-x_min), (w_y_max-w_y_min)*1/(y_max-y_min), 1)`. (scope: def load_map) |
| 1942 | `            transform.Translate(-(x_min-w_x_min), -(y_min-w_y_min), 0)` | Executes Python statement: `transform.Translate(-(x_min-w_x_min), -(y_min-w_y_min), 0)`. (scope: def load_map) |
| 1943 | `            # transform.RotateZ(90.0)` | Comment explaining intent/context: transform.RotateZ(90.0) (scope: def load_map) |
| 1944 | `            actor_map.SetUserTransform(transform)` | Executes Python statement: `actor_map.SetUserTransform(transform)`. (scope: def load_map) |
| 1945 | `            # if transformed correctly, set map extent to its new value:` | Comment explaining intent/context: if transformed correctly, set map extent to its new value: (scope: def load_map) |
| 1946 | `            self.map_extent =  w_x_min, w_x_max, w_y_min, w_y_max, 0, 0` | Assigns/updates `self.map_extent` with the expression on the right-hand side. (scope: def load_map) |
| 1947 | `        ` | Blank line used to separate logical sections and improve readability. (scope: def load_map) |
| 1948 | `        map_meta = {` | Assigns/updates `map_meta` with the expression on the right-hand side. (scope: def load_map) |
| 1949 | `                'start_lat': 5.0,` | Executes Python statement: `'start_lat': 5.0,`. (scope: def load_map) |
| 1950 | `                'end_lat': 40.0,` | Executes Python statement: `'end_lat': 40.0,`. (scope: def load_map) |
| 1951 | `                'start_lon': 65.0,` | Executes Python statement: `'start_lon': 65.0,`. (scope: def load_map) |
| 1952 | `                'end_lon': 100.0 ` | Executes Python statement: `'end_lon': 100.0`. (scope: def load_map) |
| 1953 | `            }` | Executes Python statement: `}`. (scope: def load_map) |
| 1954 | `        self.map_meta = map_meta` | Assigns/updates `self.map_meta` with the expression on the right-hand side. (scope: def load_map) |
| 1955 | `        self.ren.AddActor(actor_map)` | Executes Python statement: `self.ren.AddActor(actor_map)`. (scope: def load_map) |
| 1956 | `    ` | Blank line used to separate logical sections and improve readability. |
| 1957 | `    def get_map_xy_from_latlon(self, lat, lon):` | Defines function/method `get_map_xy_from_latlon` (entry point for reusable logic). (scope: def get_map_xy_from_latlon) |
| 1958 | `        map_meta = self.map_meta` | Assigns/updates `map_meta` with the expression on the right-hand side. (scope: def get_map_xy_from_latlon) |
| 1959 | `        map_extent = self.map_extent` | Assigns/updates `map_extent` with the expression on the right-hand side. (scope: def get_map_xy_from_latlon) |
| 1960 | `        lat_min = map_meta['start_lat']` | Assigns/updates `lat_min` with the expression on the right-hand side. (scope: def get_map_xy_from_latlon) |
| 1961 | `        lat_max = map_meta['end_lat']` | Assigns/updates `lat_max` with the expression on the right-hand side. (scope: def get_map_xy_from_latlon) |
| 1962 | `        lon_min = map_meta['start_lon']` | Assigns/updates `lon_min` with the expression on the right-hand side. (scope: def get_map_xy_from_latlon) |
| 1963 | `        lon_max = map_meta['end_lon']` | Assigns/updates `lon_max` with the expression on the right-hand side. (scope: def get_map_xy_from_latlon) |
| 1964 | `        x_min, x_max, y_min, y_max, z_, z__ = map_extent` | Assigns/updates `x_min, x_max, y_min, y_max, z_, z__` with the expression on the right-hand side. (scope: def get_map_xy_from_latlon) |
| 1965 | `        x = x_min + (x_max - x_min)*(lon - lon_min)/(lon_max - lon_min)` | Assigns/updates `x` with the expression on the right-hand side. (scope: def get_map_xy_from_latlon) |
| 1966 | `        y = y_min + (y_max - y_min)*(lat - lat_min)/(lat_max - lat_min)` | Assigns/updates `y` with the expression on the right-hand side. (scope: def get_map_xy_from_latlon) |
| 1967 | `        return (x,y)` | Returns value(s) from the current function: `(x,y)`. (scope: def get_map_xy_from_latlon) |
| 1968 | `    ` | Blank line used to separate logical sections and improve readability. |
| 1969 | `    def get_map_lat_lon_from_xy(self, x, y):` | Defines function/method `get_map_lat_lon_from_xy` (entry point for reusable logic). (scope: def get_map_lat_lon_from_xy) |
| 1970 | `        map_meta = self.map_meta` | Assigns/updates `map_meta` with the expression on the right-hand side. (scope: def get_map_lat_lon_from_xy) |
| 1971 | `        map_extent = self.map_extent` | Assigns/updates `map_extent` with the expression on the right-hand side. (scope: def get_map_lat_lon_from_xy) |
| 1972 | `        lat_min = map_meta['start_lat']` | Assigns/updates `lat_min` with the expression on the right-hand side. (scope: def get_map_lat_lon_from_xy) |
| 1973 | `        lat_max = map_meta['end_lat']` | Assigns/updates `lat_max` with the expression on the right-hand side. (scope: def get_map_lat_lon_from_xy) |
| 1974 | `        lon_min = map_meta['start_lon']` | Assigns/updates `lon_min` with the expression on the right-hand side. (scope: def get_map_lat_lon_from_xy) |
| 1975 | `        lon_max = map_meta['end_lon']` | Assigns/updates `lon_max` with the expression on the right-hand side. (scope: def get_map_lat_lon_from_xy) |
| 1976 | `        x_min, x_max, y_min, y_max, z_, z__ = map_extent` | Assigns/updates `x_min, x_max, y_min, y_max, z_, z__` with the expression on the right-hand side. (scope: def get_map_lat_lon_from_xy) |
| 1977 | `        lon = lon_min + (lon_max - lon_min)*(x - x_min)/(x_max - x_min)` | Assigns/updates `lon` with the expression on the right-hand side. (scope: def get_map_lat_lon_from_xy) |
| 1978 | `        lat = lat_min + (lat_max - y_min)*(y - y_min)/(y_max - y_min)` | Assigns/updates `lat` with the expression on the right-hand side. (scope: def get_map_lat_lon_from_xy) |
| 1979 | `        return (lat, lon)` | Returns value(s) from the current function: `(lat, lon)`. (scope: def get_map_lat_lon_from_xy) |
| 1980 | `` | Blank line used to separate logical sections and improve readability. |
| 1981 | `if __name__ == "__main__":` | Starts an `if` block: conditional control flow based on `__name__ == "__main__"`. |
| 1982 | `    app = Application([])` | Assigns/updates `app` with the expression on the right-hand side. |
| 1983 | `    sys.exit(app.exec())` | Executes Python statement: `sys.exit(app.exec())`. |
