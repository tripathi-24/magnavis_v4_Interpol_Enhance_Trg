# Line-by-line documentation: `src/application.py`

- **File**: `src/application.py`
- **Lines covered in this document**: 201–400 (of 1983)

## Structure overview (file-level)

- Line 72: function `load_vtk_file()`
- Line 81: function `getFilePath()`
- Line 94: class `SessionDataManager`
- Line 98: function `update_api_df()`
- Line 124: class `Color`
- Line 126: function `__init__()`
- Line 134: class `DraggableLabel`
- Line 135: method `mousePressEvent()`
- Line 139: function `mouseMoveEvent()`
- Line 156: class `DropLabel`
- Line 157: method `__init__()`
- Line 161: function `dragEnterEvent()`
- Line 165: function `dropEvent()`
- Line 172: class `MapWidget`
- Line 173: method `__init__()`
- Line 177: function `initUI()`
- Line 206: class `PandasModel`
- Line 209: function `__init__()`
- Line 213: function `rowCount()`
- Line 223: function `columnCount()`
- Line 232: function `data()`
- Line 245: function `headerData()`
- Line 262: class `MagTimeSeriesWidget`
- Line 263: method `__init__()`
- Line 269: method `timerRefreshEvent()`
- Line 283: method `initWidget()`
- Line 350: function `on_threshold_changed()`
- Line 377: function `on_train_window_changed()`
- Line 390: function `update_train_window_status()`
- Line 403: class `ApplicationWindow`
- Line 406: method `__init__()`
- Line 432: method `connectSlots()`
- Line 436: method `createThread()`
- Line 447: method `updateData()`
- Line 455: method `startThreads()`
- Line 462: function `addTimeSeries()`
- Line 469: function `showCsvData()`
- Line 493: function `setTreeView()`
- Line 500: function `updateTreeView()`
- Line 507: function `createMailModel()`
- Line 514: function `addSource()`
- Line 521: function `update_visualisation()`
- Line 604: function `clear_point_actors()`
- Line 612: function `set_points()`
- Line 673: function `sphere_prop_to_vtkarray()`
- Line 691: function `build_scene()`
- Line 728: function `setup_point_picker_framework()`
- Line 746: function `on_pick_left()`
- Line 771: function `on_pick_right()`
- Line 777: function `loadFile()`
- Line 803: function `log()`
- Line 824: class `Source`
- Line 825: method `__init__()`
- Line 830: class `TimeSeriesSource`
- Line 831: method `__init__()`
- Line 837: class `World`
- Line 838: method `__init__()`
- Line 845: method `loadCsv()`
- Line 860: class `DataSourceManager`
- Line 861: method `__init__()`
- Line 877: method `loadCsv()`
- Line 883: method `updateUI()`
- Line 887: method `createTimeSeriesSource()`
- Line 892: class `Application`
- Line 893: method `__init__()`
- Line 981: function `showAppMaximized()`
- Line 985: function `log()`
- Line 988: function `_save_data()`
- Line 1039: function `load_plot_framework()`
- Line 1119: function `load_plot_framework_2()`
- Line 1178: function `on_add_static()`
- Line 1196: function `on_add_dynamic()`
- Line 1222: function `start_prediction_process()`
- Line 1261: function `_update_xydata()`
- Line 1290: function `_update_predictions_data()`
- Line 1387: function `_sync_threshold_spinbox()`
- Line 1401: function `_detect_anomalies()`
- Line 1575: function `_update_canvas()`
- Line 1589: function `on_add_new()`
- Line 1626: function `on_add_pred()`
- Line 1745: function `appWin()`
- Line 1748: function `initViews()`
- Line 1754: function `load_visualization_framework()`
- Line 1858: function `load_axes()`
- Line 1881: function `load_sphere_source()`
- Line 1898: function `load_world_grid()`
- Line 1922: function `load_map()`
- Line 1957: function `get_map_xy_from_latlon()`
- Line 1969: function `get_map_lat_lon_from_xy()`

## Line-by-line explanation

| Line | Code | Explanation |
|---:|---|---|
| 201 | `        layout3.addWidget(Color('green'))` | Executes Python statement: `layout3.addWidget(Color('green'))`. |
| 202 | `        layout3.addWidget(label)` | Executes Python statement: `layout3.addWidget(label)`. |
| 203 | `        self.setLayout(layout1)` | Executes Python statement: `self.setLayout(layout1)`. |
| 204 | `        self.show()` | Executes Python statement: `self.show()`. |
| 205 | `    ` | Blank line used to separate logical sections and improve readability. |
| 206 | `class PandasModel(QAbstractTableModel):` | Defines class `PandasModel` (starts a new type/namespace for related behavior). (scope: class PandasModel) |
| 207 | `    """A model to interface a Qt view with pandas dataframe """` | Single-line triple-quoted string (docstring or multiline literal expressed on one line). (scope: class PandasModel) |
| 208 | `` | Blank line used to separate logical sections and improve readability. |
| 209 | `    def __init__(self, dataframe: pd.DataFrame, parent=None):` | Defines function/method `__init__` (entry point for reusable logic). (scope: def __init__) |
| 210 | `        QAbstractTableModel.__init__(self, parent)` | Executes Python statement: `QAbstractTableModel.__init__(self, parent)`. (scope: def __init__) |
| 211 | `        self._dataframe = dataframe` | Assigns/updates `self._dataframe` with the expression on the right-hand side. (scope: def __init__) |
| 212 | `` | Blank line used to separate logical sections and improve readability. |
| 213 | `    def rowCount(self, parent=QModelIndex()) -> int:` | Defines function/method `rowCount` (entry point for reusable logic). (scope: def rowCount) |
| 214 | `        """ Override method from QAbstractTableModel` | Executes Python statement: `""" Override method from QAbstractTableModel`. (scope: def rowCount) |
| 215 | `` | Blank line used to separate logical sections and improve readability. |
| 216 | `        Return row count of the pandas DataFrame` | Executes Python statement: `Return row count of the pandas DataFrame`. |
| 217 | `        """` | Ends a triple-quoted string (closing a docstring/multiline literal). |
| 218 | `        if parent == QModelIndex():` | Starts an `if` block: conditional control flow based on `parent == QModelIndex()`. |
| 219 | `            return len(self._dataframe)` | Returns value(s) from the current function: `len(self._dataframe)`. |
| 220 | `` | Blank line used to separate logical sections and improve readability. |
| 221 | `        return 0` | Returns value(s) from the current function: `0`. |
| 222 | `` | Blank line used to separate logical sections and improve readability. |
| 223 | `    def columnCount(self, parent=QModelIndex()) -> int:` | Defines function/method `columnCount` (entry point for reusable logic). (scope: def columnCount) |
| 224 | `        """Override method from QAbstractTableModel` | Executes Python statement: `"""Override method from QAbstractTableModel`. (scope: def columnCount) |
| 225 | `` | Blank line used to separate logical sections and improve readability. |
| 226 | `        Return column count of the pandas DataFrame` | Executes Python statement: `Return column count of the pandas DataFrame`. |
| 227 | `        """` | Ends a triple-quoted string (closing a docstring/multiline literal). |
| 228 | `        if parent == QModelIndex():` | Starts an `if` block: conditional control flow based on `parent == QModelIndex()`. |
| 229 | `            return len(self._dataframe.columns)` | Returns value(s) from the current function: `len(self._dataframe.columns)`. |
| 230 | `        return 0` | Returns value(s) from the current function: `0`. |
| 231 | `` | Blank line used to separate logical sections and improve readability. |
| 232 | `    def data(self, index: QModelIndex, role=QtCore.Qt.ItemDataRole):` | Defines function/method `data` (entry point for reusable logic). (scope: def data) |
| 233 | `        """Override method from QAbstractTableModel` | Executes Python statement: `"""Override method from QAbstractTableModel`. (scope: def data) |
| 234 | `` | Blank line used to separate logical sections and improve readability. |
| 235 | `        Return data cell from the pandas DataFrame` | Executes Python statement: `Return data cell from the pandas DataFrame`. |
| 236 | `        """` | Ends a triple-quoted string (closing a docstring/multiline literal). |
| 237 | `        if not index.isValid():` | Starts an `if` block: conditional control flow based on `not index.isValid()`. |
| 238 | `            return None` | Returns value(s) from the current function: `None`. |
| 239 | `` | Blank line used to separate logical sections and improve readability. |
| 240 | `        if role == QtCore.Qt.DisplayRole:` | Starts an `if` block: conditional control flow based on `role == QtCore.Qt.DisplayRole`. |
| 241 | `            return str(self._dataframe.iloc[index.row(), index.column()])` | Returns value(s) from the current function: `str(self._dataframe.iloc[index.row(), index.column()])`. |
| 242 | `` | Blank line used to separate logical sections and improve readability. |
| 243 | `        return None` | Returns value(s) from the current function: `None`. |
| 244 | `` | Blank line used to separate logical sections and improve readability. |
| 245 | `    def headerData(` | Defines function/method `headerData` (entry point for reusable logic). (scope: def headerData) |
| 246 | `        self, section: int, orientation: QtCore.Qt.Vertical, role: QtCore.Qt.ItemDataRole` | Executes Python statement: `self, section: int, orientation: QtCore.Qt.Vertical, role: QtCore.Qt.ItemDataRole`. (scope: def headerData) |
| 247 | `    ):` | Executes Python statement: `):`. |
| 248 | `        """Override method from QAbstractTableModel` | Executes Python statement: `"""Override method from QAbstractTableModel`. |
| 249 | `` | Blank line used to separate logical sections and improve readability. |
| 250 | `        Return dataframe index as vertical header data and columns as horizontal header data.` | Executes Python statement: `Return dataframe index as vertical header data and columns as horizontal header data.`. |
| 251 | `        """` | Ends a triple-quoted string (closing a docstring/multiline literal). |
| 252 | `        if role == QtCore.Qt.DisplayRole:` | Starts an `if` block: conditional control flow based on `role == QtCore.Qt.DisplayRole`. |
| 253 | `            if orientation == QtCore.Qt.Horizontal:` | Starts an `if` block: conditional control flow based on `orientation == QtCore.Qt.Horizontal`. |
| 254 | `                return str(self._dataframe.columns[section])` | Returns value(s) from the current function: `str(self._dataframe.columns[section])`. |
| 255 | `` | Blank line used to separate logical sections and improve readability. |
| 256 | `            if orientation == QtCore.Qt.Vertical:` | Starts an `if` block: conditional control flow based on `orientation == QtCore.Qt.Vertical`. |
| 257 | `                return str(self._dataframe.index[section])` | Returns value(s) from the current function: `str(self._dataframe.index[section])`. |
| 258 | `` | Blank line used to separate logical sections and improve readability. |
| 259 | `        return None` | Returns value(s) from the current function: `None`. |
| 260 | `` | Blank line used to separate logical sections and improve readability. |
| 261 | `timeSerBase, timeSerForm = uic.loadUiType(os.path.join(APP_BASE, 'ui_files', 'ui_MagneticTimeSeriesWidget.ui'))` | Assigns/updates `timeSerBase, timeSerForm` with the expression on the right-hand side. |
| 262 | `class MagTimeSeriesWidget(timeSerBase, timeSerForm):` | Defines class `MagTimeSeriesWidget` (starts a new type/namespace for related behavior). (scope: class MagTimeSeriesWidget) |
| 263 | `    def __init__(self, app, parent=None):` | Defines function/method `__init__` (entry point for reusable logic). (scope: class MagTimeSeriesWidget → def __init__) |
| 264 | `        super(timeSerBase, self).__init__(parent)` | Executes Python statement: `super(timeSerBase, self).__init__(parent)`. (scope: class MagTimeSeriesWidget → def __init__) |
| 265 | `        self.app = app` | Assigns/updates `self.app` with the expression on the right-hand side. (scope: class MagTimeSeriesWidget → def __init__) |
| 266 | `        self.setupUi(self)` | Executes Python statement: `self.setupUi(self)`. (scope: class MagTimeSeriesWidget → def __init__) |
| 267 | `        self.initWidget()` | Executes Python statement: `self.initWidget()`. (scope: class MagTimeSeriesWidget → def __init__) |
| 268 | `    ` | Blank line used to separate logical sections and improve readability. (scope: class MagTimeSeriesWidget) |
| 269 | `    def timerRefreshEvent(self):` | Defines function/method `timerRefreshEvent` (entry point for reusable logic). (scope: class MagTimeSeriesWidget → def timerRefreshEvent) |
| 270 | `        mag_t_df = get_timeseries_magnetic_data()` | Assigns/updates `mag_t_df` with the expression on the right-hand side. (scope: class MagTimeSeriesWidget → def timerRefreshEvent) |
| 271 | `        # print(mag_t_df)` | Comment explaining intent/context: print(mag_t_df) (scope: class MagTimeSeriesWidget → def timerRefreshEvent) |
| 272 | `        t = mag_t_df['time_H'].tolist()` | Assigns/updates `t` with the expression on the right-hand side. (scope: class MagTimeSeriesWidget → def timerRefreshEvent) |
| 273 | `        y_t = mag_t_df['mag_H_nT'].tolist()` | Assigns/updates `y_t` with the expression on the right-hand side. (scope: class MagTimeSeriesWidget → def timerRefreshEvent) |
| 274 | `        xfmt = md.DateFormatter('%Y-%m-%d %H:%M:%S')` | Assigns/updates `xfmt` with the expression on the right-hand side. (scope: class MagTimeSeriesWidget → def timerRefreshEvent) |
| 275 | `        self.app._static_ax.clear()` | Executes Python statement: `self.app._static_ax.clear()`. (scope: class MagTimeSeriesWidget → def timerRefreshEvent) |
| 276 | `        print('plot cleared...')` | Executes Python statement: `print('plot cleared...')`. (scope: class MagTimeSeriesWidget → def timerRefreshEvent) |
| 277 | `        self.app._static_ax.xaxis.set_major_formatter(xfmt)` | Executes Python statement: `self.app._static_ax.xaxis.set_major_formatter(xfmt)`. (scope: class MagTimeSeriesWidget → def timerRefreshEvent) |
| 278 | `        # self.app.self.magetic_time_ser_canvas.figure.subplots_adjust(bottom=0.3)` | Comment explaining intent/context: self.app.self.magetic_time_ser_canvas.figure.subplots_adjust(bottom=0.3) (scope: class MagTimeSeriesWidget → def timerRefreshEvent) |
| 279 | `        # self.app._static_ax.tick_params(rotation=15) # xticks(rotation=25)` | Comment explaining intent/context: self.app._static_ax.tick_params(rotation=15) # xticks(rotation=25) (scope: class MagTimeSeriesWidget → def timerRefreshEvent) |
| 280 | `        self.app._static_ax.plot(t, y_t, ".")` | Executes Python statement: `self.app._static_ax.plot(t, y_t, ".")`. (scope: class MagTimeSeriesWidget → def timerRefreshEvent) |
| 281 | `        print('plot updated...')` | Executes Python statement: `print('plot updated...')`. (scope: class MagTimeSeriesWidget → def timerRefreshEvent) |
| 282 | `        ` | Blank line used to separate logical sections and improve readability. (scope: class MagTimeSeriesWidget → def timerRefreshEvent) |
| 283 | `    def initWidget(self):` | Defines function/method `initWidget` (entry point for reusable logic). (scope: class MagTimeSeriesWidget → def initWidget) |
| 284 | `        currentTime = QDateTime.currentDateTime()` | Assigns/updates `currentTime` with the expression on the right-hand side. (scope: class MagTimeSeriesWidget → def initWidget) |
| 285 | `        self.dateTimeEdit_2.setDateTime(currentTime)` | Executes Python statement: `self.dateTimeEdit_2.setDateTime(currentTime)`. (scope: class MagTimeSeriesWidget → def initWidget) |
| 286 | `        default_start_time = currentTime.addDays(-1)` | Assigns/updates `default_start_time` with the expression on the right-hand side. (scope: class MagTimeSeriesWidget → def initWidget) |
| 287 | `        self.dateTimeEdit.setDateTime(default_start_time)` | Executes Python statement: `self.dateTimeEdit.setDateTime(default_start_time)`. (scope: class MagTimeSeriesWidget → def initWidget) |
| 288 | `        refresh_rate_option = self.comboBox_3.currentText()` | Assigns/updates `refresh_rate_option` with the expression on the right-hand side. (scope: class MagTimeSeriesWidget → def initWidget) |
| 289 | `        refresh_rate = None` | Assigns/updates `refresh_rate` with the expression on the right-hand side. (scope: class MagTimeSeriesWidget → def initWidget) |
| 290 | `        if '20 sec' in refresh_rate_option:` | Starts an `if` block: conditional control flow based on `'20 sec' in refresh_rate_option`. (scope: class MagTimeSeriesWidget → def initWidget) |
| 291 | `            refresh_rate = 10` | Assigns/updates `refresh_rate` with the expression on the right-hand side. (scope: class MagTimeSeriesWidget → def initWidget) |
| 292 | `        elif '1 min' in refresh_rate_option:` | Starts an `elif` branch: additional conditional `'1 min' in refresh_rate_option`. (scope: class MagTimeSeriesWidget → def initWidget) |
| 293 | `            refresh_rate = 60` | Assigns/updates `refresh_rate` with the expression on the right-hand side. (scope: class MagTimeSeriesWidget → def initWidget) |
| 294 | `        elif '5 min' in refresh_rate_option:` | Starts an `elif` branch: additional conditional `'5 min' in refresh_rate_option`. (scope: class MagTimeSeriesWidget → def initWidget) |
| 295 | `            refresh_rate = 300` | Assigns/updates `refresh_rate` with the expression on the right-hand side. (scope: class MagTimeSeriesWidget → def initWidget) |
| 296 | `        ` | Blank line used to separate logical sections and improve readability. (scope: class MagTimeSeriesWidget → def initWidget) |
| 297 | `        # Add Anomaly Detection Threshold Control` | Comment explaining intent/context: Add Anomaly Detection Threshold Control (scope: class MagTimeSeriesWidget → def initWidget) |
| 298 | `        anomaly_layout = QHBoxLayout()` | Assigns/updates `anomaly_layout` with the expression on the right-hand side. (scope: class MagTimeSeriesWidget → def initWidget) |
| 299 | `        anomaly_label = QLabel("Anomaly Threshold Multiplier:")` | Assigns/updates `anomaly_label` with the expression on the right-hand side. Hint: PyQt5/Qt GUI framework usage. (scope: class MagTimeSeriesWidget → def initWidget) |
| 300 | `        anomaly_layout.addWidget(anomaly_label)` | Executes Python statement: `anomaly_layout.addWidget(anomaly_label)`. (scope: class MagTimeSeriesWidget → def initWidget) |
| 301 | `        ` | Blank line used to separate logical sections and improve readability. (scope: class MagTimeSeriesWidget → def initWidget) |
| 302 | `        self.threshold_spinbox = QDoubleSpinBox()` | Assigns/updates `self.threshold_spinbox` with the expression on the right-hand side. (scope: class MagTimeSeriesWidget → def initWidget) |
| 303 | `        self.threshold_spinbox.setMinimum(0.1)` | Executes Python statement: `self.threshold_spinbox.setMinimum(0.1)`. (scope: class MagTimeSeriesWidget → def initWidget) |
| 304 | `        self.threshold_spinbox.setMaximum(10.0)` | Executes Python statement: `self.threshold_spinbox.setMaximum(10.0)`. (scope: class MagTimeSeriesWidget → def initWidget) |
| 305 | `        self.threshold_spinbox.setSingleStep(0.1)` | Executes Python statement: `self.threshold_spinbox.setSingleStep(0.1)`. (scope: class MagTimeSeriesWidget → def initWidget) |
| 306 | `        self.threshold_spinbox.setDecimals(2)` | Executes Python statement: `self.threshold_spinbox.setDecimals(2)`. (scope: class MagTimeSeriesWidget → def initWidget) |
| 307 | `        self.threshold_spinbox.setValue(2.5)  # Default value` | Executes Python statement: `self.threshold_spinbox.setValue(2.5)  # Default value`. (scope: class MagTimeSeriesWidget → def initWidget) |
| 308 | `        self.threshold_spinbox.setToolTip("Higher values = fewer anomalies detected (more strict)\\nLower values = more anomalies detected (more sensitive)")` | Executes Python statement: `self.threshold_spinbox.setToolTip("Higher values = fewer anomalies detected (more strict)\nLower values = more anomalies detected (more sensitive)")`. (scope: class MagTimeSeriesWidget → def initWidget) |
| 309 | `        anomaly_layout.addWidget(self.threshold_spinbox)` | Executes Python statement: `anomaly_layout.addWidget(self.threshold_spinbox)`. (scope: class MagTimeSeriesWidget → def initWidget) |
| 310 | `        ` | Blank line used to separate logical sections and improve readability. (scope: class MagTimeSeriesWidget → def initWidget) |
| 311 | `        # Connect threshold change to update anomaly detector` | Comment explaining intent/context: Connect threshold change to update anomaly detector (scope: class MagTimeSeriesWidget → def initWidget) |
| 312 | `        self.threshold_spinbox.valueChanged.connect(self.on_threshold_changed)` | Connects a Qt signal to a slot/callback so UI events trigger Python code. (scope: class MagTimeSeriesWidget → def initWidget) |
| 313 | `        ` | Blank line used to separate logical sections and improve readability. (scope: class MagTimeSeriesWidget → def initWidget) |
| 314 | `        # Add to the grid layout (add after row 3, column 5)` | Comment explaining intent/context: Add to the grid layout (add after row 3, column 5) (scope: class MagTimeSeriesWidget → def initWidget) |
| 315 | `        self.gridLayout.addLayout(anomaly_layout, 4, 0, 1, 5)` | Executes Python statement: `self.gridLayout.addLayout(anomaly_layout, 4, 0, 1, 5)`. (scope: class MagTimeSeriesWidget → def initWidget) |
| 316 | `` | Blank line used to separate logical sections and improve readability. |
| 317 | `        # Add Training Window Control (minutes; 0 = full history)` | Comment explaining intent/context: Add Training Window Control (minutes; 0 = full history) |
| 318 | `        train_window_layout = QHBoxLayout()` | Assigns/updates `train_window_layout` with the expression on the right-hand side. |
| 319 | `        train_window_label = QLabel("LSTM Training Window (minutes, 0 = all):")` | Assigns/updates `train_window_label` with the expression on the right-hand side. Hint: PyQt5/Qt GUI framework usage. |
| 320 | `        train_window_layout.addWidget(train_window_label)` | Executes Python statement: `train_window_layout.addWidget(train_window_label)`. |
| 321 | `` | Blank line used to separate logical sections and improve readability. |
| 322 | `        self.train_window_spinbox = QDoubleSpinBox()` | Assigns/updates `self.train_window_spinbox` with the expression on the right-hand side. |
| 323 | `        self.train_window_spinbox.setMinimum(0)` | Executes Python statement: `self.train_window_spinbox.setMinimum(0)`. |
| 324 | `        self.train_window_spinbox.setMaximum(100000)  # effectively unbounded` | Executes Python statement: `self.train_window_spinbox.setMaximum(100000)  # effectively unbounded`. |
| 325 | `        self.train_window_spinbox.setSingleStep(5)` | Executes Python statement: `self.train_window_spinbox.setSingleStep(5)`. |
| 326 | `        self.train_window_spinbox.setDecimals(0)` | Executes Python statement: `self.train_window_spinbox.setDecimals(0)`. |
| 327 | `        self.train_window_spinbox.setValue(0)  # default = full history` | Assigns/updates `self.train_window_spinbox.setValue(0)  # default` with the expression on the right-hand side. |
| 328 | `        self.train_window_spinbox.setToolTip("Use 0 for full history; set minutes to limit training window.")` | Executes Python statement: `self.train_window_spinbox.setToolTip("Use 0 for full history; set minutes to limit training window.")`. |
| 329 | `        train_window_layout.addWidget(self.train_window_spinbox)` | Executes Python statement: `train_window_layout.addWidget(self.train_window_spinbox)`. |
| 330 | `` | Blank line used to separate logical sections and improve readability. |
| 331 | `        self.train_window_spinbox.valueChanged.connect(self.on_train_window_changed)` | Connects a Qt signal to a slot/callback so UI events trigger Python code. |
| 332 | `` | Blank line used to separate logical sections and improve readability. |
| 333 | `        # Place below anomaly control` | Comment explaining intent/context: Place below anomaly control |
| 334 | `        self.gridLayout.addLayout(train_window_layout, 5, 0, 1, 5)` | Executes Python statement: `self.gridLayout.addLayout(train_window_layout, 5, 0, 1, 5)`. |
| 335 | `` | Blank line used to separate logical sections and improve readability. |
| 336 | `        # Status badge for training window + time-aware flag` | Comment explaining intent/context: Status badge for training window + time-aware flag |
| 337 | `        status_layout = QHBoxLayout()` | Assigns/updates `status_layout` with the expression on the right-hand side. |
| 338 | `        self.train_window_status_label = QLabel()` | Assigns/updates `self.train_window_status_label` with the expression on the right-hand side. Hint: PyQt5/Qt GUI framework usage. |
| 339 | `        self.train_window_status_label.setStyleSheet("color: #444;")` | Executes Python statement: `self.train_window_status_label.setStyleSheet("color: #444;")`. |
| 340 | `        status_layout.addWidget(self.train_window_status_label)` | Executes Python statement: `status_layout.addWidget(self.train_window_status_label)`. |
| 341 | `        self.gridLayout.addLayout(status_layout, 6, 0, 1, 5)` | Executes Python statement: `self.gridLayout.addLayout(status_layout, 6, 0, 1, 5)`. |
| 342 | `        self.update_train_window_status()` | Executes Python statement: `self.update_train_window_status()`. |
| 343 | `        ` | Blank line used to separate logical sections and improve readability. |
| 344 | `        # if refresh_rate:` | Comment explaining intent/context: if refresh_rate: |
| 345 | `        #     self.timerRefresh = QtCore.QTimer()` | Comment explaining intent/context: self.timerRefresh = QtCore.QTimer() |
| 346 | `        #     self.timerRefresh.timeout.connect(self.timerRefreshEvent)` | Comment explaining intent/context: self.timerRefresh.timeout.connect(self.timerRefreshEvent) |
| 347 | `        #     self.timerRefresh.start(1000*refresh_rate)` | Comment explaining intent/context: self.timerRefresh.start(1000*refresh_rate) |
| 348 | `        #     print('refresh timer on')` | Comment explaining intent/context: print('refresh timer on') |
| 349 | `    ` | Blank line used to separate logical sections and improve readability. |
| 350 | `    def on_threshold_changed(self, value):` | Defines function/method `on_threshold_changed` (entry point for reusable logic). (scope: def on_threshold_changed) |
| 351 | `        """Called when user changes the threshold multiplier"""` | Single-line triple-quoted string (docstring or multiline literal expressed on one line). (scope: def on_threshold_changed) |
| 352 | `        if hasattr(self.app, 'anomaly_detector'):` | Starts an `if` block: conditional control flow based on `hasattr(self.app, 'anomaly_detector')`. (scope: def on_threshold_changed) |
| 353 | `            old_threshold = self.app.anomaly_detector.threshold_multiplier` | Assigns/updates `old_threshold` with the expression on the right-hand side. (scope: def on_threshold_changed) |
| 354 | `            self.app.anomaly_detector.threshold_multiplier = value` | Assigns/updates `self.app.anomaly_detector.threshold_multiplier` with the expression on the right-hand side. (scope: def on_threshold_changed) |
| 355 | `            self.app.log(f'Anomaly Detection: Threshold multiplier changed from {old_threshold:.2f} to {value:.2f}', level='Info')` | Executes Python statement: `self.app.log(f'Anomaly Detection: Threshold multiplier changed from {old_threshold:.2f} to {value:.2f}', level='Info')`. (scope: def on_threshold_changed) |
| 356 | `            ` | Blank line used to separate logical sections and improve readability. (scope: def on_threshold_changed) |
| 357 | `            # Recalculate threshold with new multiplier` | Comment explaining intent/context: Recalculate threshold with new multiplier (scope: def on_threshold_changed) |
| 358 | `            if len(self.app.anomaly_detector.prediction_errors) >= self.app.anomaly_detector.min_samples_for_threshold:` | Starts an `if` block: conditional control flow based on `len(self.app.anomaly_detector.prediction_errors) >= self.app.anomaly_detector.min_samples_for_threshold`. (scope: def on_threshold_changed) |
| 359 | `                mean_error = np.mean(self.app.anomaly_detector.prediction_errors)` | Assigns/updates `mean_error` with the expression on the right-hand side. (scope: def on_threshold_changed) |
| 360 | `                std_error = np.std(self.app.anomaly_detector.prediction_errors)` | Assigns/updates `std_error` with the expression on the right-hand side. (scope: def on_threshold_changed) |
| 361 | `                new_threshold_value = mean_error + value * std_error` | Assigns/updates `new_threshold_value` with the expression on the right-hand side. (scope: def on_threshold_changed) |
| 362 | `                self.app.anomaly_detector.anomaly_threshold = new_threshold_value` | Assigns/updates `self.app.anomaly_detector.anomaly_threshold` with the expression on the right-hand side. (scope: def on_threshold_changed) |
| 363 | `                self.app.log(f'Anomaly Detection: New threshold value calculated: {new_threshold_value:.2f} nT (mean: {mean_error:.2f}, std: {std_error:.2f}, samples: {len(self.app.anomaly_detector.prediction_errors)})', level='Info')` | Executes Python statement: `self.app.log(f'Anomaly Detection: New threshold value calculated: {new_threshold_value:.2f} nT (mean: {mean_error:.2f}, std: {std_error:.2f}, samples: {len(self.app.anomaly_detector.prediction_errors)})', level='Info')`. (scope: def on_threshold_changed) |
| 364 | `            else:` | Starts an `else` branch for the preceding conditional. (scope: def on_threshold_changed) |
| 365 | `                # Not enough samples yet - use default or show warning` | Comment explaining intent/context: Not enough samples yet - use default or show warning (scope: def on_threshold_changed) |
| 366 | `                current_threshold = self.app.anomaly_detector.anomaly_threshold` | Assigns/updates `current_threshold` with the expression on the right-hand side. (scope: def on_threshold_changed) |
| 367 | `                threshold_str = f'{current_threshold:.2f}' if current_threshold is not None else '30.0 (default)'` | Assigns/updates `threshold_str` with the expression on the right-hand side. (scope: def on_threshold_changed) |
| 368 | `                self.app.log(f'Anomaly Detection: Not enough samples for threshold calculation. Need {self.app.anomaly_detector.min_samples_for_threshold}, have {len(self.app.anomaly_detector.prediction_errors)}. Using threshold: {threshold_str} nT', level='Warning')` | Executes Python statement: `self.app.log(f'Anomaly Detection: Not enough samples for threshold calculation. Need {self.app.anomaly_detector.min_samples_for_threshold}, have {len(self.app.anomaly_detector.prediction_errors)}. Using threshold: {threshold_str} nT', level='Warning')`. (scope: def on_threshold_changed) |
| 369 | `            ` | Blank line used to separate logical sections and improve readability. (scope: def on_threshold_changed) |
| 370 | `            # Re-run anomaly detection with new threshold` | Comment explaining intent/context: Re-run anomaly detection with new threshold (scope: def on_threshold_changed) |
| 371 | `            # This will recalculate the threshold using the new multiplier and re-check all data` | Comment explaining intent/context: This will recalculate the threshold using the new multiplier and re-check all data (scope: def on_threshold_changed) |
| 372 | `            self.app._detect_anomalies()` | Executes Python statement: `self.app._detect_anomalies()`. (scope: def on_threshold_changed) |
| 373 | `            # Force canvas update to show new anomalies (with small delay to ensure detection completes)` | Comment explaining intent/context: Force canvas update to show new anomalies (with small delay to ensure detection completes) (scope: def on_threshold_changed) |
| 374 | `            if hasattr(self.app, '_update_canvas'):` | Starts an `if` block: conditional control flow based on `hasattr(self.app, '_update_canvas')`. (scope: def on_threshold_changed) |
| 375 | `                QTimer.singleShot(200, self.app._update_canvas)` | Configures/starts a timer to periodically trigger updates without blocking the UI thread. (scope: def on_threshold_changed) |
| 376 | `` | Blank line used to separate logical sections and improve readability. |
| 377 | `    def on_train_window_changed(self, value):` | Defines function/method `on_train_window_changed` (entry point for reusable logic). (scope: def on_train_window_changed) |
| 378 | `        """Called when user changes the LSTM training window (minutes). 0 means use full history."""` | Single-line triple-quoted string (docstring or multiline literal expressed on one line). (scope: def on_train_window_changed) |
| 379 | `        try:` | Starts a `try` block to catch and handle exceptions. (scope: def on_train_window_changed) |
| 380 | `            if value <= 0:` | Starts an `if` block: conditional control flow based on `value <= 0`. (scope: def on_train_window_changed) |
| 381 | `                self.app.train_window_minutes = None` | Assigns/updates `self.app.train_window_minutes` with the expression on the right-hand side. (scope: def on_train_window_changed) |
| 382 | `                self.app.log('Training window set to full history (0 minutes).', level='Info')` | Executes Python statement: `self.app.log('Training window set to full history (0 minutes).', level='Info')`. (scope: def on_train_window_changed) |
| 383 | `            else:` | Starts an `else` branch for the preceding conditional. (scope: def on_train_window_changed) |
| 384 | `                self.app.train_window_minutes = int(value)` | Assigns/updates `self.app.train_window_minutes` with the expression on the right-hand side. (scope: def on_train_window_changed) |
| 385 | `                self.app.log(f'Training window set to last {int(value)} minutes.', level='Info')` | Executes Python statement: `self.app.log(f'Training window set to last {int(value)} minutes.', level='Info')`. (scope: def on_train_window_changed) |
| 386 | `            self.update_train_window_status()` | Executes Python statement: `self.update_train_window_status()`. (scope: def on_train_window_changed) |
| 387 | `        except Exception as e:` | Starts an `except` handler: `except Exception as e:`. (scope: def on_train_window_changed) |
| 388 | `            self.app.log(f'Error setting training window: {e}', level='Error')` | Executes Python statement: `self.app.log(f'Error setting training window: {e}', level='Error')`. (scope: def on_train_window_changed) |
| 389 | `` | Blank line used to separate logical sections and improve readability. |
| 390 | `    def update_train_window_status(self):` | Defines function/method `update_train_window_status` (entry point for reusable logic). (scope: def update_train_window_status) |
| 391 | `        """Update UI badge to reflect training window minutes and time-aware status."""` | Single-line triple-quoted string (docstring or multiline literal expressed on one line). (scope: def update_train_window_status) |
| 392 | `        minutes = getattr(self.app, 'train_window_minutes', None)` | Assigns/updates `minutes` with the expression on the right-hand side. (scope: def update_train_window_status) |
| 393 | `        if minutes is None or minutes <= 0:` | Starts an `if` block: conditional control flow based on `minutes is None or minutes <= 0`. (scope: def update_train_window_status) |
| 394 | `            window_text = "Training window: all data"` | Assigns/updates `window_text` with the expression on the right-hand side. (scope: def update_train_window_status) |
| 395 | `        else:` | Starts an `else` branch for the preceding conditional. (scope: def update_train_window_status) |
| 396 | `            window_text = f"Training window: last {int(minutes)} min"` | Assigns/updates `window_text` with the expression on the right-hand side. (scope: def update_train_window_status) |
| 397 | `        # Daily sin/cos time-of-day features are always used; yearly is optional elsewhere.` | Comment explaining intent/context: Daily sin/cos time-of-day features are always used; yearly is optional elsewhere. (scope: def update_train_window_status) |
| 398 | `        self.train_window_status_label.setText(f"{window_text} \| Time-of-day features: on (daily sin/cos)")` | Executes Python statement: `self.train_window_status_label.setText(f"{window_text} | Time-of-day features: on (daily sin/cos)")`. (scope: def update_train_window_status) |
| 399 | `` | Blank line used to separate logical sections and improve readability. |
| 400 | `` | Blank line used to separate logical sections and improve readability. |
