# Line-by-line documentation: `src/application.py`

- **File**: `src/application.py`
- **Lines covered in this document**: 601–800 (of 1983)

## Structure overview (file-level)

- Line 72: function `load_vtk_file()`
- Line 81: function `getFilePath()`
- Line 94: class `SessionDataManager`
- Line 98: function `update_api_df()`
- Line 124: class `Color`
- Line 126: function `__init__()`
- Line 134: class `DraggableLabel`
- Line 135: method `mousePressEvent()`
- Line 139: function `mouseMoveEvent()`
- Line 156: class `DropLabel`
- Line 157: method `__init__()`
- Line 161: function `dragEnterEvent()`
- Line 165: function `dropEvent()`
- Line 172: class `MapWidget`
- Line 173: method `__init__()`
- Line 177: function `initUI()`
- Line 206: class `PandasModel`
- Line 209: function `__init__()`
- Line 213: function `rowCount()`
- Line 223: function `columnCount()`
- Line 232: function `data()`
- Line 245: function `headerData()`
- Line 262: class `MagTimeSeriesWidget`
- Line 263: method `__init__()`
- Line 269: method `timerRefreshEvent()`
- Line 283: method `initWidget()`
- Line 350: function `on_threshold_changed()`
- Line 377: function `on_train_window_changed()`
- Line 390: function `update_train_window_status()`
- Line 403: class `ApplicationWindow`
- Line 406: method `__init__()`
- Line 432: method `connectSlots()`
- Line 436: method `createThread()`
- Line 447: method `updateData()`
- Line 455: method `startThreads()`
- Line 462: function `addTimeSeries()`
- Line 469: function `showCsvData()`
- Line 493: function `setTreeView()`
- Line 500: function `updateTreeView()`
- Line 507: function `createMailModel()`
- Line 514: function `addSource()`
- Line 521: function `update_visualisation()`
- Line 604: function `clear_point_actors()`
- Line 612: function `set_points()`
- Line 673: function `sphere_prop_to_vtkarray()`
- Line 691: function `build_scene()`
- Line 728: function `setup_point_picker_framework()`
- Line 746: function `on_pick_left()`
- Line 771: function `on_pick_right()`
- Line 777: function `loadFile()`
- Line 803: function `log()`
- Line 824: class `Source`
- Line 825: method `__init__()`
- Line 830: class `TimeSeriesSource`
- Line 831: method `__init__()`
- Line 837: class `World`
- Line 838: method `__init__()`
- Line 845: method `loadCsv()`
- Line 860: class `DataSourceManager`
- Line 861: method `__init__()`
- Line 877: method `loadCsv()`
- Line 883: method `updateUI()`
- Line 887: method `createTimeSeriesSource()`
- Line 892: class `Application`
- Line 893: method `__init__()`
- Line 981: function `showAppMaximized()`
- Line 985: function `log()`
- Line 988: function `_save_data()`
- Line 1039: function `load_plot_framework()`
- Line 1119: function `load_plot_framework_2()`
- Line 1178: function `on_add_static()`
- Line 1196: function `on_add_dynamic()`
- Line 1222: function `start_prediction_process()`
- Line 1261: function `_update_xydata()`
- Line 1290: function `_update_predictions_data()`
- Line 1387: function `_sync_threshold_spinbox()`
- Line 1401: function `_detect_anomalies()`
- Line 1575: function `_update_canvas()`
- Line 1589: function `on_add_new()`
- Line 1626: function `on_add_pred()`
- Line 1745: function `appWin()`
- Line 1748: function `initViews()`
- Line 1754: function `load_visualization_framework()`
- Line 1858: function `load_axes()`
- Line 1881: function `load_sphere_source()`
- Line 1898: function `load_world_grid()`
- Line 1922: function `load_map()`
- Line 1957: function `get_map_xy_from_latlon()`
- Line 1969: function `get_map_lat_lon_from_xy()`

## Line-by-line explanation

| Line | Code | Explanation |
|---:|---|---|
| 601 | `        self._app.ren.Render()` | Executes Python statement: `self._app.ren.Render()`. |
| 602 | `        self._app.renwin.Render()` | Executes Python statement: `self._app.renwin.Render()`. |
| 603 | `` | Blank line used to separate logical sections and improve readability. |
| 604 | `    def clear_point_actors(self):` | Defines function/method `clear_point_actors` (entry point for reusable logic). (scope: def clear_point_actors) |
| 605 | `        if not hasattr(self, 'current_point_actors'):` | Starts an `if` block: conditional control flow based on `not hasattr(self, 'current_point_actors')`. (scope: def clear_point_actors) |
| 606 | `            pass` | No-op placeholder statement (keeps block syntactically valid). (scope: def clear_point_actors) |
| 607 | `        else:` | Starts an `else` branch for the preceding conditional. (scope: def clear_point_actors) |
| 608 | `            self.current_point_actors.VisibilityOff()` | Executes Python statement: `self.current_point_actors.VisibilityOff()`. (scope: def clear_point_actors) |
| 609 | `            self.current_point_actors.ReleaseGraphicsResources(self._app.renwin)` | Executes Python statement: `self.current_point_actors.ReleaseGraphicsResources(self._app.renwin)`. (scope: def clear_point_actors) |
| 610 | `            self._app.ren.RemoveActor(self.current_point_actors)` | Executes Python statement: `self._app.ren.RemoveActor(self.current_point_actors)`. (scope: def clear_point_actors) |
| 611 | `    ` | Blank line used to separate logical sections and improve readability. |
| 612 | `    def set_points(self, coords):` | Defines function/method `set_points` (entry point for reusable logic). (scope: def set_points) |
| 613 | `        """This function sets the new set of coordinates on the canvas` | Executes Python statement: `"""This function sets the new set of coordinates on the canvas`. (scope: def set_points) |
| 614 | `        """` | Ends a triple-quoted string (closing a docstring/multiline literal). (scope: def set_points) |
| 615 | `        n_tgt = len(coords)` | Assigns/updates `n_tgt` with the expression on the right-hand side. (scope: def set_points) |
| 616 | `        radii, colors, indices = ApplicationWindow.sphere_prop_to_vtkarray(n_tgt, 0.01, 0)` | Assigns/updates `radii, colors, indices` with the expression on the right-hand side. (scope: def set_points) |
| 617 | `        ` | Blank line used to separate logical sections and improve readability. (scope: def set_points) |
| 618 | `        polydata = vtk.vtkPolyData()` | Assigns/updates `polydata` with the expression on the right-hand side. Hint: VTK (3D visualization) usage. (scope: def set_points) |
| 619 | `        polydata.GetPointData().AddArray(radii)` | Executes Python statement: `polydata.GetPointData().AddArray(radii)`. (scope: def set_points) |
| 620 | `        polydata.GetPointData().SetActiveScalars(radii.GetName())` | Executes Python statement: `polydata.GetPointData().SetActiveScalars(radii.GetName())`. (scope: def set_points) |
| 621 | `        polydata.GetPointData().AddArray(colors)` | Executes Python statement: `polydata.GetPointData().AddArray(colors)`. (scope: def set_points) |
| 622 | `        polydata.GetPointData().AddArray(indices)` | Executes Python statement: `polydata.GetPointData().AddArray(indices)`. (scope: def set_points) |
| 623 | `` | Blank line used to separate logical sections and improve readability. |
| 624 | `        points = vtk.vtkPoints()` | Assigns/updates `points` with the expression on the right-hand side. Hint: VTK (3D visualization) usage. |
| 625 | `        points.SetNumberOfPoints(n_tgt)` | Executes Python statement: `points.SetNumberOfPoints(n_tgt)`. |
| 626 | `        mag_scalars = vtk.vtkFloatArray()` | Assigns/updates `mag_scalars` with the expression on the right-hand side. Hint: VTK (3D visualization) usage. |
| 627 | `        mag_scalars.SetName("mag")` | Executes Python statement: `mag_scalars.SetName("mag")`. |
| 628 | `        for i, (x, y, z, mag) in enumerate(coords):` | Starts a `for` loop: iterates as described by `i, (x, y, z, mag) in enumerate(coords)`. |
| 629 | `            points.SetPoint(i, x, y, z)` | Executes Python statement: `points.SetPoint(i, x, y, z)`. |
| 630 | `            mag_scalars.InsertNextValue(mag)` | Executes Python statement: `mag_scalars.InsertNextValue(mag)`. |
| 631 | `        # polydata.GetPointData().SetScalars(mag_scalars)` | Comment explaining intent/context: polydata.GetPointData().SetScalars(mag_scalars) |
| 632 | `        polydata.SetPoints(points)` | Executes Python statement: `polydata.SetPoints(points)`. |
| 633 | `` | Blank line used to separate logical sections and improve readability. |
| 634 | `        # Finally update the renderer` | Comment explaining intent/context: Finally update the renderer |
| 635 | `        self.current_point_actors = self.build_scene(polydata)` | Assigns/updates `self.current_point_actors` with the expression on the right-hand side. |
| 636 | `        self._app.ren.AddActor(self.current_point_actors)` | Executes Python statement: `self._app.ren.AddActor(self.current_point_actors)`. |
| 637 | `` | Blank line used to separate logical sections and improve readability. |
| 638 | `        self._app.iren.Render()` | Executes Python statement: `self._app.iren.Render()`. |
| 639 | `        ` | Blank line used to separate logical sections and improve readability. |
| 640 | `        ` | Blank line used to separate logical sections and improve readability. |
| 641 | `        use_delaunay = False # creating a 2d surface using the points - not working quite well for unordered points, as well as not working for large number of points` | Assigns/updates `use_delaunay` with the expression on the right-hand side. |
| 642 | `        if use_delaunay:` | Starts an `if` block: conditional control flow based on `use_delaunay`. |
| 643 | `            # # To create surface of a sphere we need to use Delaunay triangulation` | Comment explaining intent/context: # To create surface of a sphere we need to use Delaunay triangulation |
| 644 | `            d2D = vtk.vtkDelaunay2D()` | Assigns/updates `d2D` with the expression on the right-hand side. Hint: VTK (3D visualization) usage. |
| 645 | `            d2D.SetInputData( polydata ) # This generates a 3D mesh` | Executes Python statement: `d2D.SetInputData( polydata ) # This generates a 3D mesh`. |
| 646 | `            # We need to extract the surface from the 3D mesh` | Comment explaining intent/context: We need to extract the surface from the 3D mesh |
| 647 | `            dss = vtk.vtkDataSetSurfaceFilter()` | Assigns/updates `dss` with the expression on the right-hand side. Hint: VTK (3D visualization) usage. |
| 648 | `            dss.SetInputConnection( d2D.GetOutputPort() )` | Executes Python statement: `dss.SetInputConnection( d2D.GetOutputPort() )`. |
| 649 | `            print('input set')` | Executes Python statement: `print('input set')`. |
| 650 | `            dss.Update()` | Executes Python statement: `dss.Update()`. |
| 651 | `` | Blank line used to separate logical sections and improve readability. |
| 652 | `            # Now we have our final polydata` | Comment explaining intent/context: Now we have our final polydata |
| 653 | `            # The running time of Delaunay triangulation in VTK is \\(O(n\\log n)\\).` | Comment explaining intent/context: The running time of Delaunay triangulation in VTK is \(O(n\log n)\). |
| 654 | `            # This is because the triangulation is computed for each set and then the sets are merged along the splitting line` | Comment explaining intent/context: This is because the triangulation is computed for each set and then the sets are merged along the splitting line |
| 655 | `` | Blank line used to separate logical sections and improve readability. |
| 656 | `            dsPoly = dss.GetOutput()` | Assigns/updates `dsPoly` with the expression on the right-hand side. |
| 657 | `            print('output set', dsPoly)` | Executes Python statement: `print('output set', dsPoly)`. |
| 658 | `            sphere_mapper = vtkPolyDataMapper()` | Assigns/updates `sphere_mapper` with the expression on the right-hand side. Hint: VTK (3D visualization) usage. |
| 659 | `            sphere_mapper.SetInputData(dsPoly)` | Executes Python statement: `sphere_mapper.SetInputData(dsPoly)`. |
| 660 | `` | Blank line used to separate logical sections and improve readability. |
| 661 | `            sphere_actor = vtk.vtkActor()` | Assigns/updates `sphere_actor` with the expression on the right-hand side. Hint: VTK (3D visualization) usage. |
| 662 | `            sphere_actor.SetMapper(sphere_mapper)` | Executes Python statement: `sphere_actor.SetMapper(sphere_mapper)`. |
| 663 | `            self._app.ren.AddActor(sphere_actor)` | Executes Python statement: `self._app.ren.AddActor(sphere_actor)`. |
| 664 | `            self._app.iren.Render()` | Executes Python statement: `self._app.iren.Render()`. |
| 665 | `        ` | Blank line used to separate logical sections and improve readability. |
| 666 | `        ` | Blank line used to separate logical sections and improve readability. |
| 667 | `        ` | Blank line used to separate logical sections and improve readability. |
| 668 | `        # glyphFilter = vtk.vtkVertexGlyphFilter()` | Comment explaining intent/context: glyphFilter = vtk.vtkVertexGlyphFilter() |
| 669 | `        # glyphFilter.SetInputData(poly)` | Comment explaining intent/context: glyphFilter.SetInputData(poly) |
| 670 | `        # glyphFilter.Update()` | Comment explaining intent/context: glyphFilter.Update() |
| 671 | `    ` | Blank line used to separate logical sections and improve readability. |
| 672 | `    @staticmethod` | Executes Python statement: `@staticmethod`. |
| 673 | `    def sphere_prop_to_vtkarray(n_sphere, radius, color):` | Defines function/method `sphere_prop_to_vtkarray` (entry point for reusable logic). (scope: def sphere_prop_to_vtkarray) |
| 674 | `        radii = vtk.vtkFloatArray()` | Assigns/updates `radii` with the expression on the right-hand side. Hint: VTK (3D visualization) usage. (scope: def sphere_prop_to_vtkarray) |
| 675 | `        radii.SetName('radius')` | Executes Python statement: `radii.SetName('radius')`. (scope: def sphere_prop_to_vtkarray) |
| 676 | `        for _ in range(n_sphere):` | Starts a `for` loop: iterates as described by `_ in range(n_sphere)`. (scope: def sphere_prop_to_vtkarray) |
| 677 | `            radii.InsertNextTuple1(radius)` | Executes Python statement: `radii.InsertNextTuple1(radius)`. (scope: def sphere_prop_to_vtkarray) |
| 678 | `` | Blank line used to separate logical sections and improve readability. |
| 679 | `        colors = vtk.vtkFloatArray()` | Assigns/updates `colors` with the expression on the right-hand side. Hint: VTK (3D visualization) usage. |
| 680 | `        colors.SetName('color')` | Executes Python statement: `colors.SetName('color')`. |
| 681 | `        for _ in range(n_sphere):` | Starts a `for` loop: iterates as described by `_ in range(n_sphere)`. |
| 682 | `            colors.InsertNextTuple1(color)` | Executes Python statement: `colors.InsertNextTuple1(color)`. |
| 683 | `` | Blank line used to separate logical sections and improve readability. |
| 684 | `        indices = vtk.vtkIntArray()` | Assigns/updates `indices` with the expression on the right-hand side. Hint: VTK (3D visualization) usage. |
| 685 | `        indices.SetName('index')` | Executes Python statement: `indices.SetName('index')`. |
| 686 | `        for idx in range(n_sphere):` | Starts a `for` loop: iterates as described by `idx in range(n_sphere)`. |
| 687 | `            indices.InsertNextTuple1(idx)` | Executes Python statement: `indices.InsertNextTuple1(idx)`. |
| 688 | `` | Blank line used to separate logical sections and improve readability. |
| 689 | `        return radii, colors, indices` | Returns value(s) from the current function: `radii, colors, indices`. |
| 690 | `    ` | Blank line used to separate logical sections and improve readability. |
| 691 | `    def build_scene(self, polydata):` | Defines function/method `build_scene` (entry point for reusable logic). (scope: def build_scene) |
| 692 | `        """build a vtkPolyData object for a given frame of the trajectory` | Executes statement related to: VTK (3D visualization) usage: `"""build a vtkPolyData object for a given frame of the trajectory`. (scope: def build_scene) |
| 693 | `        """` | Ends a triple-quoted string (closing a docstring/multiline literal). (scope: def build_scene) |
| 694 | `` | Blank line used to separate logical sections and improve readability. |
| 695 | `        # The rest is for building the point-spheres` | Comment explaining intent/context: The rest is for building the point-spheres |
| 696 | `        sphere = vtk.vtkSphereSource()` | Assigns/updates `sphere` with the expression on the right-hand side. Hint: VTK (3D visualization) usage. |
| 697 | `        sphere.SetCenter(0, 0, 0)` | Executes Python statement: `sphere.SetCenter(0, 0, 0)`. |
| 698 | `        sphere.SetRadius(0.2)` | Executes Python statement: `sphere.SetRadius(0.2)`. |
| 699 | `        sphere.SetPhiResolution(10)` | Executes Python statement: `sphere.SetPhiResolution(10)`. |
| 700 | `        sphere.SetThetaResolution(10)` | Executes Python statement: `sphere.SetThetaResolution(10)`. |
| 701 | `` | Blank line used to separate logical sections and improve readability. |
| 702 | `        self.glyph = vtk.vtkGlyph3D()` | Assigns/updates `self.glyph` with the expression on the right-hand side. Hint: VTK (3D visualization) usage. |
| 703 | `        self.glyph.GeneratePointIdsOn()` | Executes Python statement: `self.glyph.GeneratePointIdsOn()`. |
| 704 | `        self.glyph.SetInputData(polydata)` | Executes Python statement: `self.glyph.SetInputData(polydata)`. |
| 705 | `        self.glyph.SetScaleModeToScaleByScalar()` | Executes Python statement: `self.glyph.SetScaleModeToScaleByScalar()`. |
| 706 | `        self.glyph.SetSourceConnection(sphere.GetOutputPort())` | Executes Python statement: `self.glyph.SetSourceConnection(sphere.GetOutputPort())`. |
| 707 | `        self.glyph.Update()` | Executes Python statement: `self.glyph.Update()`. |
| 708 | `` | Blank line used to separate logical sections and improve readability. |
| 709 | `        sphere_mapper = vtkPolyDataMapper()` | Assigns/updates `sphere_mapper` with the expression on the right-hand side. Hint: VTK (3D visualization) usage. |
| 710 | `        # sphere_mapper.SetLookupTable(self.lut_color)` | Comment explaining intent/context: sphere_mapper.SetLookupTable(self.lut_color) |
| 711 | `        sphere_mapper.SetInputConnection(self.glyph.GetOutputPort())` | Executes Python statement: `sphere_mapper.SetInputConnection(self.glyph.GetOutputPort())`. |
| 712 | `        sphere_mapper.SetScalarModeToUsePointFieldData()` | Executes Python statement: `sphere_mapper.SetScalarModeToUsePointFieldData()`. |
| 713 | `        sphere_mapper.SelectColorArray('color')` | Executes Python statement: `sphere_mapper.SelectColorArray('color')`. |
| 714 | `        ` | Blank line used to separate logical sections and improve readability. |
| 715 | `        ball_actor = vtk.vtkLODActor()` | Assigns/updates `ball_actor` with the expression on the right-hand side. Hint: VTK (3D visualization) usage. |
| 716 | `        ball_actor.SetMapper(sphere_mapper)` | Executes Python statement: `ball_actor.SetMapper(sphere_mapper)`. |
| 717 | `        ball_actor.GetProperty().SetAmbient(0.2)` | Executes Python statement: `ball_actor.GetProperty().SetAmbient(0.2)`. |
| 718 | `        ball_actor.GetProperty().SetDiffuse(0.5)` | Executes Python statement: `ball_actor.GetProperty().SetDiffuse(0.5)`. |
| 719 | `        ball_actor.GetProperty().SetSpecular(0.3)` | Executes Python statement: `ball_actor.GetProperty().SetSpecular(0.3)`. |
| 720 | `` | Blank line used to separate logical sections and improve readability. |
| 721 | `        self._picking_domain = ball_actor` | Assigns/updates `self._picking_domain` with the expression on the right-hand side. |
| 722 | `` | Blank line used to separate logical sections and improve readability. |
| 723 | `        assembly = vtk.vtkAssembly()` | Assigns/updates `assembly` with the expression on the right-hand side. Hint: VTK (3D visualization) usage. |
| 724 | `        assembly.AddPart(ball_actor)` | Executes Python statement: `assembly.AddPart(ball_actor)`. |
| 725 | `` | Blank line used to separate logical sections and improve readability. |
| 726 | `        return assembly` | Returns value(s) from the current function: `assembly`. |
| 727 | `` | Blank line used to separate logical sections and improve readability. |
| 728 | `    def setup_point_picker_framework(self):` | Defines function/method `setup_point_picker_framework` (entry point for reusable logic). (scope: def setup_point_picker_framework) |
| 729 | `        # Text actor for printing the coordinates of the clicked point` | Comment explaining intent/context: Text actor for printing the coordinates of the clicked point (scope: def setup_point_picker_framework) |
| 730 | `        self.actor_title = vtk.vtkTextActor()` | Assigns/updates `self.actor_title` with the expression on the right-hand side. Hint: VTK (3D visualization) usage. (scope: def setup_point_picker_framework) |
| 731 | `        self.actor_title.SetInput('')` | Executes Python statement: `self.actor_title.SetInput('')`. (scope: def setup_point_picker_framework) |
| 732 | `        self.actor_title.GetTextProperty().SetFontFamilyToArial()` | Executes Python statement: `self.actor_title.GetTextProperty().SetFontFamilyToArial()`. (scope: def setup_point_picker_framework) |
| 733 | `        self.actor_title.GetTextProperty().BoldOn()` | Executes Python statement: `self.actor_title.GetTextProperty().BoldOn()`. (scope: def setup_point_picker_framework) |
| 734 | `        self.actor_title.GetTextProperty().SetFontSize(12)` | Executes Python statement: `self.actor_title.GetTextProperty().SetFontSize(12)`. (scope: def setup_point_picker_framework) |
| 735 | `        self.actor_title.GetTextProperty().SetColor(1, 0.9, 0.8)` | Executes Python statement: `self.actor_title.GetTextProperty().SetColor(1, 0.9, 0.8)`. (scope: def setup_point_picker_framework) |
| 736 | `        self.actor_title.SetDisplayPosition(10, 10)` | Executes Python statement: `self.actor_title.SetDisplayPosition(10, 10)`. (scope: def setup_point_picker_framework) |
| 737 | `        self._app.ren.AddActor(self.actor_title)` | Executes Python statement: `self._app.ren.AddActor(self.actor_title)`. (scope: def setup_point_picker_framework) |
| 738 | `        ` | Blank line used to separate logical sections and improve readability. (scope: def setup_point_picker_framework) |
| 739 | `        self._app.iren.AddObserver('LeftButtonPressEvent', self.on_pick_left)` | Executes Python statement: `self._app.iren.AddObserver('LeftButtonPressEvent', self.on_pick_left)`. (scope: def setup_point_picker_framework) |
| 740 | `        self._app.iren.AddObserver('RightButtonPressEvent', self.on_pick_right)` | Executes Python statement: `self._app.iren.AddObserver('RightButtonPressEvent', self.on_pick_right)`. (scope: def setup_point_picker_framework) |
| 741 | `` | Blank line used to separate logical sections and improve readability. |
| 742 | `        self.current_points_to_plot = np.empty((0, 3))` | Assigns/updates `self.current_points_to_plot` with the expression on the right-hand side. |
| 743 | `` | Blank line used to separate logical sections and improve readability. |
| 744 | `        # self.set_camera_position() #not required imo` | Comment explaining intent/context: self.set_camera_position() #not required imo |
| 745 | `` | Blank line used to separate logical sections and improve readability. |
| 746 | `    def on_pick_left(self, obj, event=None):` | Defines function/method `on_pick_left` (entry point for reusable logic). (scope: def on_pick_left) |
| 747 | `        """Event handler when a point is mouse-picked with the left mouse button` | Executes Python statement: `"""Event handler when a point is mouse-picked with the left mouse button`. (scope: def on_pick_left) |
| 748 | `        """` | Ends a triple-quoted string (closing a docstring/multiline literal). (scope: def on_pick_left) |
| 749 | `        ` | Blank line used to separate logical sections and improve readability. (scope: def on_pick_left) |
| 750 | `        if not hasattr(self, '_picking_domain'):` | Starts an `if` block: conditional control flow based on `not hasattr(self, '_picking_domain')`. (scope: def on_pick_left) |
| 751 | `            return` | Returns from the current function (no explicit value, so `None`). (scope: def on_pick_left) |
| 752 | `` | Blank line used to separate logical sections and improve readability. |
| 753 | `        # Get the picked position and retrieve the index of the target that was picked from it` | Comment explaining intent/context: Get the picked position and retrieve the index of the target that was picked from it |
| 754 | `        pos = obj.GetEventPosition()` | Assigns/updates `pos` with the expression on the right-hand side. |
| 755 | `` | Blank line used to separate logical sections and improve readability. |
| 756 | `        picker = vtk.vtkPointPicker()` | Assigns/updates `picker` with the expression on the right-hand side. Hint: VTK (3D visualization) usage. |
| 757 | `        picker.SetTolerance(0.05)` | Executes Python statement: `picker.SetTolerance(0.05)`. |
| 758 | `` | Blank line used to separate logical sections and improve readability. |
| 759 | `        picker.AddPickList(self._picking_domain)` | Executes Python statement: `picker.AddPickList(self._picking_domain)`. |
| 760 | `        picker.PickFromListOn()` | Executes Python statement: `picker.PickFromListOn()`. |
| 761 | `        picker.Pick(pos[0], pos[1], 0, self._app.ren)` | Executes Python statement: `picker.Pick(pos[0], pos[1], 0, self._app.ren)`. |
| 762 | `        pid = picker.GetPointId()` | Assigns/updates `pid` with the expression on the right-hand side. |
| 763 | `        print('pick event pid', pid)` | Executes Python statement: `print('pick event pid', pid)`. |
| 764 | `        if pid > 0:` | Starts an `if` block: conditional control flow based on `pid > 0`. |
| 765 | `            idx = int(self.glyph.GetOutput().GetPointData().GetArray('index').GetTuple1(pid))` | Assigns/updates `idx` with the expression on the right-hand side. |
| 766 | `            x, y, z, mag = self.current_points_to_plot[idx]` | Assigns/updates `x, y, z, mag` with the expression on the right-hand side. |
| 767 | `            text = f'postion: {(x, y, z)}; mag: {mag}' # Index: {idx};` | Assigns/updates `text` with the expression on the right-hand side. |
| 768 | `            print(text)` | Executes Python statement: `print(text)`. |
| 769 | `            self.actor_title.SetInput(text)` | Executes Python statement: `self.actor_title.SetInput(text)`. |
| 770 | `    ` | Blank line used to separate logical sections and improve readability. |
| 771 | `    def on_pick_right(self, obj, event=None):` | Defines function/method `on_pick_right` (entry point for reusable logic). (scope: def on_pick_right) |
| 772 | `        """Clears the the text field when right mouse button is clicked` | Executes Python statement: `"""Clears the the text field when right mouse button is clicked`. (scope: def on_pick_right) |
| 773 | `        """` | Ends a triple-quoted string (closing a docstring/multiline literal). (scope: def on_pick_right) |
| 774 | `    ` | Blank line used to separate logical sections and improve readability. |
| 775 | `        self.actor_title.SetInput(f'')` | Executes Python statement: `self.actor_title.SetInput(f'')`. |
| 776 | `    ` | Blank line used to separate logical sections and improve readability. |
| 777 | `    def loadFile(self):` | Defines function/method `loadFile` (entry point for reusable logic). (scope: def loadFile) |
| 778 | `        try:` | Starts a `try` block to catch and handle exceptions. (scope: def loadFile) |
| 779 | `            self.log('upload clicked', level='Debug')` | Executes Python statement: `self.log('upload clicked', level='Debug')`. (scope: def loadFile) |
| 780 | `            uploaded_f_name = getFilePath('Load CSV Data: Geo-Magnetic data')` | Assigns/updates `uploaded_f_name` with the expression on the right-hand side. (scope: def loadFile) |
| 781 | `            if uploaded_f_name and type(uploaded_f_name)==tuple:` | Starts an `if` block: conditional control flow based on `uploaded_f_name and type(uploaded_f_name)==tuple`. (scope: def loadFile) |
| 782 | `                f_name = uploaded_f_name[0]` | Assigns/updates `f_name` with the expression on the right-hand side. (scope: def loadFile) |
| 783 | `                f_ext = uploaded_f_name[1]` | Assigns/updates `f_ext` with the expression on the right-hand side. (scope: def loadFile) |
| 784 | `                if f_name:` | Starts an `if` block: conditional control flow based on `f_name`. (scope: def loadFile) |
| 785 | `                    try:` | Starts a `try` block to catch and handle exceptions. (scope: def loadFile) |
| 786 | `                        if f_name.lower().endswith('.csv'):` | Starts an `if` block: conditional control flow based on `f_name.lower().endswith('.csv')`. (scope: def loadFile) |
| 787 | `                            self._app._dataSourceManager.loadCsv(f_name) # can this happen in different thread to prevent ui freeze? probably yes - need to figure out - todo` | Executes Python statement: `self._app._dataSourceManager.loadCsv(f_name) # can this happen in different thread to prevent ui freeze? probably yes - need to figure out - todo`. (scope: def loadFile) |
| 788 | `                            self.log(f'Loaded file "{f_name}"')` | Executes Python statement: `self.log(f'Loaded file "{f_name}"')`. (scope: def loadFile) |
| 789 | `                            self.showCsvData(self._app._dataSourceManager._world_list[-1].dataFrame)` | Executes Python statement: `self.showCsvData(self._app._dataSourceManager._world_list[-1].dataFrame)`. (scope: def loadFile) |
| 790 | `                            self.updateTreeView()` | Executes Python statement: `self.updateTreeView()`. (scope: def loadFile) |
| 791 | `                            self.update_visualisation()` | Executes Python statement: `self.update_visualisation()`. (scope: def loadFile) |
| 792 | `                            ` | Blank line used to separate logical sections and improve readability. (scope: def loadFile) |
| 793 | `                        else:` | Starts an `else` branch for the preceding conditional. (scope: def loadFile) |
| 794 | `                            self.log(f'{"f_name"} is of unsupported file format. Expected *.csv')` | Executes Python statement: `self.log(f'{"f_name"} is of unsupported file format. Expected *.csv')`. (scope: def loadFile) |
| 795 | `                    except Exception as e_in:` | Starts an `except` handler: `except Exception as e_in:`. (scope: def loadFile) |
| 796 | `                        self.log(f'error loading {f_name}', error=e_in, level='Error')` | Executes Python statement: `self.log(f'error loading {f_name}', error=e_in, level='Error')`. (scope: def loadFile) |
| 797 | `                        print('e_in', e_in)` | Executes Python statement: `print('e_in', e_in)`. (scope: def loadFile) |
| 798 | `                else:` | Starts an `else` branch for the preceding conditional. (scope: def loadFile) |
| 799 | `                    self.log(f'no file selected')` | Executes Python statement: `self.log(f'no file selected')`. (scope: def loadFile) |
| 800 | `        except Exception as e:` | Starts an `except` handler: `except Exception as e:`. (scope: def loadFile) |
