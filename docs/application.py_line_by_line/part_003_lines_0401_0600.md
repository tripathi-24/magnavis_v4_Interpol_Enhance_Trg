# Line-by-line documentation: `src/application.py`

- **File**: `src/application.py`
- **Lines covered in this document**: 401–600 (of 1983)

## Structure overview (file-level)

- Line 72: function `load_vtk_file()`
- Line 81: function `getFilePath()`
- Line 94: class `SessionDataManager`
- Line 98: function `update_api_df()`
- Line 124: class `Color`
- Line 126: function `__init__()`
- Line 134: class `DraggableLabel`
- Line 135: method `mousePressEvent()`
- Line 139: function `mouseMoveEvent()`
- Line 156: class `DropLabel`
- Line 157: method `__init__()`
- Line 161: function `dragEnterEvent()`
- Line 165: function `dropEvent()`
- Line 172: class `MapWidget`
- Line 173: method `__init__()`
- Line 177: function `initUI()`
- Line 206: class `PandasModel`
- Line 209: function `__init__()`
- Line 213: function `rowCount()`
- Line 223: function `columnCount()`
- Line 232: function `data()`
- Line 245: function `headerData()`
- Line 262: class `MagTimeSeriesWidget`
- Line 263: method `__init__()`
- Line 269: method `timerRefreshEvent()`
- Line 283: method `initWidget()`
- Line 350: function `on_threshold_changed()`
- Line 377: function `on_train_window_changed()`
- Line 390: function `update_train_window_status()`
- Line 403: class `ApplicationWindow`
- Line 406: method `__init__()`
- Line 432: method `connectSlots()`
- Line 436: method `createThread()`
- Line 447: method `updateData()`
- Line 455: method `startThreads()`
- Line 462: function `addTimeSeries()`
- Line 469: function `showCsvData()`
- Line 493: function `setTreeView()`
- Line 500: function `updateTreeView()`
- Line 507: function `createMailModel()`
- Line 514: function `addSource()`
- Line 521: function `update_visualisation()`
- Line 604: function `clear_point_actors()`
- Line 612: function `set_points()`
- Line 673: function `sphere_prop_to_vtkarray()`
- Line 691: function `build_scene()`
- Line 728: function `setup_point_picker_framework()`
- Line 746: function `on_pick_left()`
- Line 771: function `on_pick_right()`
- Line 777: function `loadFile()`
- Line 803: function `log()`
- Line 824: class `Source`
- Line 825: method `__init__()`
- Line 830: class `TimeSeriesSource`
- Line 831: method `__init__()`
- Line 837: class `World`
- Line 838: method `__init__()`
- Line 845: method `loadCsv()`
- Line 860: class `DataSourceManager`
- Line 861: method `__init__()`
- Line 877: method `loadCsv()`
- Line 883: method `updateUI()`
- Line 887: method `createTimeSeriesSource()`
- Line 892: class `Application`
- Line 893: method `__init__()`
- Line 981: function `showAppMaximized()`
- Line 985: function `log()`
- Line 988: function `_save_data()`
- Line 1039: function `load_plot_framework()`
- Line 1119: function `load_plot_framework_2()`
- Line 1178: function `on_add_static()`
- Line 1196: function `on_add_dynamic()`
- Line 1222: function `start_prediction_process()`
- Line 1261: function `_update_xydata()`
- Line 1290: function `_update_predictions_data()`
- Line 1387: function `_sync_threshold_spinbox()`
- Line 1401: function `_detect_anomalies()`
- Line 1575: function `_update_canvas()`
- Line 1589: function `on_add_new()`
- Line 1626: function `on_add_pred()`
- Line 1745: function `appWin()`
- Line 1748: function `initViews()`
- Line 1754: function `load_visualization_framework()`
- Line 1858: function `load_axes()`
- Line 1881: function `load_sphere_source()`
- Line 1898: function `load_world_grid()`
- Line 1922: function `load_map()`
- Line 1957: function `get_map_xy_from_latlon()`
- Line 1969: function `get_map_lat_lon_from_xy()`

## Line-by-line explanation

| Line | Code | Explanation |
|---:|---|---|
| 401 | `appBase, appForm = uic.loadUiType(os.path.join(APP_BASE, 'ui_files', 'ui_ApplicationWindow.ui'))` | Assigns/updates `appBase, appForm` with the expression on the right-hand side. |
| 402 | `` | Blank line used to separate logical sections and improve readability. |
| 403 | `class ApplicationWindow(appBase, appForm):` | Defines class `ApplicationWindow` (starts a new type/namespace for related behavior). (scope: class ApplicationWindow) |
| 404 | `    SOURCE, _ = range(2) #, TYPE, ACTIVE = range(3)` | Assigns/updates `SOURCE, _` with the expression on the right-hand side. (scope: class ApplicationWindow) |
| 405 | `    ` | Blank line used to separate logical sections and improve readability. (scope: class ApplicationWindow) |
| 406 | `    def __init__(self, app, parent = None):` | Defines function/method `__init__` (entry point for reusable logic). (scope: class ApplicationWindow → def __init__) |
| 407 | `        super(appBase, self).__init__(parent)` | Executes Python statement: `super(appBase, self).__init__(parent)`. (scope: class ApplicationWindow → def __init__) |
| 408 | `        self.setupUi(self)` | Executes Python statement: `self.setupUi(self)`. (scope: class ApplicationWindow → def __init__) |
| 409 | `        self.threads = []` | Assigns/updates `self.threads` with the expression on the right-hand side. (scope: class ApplicationWindow → def __init__) |
| 410 | `        self.framework_2_loaded = False` | Assigns/updates `self.framework_2_loaded` with the expression on the right-hand side. (scope: class ApplicationWindow → def __init__) |
| 411 | `        # self.tab_2.setMinimumHeight(550)` | Comment explaining intent/context: self.tab_2.setMinimumHeight(550) (scope: class ApplicationWindow → def __init__) |
| 412 | `        self._app = app ` | Assigns/updates `self._app` with the expression on the right-hand side. (scope: class ApplicationWindow → def __init__) |
| 413 | `        self._app._app_window = self ` | Assigns/updates `self._app._app_window` with the expression on the right-hand side. (scope: class ApplicationWindow → def __init__) |
| 414 | `        self.model = None` | Assigns/updates `self.model` with the expression on the right-hand side. (scope: class ApplicationWindow → def __init__) |
| 415 | `        # WorkbenchHelper.application = self._app` | Comment explaining intent/context: WorkbenchHelper.application = self._app (scope: class ApplicationWindow → def __init__) |
| 416 | `        # WorkbenchHelper.window = self` | Comment explaining intent/context: WorkbenchHelper.window = self (scope: class ApplicationWindow → def __init__) |
| 417 | `        ` | Blank line used to separate logical sections and improve readability. (scope: class ApplicationWindow → def __init__) |
| 418 | `        # self._graphicsTab = MayaviQWidget(app)` | Comment explaining intent/context: self._graphicsTab = MayaviQWidget(app) (scope: class ApplicationWindow → def __init__) |
| 419 | `        # self._graphicsManager = GraphicsManager()` | Comment explaining intent/context: self._graphicsManager = GraphicsManager() (scope: class ApplicationWindow → def __init__) |
| 420 | `        ` | Blank line used to separate logical sections and improve readability. (scope: class ApplicationWindow → def __init__) |
| 421 | `        # self.uiObjectEditorTabWidget.clear()` | Comment explaining intent/context: self.uiObjectEditorTabWidget.clear() (scope: class ApplicationWindow → def __init__) |
| 422 | `        #` | Comment line used as a visual separator. (scope: class ApplicationWindow → def __init__) |
| 423 | `        # self.uiObjectEditorTabWidget.addTab(self._app._dataSourceManager._data_sourceTab, "Data_source")` | Comment explaining intent/context: self.uiObjectEditorTabWidget.addTab(self._app._dataSourceManager._data_sourceTab, "Data_source") (scope: class ApplicationWindow → def __init__) |
| 424 | `        #` | Comment line used as a visual separator. (scope: class ApplicationWindow → def __init__) |
| 425 | `        # self.uiGraphicsTabWidget.clear()` | Comment explaining intent/context: self.uiGraphicsTabWidget.clear() (scope: class ApplicationWindow → def __init__) |
| 426 | `        # self.uiGraphicsTabWidget.addTab(self._graphicsTab, 'Visualization')` | Comment explaining intent/context: self.uiGraphicsTabWidget.addTab(self._graphicsTab, 'Visualization') (scope: class ApplicationWindow → def __init__) |
| 427 | `        self.setTreeView()` | Executes Python statement: `self.setTreeView()`. (scope: class ApplicationWindow → def __init__) |
| 428 | `        self.setWindowTitle(app.productName)` | Executes Python statement: `self.setWindowTitle(app.productName)`. (scope: class ApplicationWindow → def __init__) |
| 429 | `        # self.show()` | Comment explaining intent/context: self.show() (scope: class ApplicationWindow → def __init__) |
| 430 | `        self.connectSlots()` | Executes Python statement: `self.connectSlots()`. (scope: class ApplicationWindow → def __init__) |
| 431 | `    ` | Blank line used to separate logical sections and improve readability. (scope: class ApplicationWindow) |
| 432 | `    def connectSlots(self):` | Defines function/method `connectSlots` (entry point for reusable logic). (scope: class ApplicationWindow → def connectSlots) |
| 433 | `        self.actionUpload.triggered.connect(self.loadFile)` | Connects a Qt signal to a slot/callback so UI events trigger Python code. (scope: class ApplicationWindow → def connectSlots) |
| 434 | `        self.actionAdd_Time_Series.triggered.connect(self.addTimeSeries)` | Connects a Qt signal to a slot/callback so UI events trigger Python code. (scope: class ApplicationWindow → def connectSlots) |
| 435 | `    ` | Blank line used to separate logical sections and improve readability. (scope: class ApplicationWindow) |
| 436 | `    def createThread(self, session_id, hours, start_time, new):` | Defines function/method `createThread` (entry point for reusable logic). (scope: class ApplicationWindow → def createThread) |
| 437 | `        thread = QThread()` | Assigns/updates `thread` with the expression on the right-hand side. Hint: PyQt5/Qt GUI framework usage. (scope: class ApplicationWindow → def createThread) |
| 438 | `        worker = SessionDataManager()` | Assigns/updates `worker` with the expression on the right-hand side. (scope: class ApplicationWindow → def createThread) |
| 439 | `        worker.moveToThread(thread)` | Executes Python statement: `worker.moveToThread(thread)`. (scope: class ApplicationWindow → def createThread) |
| 440 | `        thread.started.connect(lambda: worker.update_api_df(session_id, hours, start_time, new))` | Connects a Qt signal to a slot/callback so UI events trigger Python code. (scope: class ApplicationWindow → def createThread) |
| 441 | `        worker.updatedData.connect(self.updateData)` | Connects a Qt signal to a slot/callback so UI events trigger Python code. (scope: class ApplicationWindow → def createThread) |
| 442 | `        worker.finished.connect(thread.quit)` | Connects a Qt signal to a slot/callback so UI events trigger Python code. (scope: class ApplicationWindow → def createThread) |
| 443 | `        worker.finished.connect(worker.deleteLater)` | Connects a Qt signal to a slot/callback so UI events trigger Python code. (scope: class ApplicationWindow → def createThread) |
| 444 | `        thread.finished.connect(thread.deleteLater)` | Connects a Qt signal to a slot/callback so UI events trigger Python code. (scope: class ApplicationWindow → def createThread) |
| 445 | `        return thread` | Returns value(s) from the current function: `thread`. (scope: class ApplicationWindow → def createThread) |
| 446 | `    ` | Blank line used to separate logical sections and improve readability. (scope: class ApplicationWindow) |
| 447 | `    def updateData(self):` | Defines function/method `updateData` (entry point for reusable logic). (scope: class ApplicationWindow → def updateData) |
| 448 | `        # self.log('data updated')` | Comment explaining intent/context: self.log('data updated') (scope: class ApplicationWindow → def updateData) |
| 449 | `        if not self.framework_2_loaded and len(api_df)>0:` | Starts an `if` block: conditional control flow based on `not self.framework_2_loaded and len(api_df)>0`. (scope: class ApplicationWindow → def updateData) |
| 450 | `            self._app.load_plot_framework_2()` | Executes Python statement: `self._app.load_plot_framework_2()`. (scope: class ApplicationWindow → def updateData) |
| 451 | `            self.framework_2_loaded = True` | Assigns/updates `self.framework_2_loaded` with the expression on the right-hand side. (scope: class ApplicationWindow → def updateData) |
| 452 | `        else:` | Starts an `else` branch for the preceding conditional. (scope: class ApplicationWindow → def updateData) |
| 453 | `            self._app._update_xydata(force=True)` | Executes Python statement: `self._app._update_xydata(force=True)`. (scope: class ApplicationWindow → def updateData) |
| 454 | `    ` | Blank line used to separate logical sections and improve readability. (scope: class ApplicationWindow) |
| 455 | `    def startThreads(self, hours, start_time, new):` | Defines function/method `startThreads` (entry point for reusable logic). (scope: class ApplicationWindow → def startThreads) |
| 456 | `        self.threads.clear()` | Executes Python statement: `self.threads.clear()`. (scope: class ApplicationWindow → def startThreads) |
| 457 | `        self.threads = [self.createThread(self._app.session_id, hours, start_time, new)]` | Assigns/updates `self.threads` with the expression on the right-hand side. (scope: class ApplicationWindow → def startThreads) |
| 458 | `        for thread in self.threads:` | Starts a `for` loop: iterates as described by `thread in self.threads`. (scope: class ApplicationWindow → def startThreads) |
| 459 | `            print('------<<<<  started thread <<<')` | Executes Python statement: `print('------<<<<  started thread <<<')`. (scope: class ApplicationWindow → def startThreads) |
| 460 | `            thread.start()` | Executes Python statement: `thread.start()`. (scope: class ApplicationWindow → def startThreads) |
| 461 | `` | Blank line used to separate logical sections and improve readability. |
| 462 | `    def addTimeSeries(self):` | Defines function/method `addTimeSeries` (entry point for reusable logic). (scope: def addTimeSeries) |
| 463 | `        try:` | Starts a `try` block to catch and handle exceptions. (scope: def addTimeSeries) |
| 464 | `            self._app._dataSourceManager.createTimeSeriesSource()` | Executes Python statement: `self._app._dataSourceManager.createTimeSeriesSource()`. (scope: def addTimeSeries) |
| 465 | `            self.updateTreeView()` | Executes Python statement: `self.updateTreeView()`. (scope: def addTimeSeries) |
| 466 | `        except Exception as e:` | Starts an `except` handler: `except Exception as e:`. (scope: def addTimeSeries) |
| 467 | `            self.log(msg='error adding time series.', error=e, level='Error')` | Executes Python statement: `self.log(msg='error adding time series.', error=e, level='Error')`. (scope: def addTimeSeries) |
| 468 | `            ` | Blank line used to separate logical sections and improve readability. (scope: def addTimeSeries) |
| 469 | `    def showCsvData(self, df_in):` | Defines function/method `showCsvData` (entry point for reusable logic). (scope: def showCsvData) |
| 470 | `        assert df_in is not None` | Executes Python statement: `assert df_in is not None`. (scope: def showCsvData) |
| 471 | `        view = QTableView()` | Assigns/updates `view` with the expression on the right-hand side. (scope: def showCsvData) |
| 472 | `        # view.resize(800, 500)` | Comment explaining intent/context: view.resize(800, 500) (scope: def showCsvData) |
| 473 | `        view.horizontalHeader().setStretchLastSection(True)` | Executes Python statement: `view.horizontalHeader().setStretchLastSection(True)`. (scope: def showCsvData) |
| 474 | `        view.setAlternatingRowColors(True)` | Executes Python statement: `view.setAlternatingRowColors(True)`. (scope: def showCsvData) |
| 475 | `        view.setSelectionBehavior(QTableView.SelectRows)` | Executes Python statement: `view.setSelectionBehavior(QTableView.SelectRows)`. (scope: def showCsvData) |
| 476 | `        df_head = df_in.head()` | Assigns/updates `df_head` with the expression on the right-hand side. (scope: def showCsvData) |
| 477 | `        model = PandasModel(df_head)` | Assigns/updates `model` with the expression on the right-hand side. (scope: def showCsvData) |
| 478 | `        view.setModel(model)` | Executes Python statement: `view.setModel(model)`. (scope: def showCsvData) |
| 479 | `        layout = Qt.QVBoxLayout()` | Assigns/updates `layout` with the expression on the right-hand side. (scope: def showCsvData) |
| 480 | `        self.tab.setLayout(layout)` | Executes Python statement: `self.tab.setLayout(layout)`. (scope: def showCsvData) |
| 481 | `        df_count = len(df_in.index)` | Assigns/updates `df_count` with the expression on the right-hand side. (scope: def showCsvData) |
| 482 | `        head_count = len(df_head.index)` | Assigns/updates `head_count` with the expression on the right-hand side. (scope: def showCsvData) |
| 483 | `        line_ht = 20` | Assigns/updates `line_ht` with the expression on the right-hand side. (scope: def showCsvData) |
| 484 | `        nTableHeight = min(df_count, head_count) * line_ht + 100` | Assigns/updates `nTableHeight` with the expression on the right-hand side. (scope: def showCsvData) |
| 485 | `        view.setMinimumHeight(nTableHeight)` | Executes Python statement: `view.setMinimumHeight(nTableHeight)`. (scope: def showCsvData) |
| 486 | `        view.setMaximumHeight(nTableHeight)` | Executes Python statement: `view.setMaximumHeight(nTableHeight)`. (scope: def showCsvData) |
| 487 | `        layout.addWidget(view)` | Executes Python statement: `layout.addWidget(view)`. (scope: def showCsvData) |
| 488 | `        verticalSpacer = QSpacerItem(120, 140, QSizePolicy.Minimum, QSizePolicy.Expanding)` | Assigns/updates `verticalSpacer` with the expression on the right-hand side. (scope: def showCsvData) |
| 489 | `        if head_count < df_count: #(as in most of the cases)` | Executes Python statement: `if head_count < df_count: #(as in most of the cases)`. (scope: def showCsvData) |
| 490 | `            layout.addWidget(QLabel(f"Showing {head_count} out of {df_count} rows"))` | Executes statement related to: PyQt5/Qt GUI framework usage: `layout.addWidget(QLabel(f"Showing {head_count} out of {df_count} rows"))`. (scope: def showCsvData) |
| 491 | `        layout.addItem(verticalSpacer)` | Executes Python statement: `layout.addItem(verticalSpacer)`. (scope: def showCsvData) |
| 492 | `    ` | Blank line used to separate logical sections and improve readability. |
| 493 | `    def setTreeView(self):` | Defines function/method `setTreeView` (entry point for reusable logic). (scope: def setTreeView) |
| 494 | `        self.dataView = self.treeView` | Assigns/updates `self.dataView` with the expression on the right-hand side. (scope: def setTreeView) |
| 495 | `        self.dataView.setRootIsDecorated(False)` | Executes Python statement: `self.dataView.setRootIsDecorated(False)`. (scope: def setTreeView) |
| 496 | `        self.dataView.setAlternatingRowColors(True)` | Executes Python statement: `self.dataView.setAlternatingRowColors(True)`. (scope: def setTreeView) |
| 497 | `        self.model = self.createMailModel(self)` | Assigns/updates `self.model` with the expression on the right-hand side. (scope: def setTreeView) |
| 498 | `        self.dataView.setModel(self.model)` | Executes Python statement: `self.dataView.setModel(self.model)`. (scope: def setTreeView) |
| 499 | `    ` | Blank line used to separate logical sections and improve readability. |
| 500 | `    def updateTreeView(self):` | Defines function/method `updateTreeView` (entry point for reusable logic). (scope: def updateTreeView) |
| 501 | `        sources = self._app._dataSourceManager._world_list + self._app._dataSourceManager._timeseries_list` | Assigns/updates `sources` with the expression on the right-hand side. (scope: def updateTreeView) |
| 502 | `        for source in sources:` | Starts a `for` loop: iterates as described by `source in sources`. (scope: def updateTreeView) |
| 503 | `            if source not in self._app._dataSourceManager._added_sources:` | Starts an `if` block: conditional control flow based on `source not in self._app._dataSourceManager._added_sources`. (scope: def updateTreeView) |
| 504 | `                self.addSource(self.model, source_name=source.name, source_type=source.type)` | Executes Python statement: `self.addSource(self.model, source_name=source.name, source_type=source.type)`. (scope: def updateTreeView) |
| 505 | `                self._app._dataSourceManager._added_sources.append(source)` | Executes Python statement: `self._app._dataSourceManager._added_sources.append(source)`. (scope: def updateTreeView) |
| 506 | `        ` | Blank line used to separate logical sections and improve readability. (scope: def updateTreeView) |
| 507 | `    def createMailModel(self, parent):` | Defines function/method `createMailModel` (entry point for reusable logic). (scope: def createMailModel) |
| 508 | `        model = QStandardItemModel(0, 1, parent) # 3, parent)` | Assigns/updates `model` with the expression on the right-hand side. (scope: def createMailModel) |
| 509 | `        model.setHeaderData(self.SOURCE, QtCore.Qt.Horizontal, "Sources")` | Executes Python statement: `model.setHeaderData(self.SOURCE, QtCore.Qt.Horizontal, "Sources")`. (scope: def createMailModel) |
| 510 | `        # model.setHeaderData(self.TYPE, QtCore.Qt.Horizontal, "Type")` | Comment explaining intent/context: model.setHeaderData(self.TYPE, QtCore.Qt.Horizontal, "Type") (scope: def createMailModel) |
| 511 | `        # model.setHeaderData(self.ACTIVE, QtCore.Qt.Horizontal, "Active")` | Comment explaining intent/context: model.setHeaderData(self.ACTIVE, QtCore.Qt.Horizontal, "Active") (scope: def createMailModel) |
| 512 | `        return model` | Returns value(s) from the current function: `model`. (scope: def createMailModel) |
| 513 | `    ` | Blank line used to separate logical sections and improve readability. |
| 514 | `    def addSource(self, model, source_name, source_type):` | Defines function/method `addSource` (entry point for reusable logic). (scope: def addSource) |
| 515 | `        model.insertRow(0)` | Executes Python statement: `model.insertRow(0)`. (scope: def addSource) |
| 516 | `        model.setData(model.index(0, self.SOURCE), source_name)` | Executes Python statement: `model.setData(model.index(0, self.SOURCE), source_name)`. (scope: def addSource) |
| 517 | `        # todo - update prop view based on other information` | Comment explaining intent/context: todo - update prop view based on other information (scope: def addSource) |
| 518 | `        # model.setData(model.index(0, self.TYPE), source_type)` | Comment explaining intent/context: model.setData(model.index(0, self.TYPE), source_type) (scope: def addSource) |
| 519 | `        # model.setData(model.index(0, self.ACTIVE), source_active)` | Comment explaining intent/context: model.setData(model.index(0, self.ACTIVE), source_active) (scope: def addSource) |
| 520 | `    ` | Blank line used to separate logical sections and improve readability. |
| 521 | `    def update_visualisation(self):` | Defines function/method `update_visualisation` (entry point for reusable logic). (scope: def update_visualisation) |
| 522 | `        # points = vtk.vtkPoints()` | Comment explaining intent/context: points = vtk.vtkPoints() (scope: def update_visualisation) |
| 523 | `        df_inp = self._app._dataSourceManager._world_list[-1].dataFrame` | Assigns/updates `df_inp` with the expression on the right-hand side. (scope: def update_visualisation) |
| 524 | `        cols = df_inp.columns.tolist()` | Assigns/updates `cols` with the expression on the right-hand side. (scope: def update_visualisation) |
| 525 | `        print('df cols',cols)` | Executes Python statement: `print('df cols',cols)`. (scope: def update_visualisation) |
| 526 | `        df_inp['Altitude'] = df_inp['Altitude'].interpolate(method='linear')` | Assigns/updates `df_inp['Altitude']` with the expression on the right-hand side. (scope: def update_visualisation) |
| 527 | `        skip_factor = 10` | Assigns/updates `skip_factor` with the expression on the right-hand side. (scope: def update_visualisation) |
| 528 | `` | Blank line used to separate logical sections and improve readability. |
| 529 | `        df_inp = df_inp.iloc[::skip_factor].reset_index()` | Assigns/updates `df_inp` with the expression on the right-hand side. |
| 530 | `        print('df count', df_inp.count())` | Executes Python statement: `print('df count', df_inp.count())`. |
| 531 | `` | Blank line used to separate logical sections and improve readability. |
| 532 | `        x, y, z, mag = df_inp['Longitude'].to_numpy(), df_inp['Latitude'].to_numpy(), df_inp['Altitude'].to_numpy(), df_inp['Mag'].to_numpy()` | Assigns/updates `x, y, z, mag` with the expression on the right-hand side. |
| 533 | `        num_pts = 100` | Assigns/updates `num_pts` with the expression on the right-hand side. |
| 534 | `        # indices = np.arange(0, num_pts, dtype=float) + 0.5` | Comment explaining intent/context: indices = np.arange(0, num_pts, dtype=float) + 0.5 |
| 535 | `        # phi = np.arccos(1 - 2*indices/num_pts)` | Comment explaining intent/context: phi = np.arccos(1 - 2*indices/num_pts) |
| 536 | `        # theta = np.pi * (1 + 5**0.5) * indices` | Comment explaining intent/context: theta = np.pi * (1 + 5**0.5) * indices |
| 537 | `        ` | Blank line used to separate logical sections and improve readability. |
| 538 | `        # x, y, z = np.cos(theta) * np.sin(phi), np.sin(theta) * np.sin(phi), np.cos(phi);` | Comment explaining intent/context: x, y, z = np.cos(theta) * np.sin(phi), np.sin(theta) * np.sin(phi), np.cos(phi); |
| 539 | `        minx, miny, minz = min(x), min(y), min(z)` | Assigns/updates `minx, miny, minz` with the expression on the right-hand side. |
| 540 | `        x = (x-minx)*1000` | Assigns/updates `x` with the expression on the right-hand side. |
| 541 | `        y = (y-miny)*1000` | Assigns/updates `y` with the expression on the right-hand side. |
| 542 | `        z = z-minz` | Assigns/updates `z` with the expression on the right-hand side. |
| 543 | `        points = np.stack((x, y, z, mag), axis=1)` | Assigns/updates `points` with the expression on the right-hand side. |
| 544 | `        # just keep the supported points having non NaN x, y, z` | Comment explaining intent/context: just keep the supported points having non NaN x, y, z |
| 545 | `        points = [(float(x_), float(y_), float(z_), float(mag_)) for x_, y_, z_, mag_ in points if np.True_ not in [np.isnan(h) for h in [x_, y_, z_]]]` | Assigns/updates `points` with the expression on the right-hand side. |
| 546 | `        ` | Blank line used to separate logical sections and improve readability. |
| 547 | `` | Blank line used to separate logical sections and improve readability. |
| 548 | `        # points = np.random.array(size=(len(x), 3))` | Comment explaining intent/context: points = np.random.array(size=(len(x), 3)) |
| 549 | `        # for i in range(len(x)):` | Comment explaining intent/context: for i in range(len(x)): |
| 550 | `        #     points[i, 0] = x[i]` | Comment explaining intent/context: points[i, 0] = x[i] |
| 551 | `        # # z = np.zeros_like(x)` | Comment explaining intent/context: # z = np.zeros_like(x) |
| 552 | `        # # print('xyz', x, y, z)` | Comment explaining intent/context: # print('xyz', x, y, z) |
| 553 | `        # # print(df_inp.columns)` | Comment explaining intent/context: # print(df_inp.columns) |
| 554 | `        # # print('alt', df_inp['Altitude'])` | Comment explaining intent/context: # print('alt', df_inp['Altitude']) |
| 555 | `        # scalars = vtk.vtkFloatArray()` | Comment explaining intent/context: scalars = vtk.vtkFloatArray() |
| 556 | `        # scalars.SetName("Mag")` | Comment explaining intent/context: scalars.SetName("Mag") |
| 557 | `        # for i in range(len(x)):` | Comment explaining intent/context: for i in range(len(x)): |
| 558 | `        #     # array_point = np.array([x[i]-minx, y[i]-miny, z[i]-minz] )` | Comment explaining intent/context: # array_point = np.array([x[i]-minx, y[i]-miny, z[i]-minz] ) |
| 559 | `        #     points.InsertNextPoint((x[i]-minx)*10000, (y[i]-miny)*10000, z[i]-minz)` | Comment explaining intent/context: points.InsertNextPoint((x[i]-minx)*10000, (y[i]-miny)*10000, z[i]-minz) |
| 560 | `        #     # points.InsertNextPoint(x[i], y[i], z[i])` | Comment explaining intent/context: # points.InsertNextPoint(x[i], y[i], z[i]) |
| 561 | `        #     scalars.InsertNextValue(mag[i])` | Comment explaining intent/context: scalars.InsertNextValue(mag[i]) |
| 562 | `        #` | Comment line used as a visual separator. |
| 563 | `        # print('points generated', points)` | Comment explaining intent/context: print('points generated', points) |
| 564 | `        # poly = vtk.vtkPolyData()` | Comment explaining intent/context: poly = vtk.vtkPolyData() |
| 565 | `        # poly.SetPoints(points)` | Comment explaining intent/context: poly.SetPoints(points) |
| 566 | `        # poly.GetPointData().SetScalars(scalars)` | Comment explaining intent/context: poly.GetPointData().SetScalars(scalars) |
| 567 | `        ` | Blank line used to separate logical sections and improve readability. |
| 568 | `        # # To create surface of a sphere we need to use Delaunay triangulation` | Comment explaining intent/context: # To create surface of a sphere we need to use Delaunay triangulation |
| 569 | `        # d2D = vtk.vtkDelaunay2D()` | Comment explaining intent/context: d2D = vtk.vtkDelaunay2D() |
| 570 | `        # d2D.SetInputData( poly ) # This generates a 3D mesh` | Comment explaining intent/context: d2D.SetInputData( poly ) # This generates a 3D mesh |
| 571 | `        #` | Comment line used as a visual separator. |
| 572 | `        # # We need to extract the surface from the 3D mesh` | Comment explaining intent/context: # We need to extract the surface from the 3D mesh |
| 573 | `        # dss = vtk.vtkDataSetSurfaceFilter()` | Comment explaining intent/context: dss = vtk.vtkDataSetSurfaceFilter() |
| 574 | `        # dss.SetInputConnection( d2D.GetOutputPort() )` | Comment explaining intent/context: dss.SetInputConnection( d2D.GetOutputPort() ) |
| 575 | `        # print('input set')` | Comment explaining intent/context: print('input set') |
| 576 | `        # dss.Update()` | Comment explaining intent/context: dss.Update() |
| 577 | `        ` | Blank line used to separate logical sections and improve readability. |
| 578 | `        # Now we have our final polydata` | Comment explaining intent/context: Now we have our final polydata |
| 579 | `        # The running time of Delaunay triangulation in VTK is \\(O(n\\log n)\\).` | Comment explaining intent/context: The running time of Delaunay triangulation in VTK is \(O(n\log n)\). |
| 580 | `        # This is because the triangulation is computed for each set and then the sets are merged along the splitting line` | Comment explaining intent/context: This is because the triangulation is computed for each set and then the sets are merged along the splitting line |
| 581 | `        ` | Blank line used to separate logical sections and improve readability. |
| 582 | `        # spherePoly = dss.GetOutput()` | Comment explaining intent/context: spherePoly = dss.GetOutput() |
| 583 | `        # print('output set')` | Comment explaining intent/context: print('output set') |
| 584 | `        ` | Blank line used to separate logical sections and improve readability. |
| 585 | `        # glyphFilter = vtk.vtkVertexGlyphFilter()` | Comment explaining intent/context: glyphFilter = vtk.vtkVertexGlyphFilter() |
| 586 | `        # glyphFilter.SetInputData(poly)` | Comment explaining intent/context: glyphFilter.SetInputData(poly) |
| 587 | `        # glyphFilter.Update()` | Comment explaining intent/context: glyphFilter.Update() |
| 588 | `        ` | Blank line used to separate logical sections and improve readability. |
| 589 | `        # points2 = 10 * np.random.normal(size=(np.random.randint(100), 3), scale=0.2)` | Comment explaining intent/context: points2 = 10 * np.random.normal(size=(np.random.randint(100), 3), scale=0.2) |
| 590 | `        # self.current_points_to_plot = points2` | Comment explaining intent/context: self.current_points_to_plot = points2 |
| 591 | `        self.current_points_to_plot = points #[:10]/100` | Assigns/updates `self.current_points_to_plot` with the expression on the right-hand side. |
| 592 | `` | Blank line used to separate logical sections and improve readability. |
| 593 | `        self.clear_point_actors()` | Executes Python statement: `self.clear_point_actors()`. |
| 594 | `` | Blank line used to separate logical sections and improve readability. |
| 595 | `        # Finally render the canvas with current points` | Comment explaining intent/context: Finally render the canvas with current points |
| 596 | `        self.set_points(self.current_points_to_plot)` | Executes Python statement: `self.set_points(self.current_points_to_plot)`. |
| 597 | `        ` | Blank line used to separate logical sections and improve readability. |
| 598 | `        # self.current_point_actors = self.build_scene(poly)` | Comment explaining intent/context: self.current_point_actors = self.build_scene(poly) |
| 599 | `        # self.ren.AddActor(self.current_point_actors)` | Comment explaining intent/context: self.ren.AddActor(self.current_point_actors) |
| 600 | `        self._app.iren.Render()` | Executes Python statement: `self._app.iren.Render()`. |
