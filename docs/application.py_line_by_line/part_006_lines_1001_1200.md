# Line-by-line documentation: `src/application.py`

- **File**: `src/application.py`
- **Lines covered in this document**: 1001–1200 (of 1983)

## Structure overview (file-level)

- Line 72: function `load_vtk_file()`
- Line 81: function `getFilePath()`
- Line 94: class `SessionDataManager`
- Line 98: function `update_api_df()`
- Line 124: class `Color`
- Line 126: function `__init__()`
- Line 134: class `DraggableLabel`
- Line 135: method `mousePressEvent()`
- Line 139: function `mouseMoveEvent()`
- Line 156: class `DropLabel`
- Line 157: method `__init__()`
- Line 161: function `dragEnterEvent()`
- Line 165: function `dropEvent()`
- Line 172: class `MapWidget`
- Line 173: method `__init__()`
- Line 177: function `initUI()`
- Line 206: class `PandasModel`
- Line 209: function `__init__()`
- Line 213: function `rowCount()`
- Line 223: function `columnCount()`
- Line 232: function `data()`
- Line 245: function `headerData()`
- Line 262: class `MagTimeSeriesWidget`
- Line 263: method `__init__()`
- Line 269: method `timerRefreshEvent()`
- Line 283: method `initWidget()`
- Line 350: function `on_threshold_changed()`
- Line 377: function `on_train_window_changed()`
- Line 390: function `update_train_window_status()`
- Line 403: class `ApplicationWindow`
- Line 406: method `__init__()`
- Line 432: method `connectSlots()`
- Line 436: method `createThread()`
- Line 447: method `updateData()`
- Line 455: method `startThreads()`
- Line 462: function `addTimeSeries()`
- Line 469: function `showCsvData()`
- Line 493: function `setTreeView()`
- Line 500: function `updateTreeView()`
- Line 507: function `createMailModel()`
- Line 514: function `addSource()`
- Line 521: function `update_visualisation()`
- Line 604: function `clear_point_actors()`
- Line 612: function `set_points()`
- Line 673: function `sphere_prop_to_vtkarray()`
- Line 691: function `build_scene()`
- Line 728: function `setup_point_picker_framework()`
- Line 746: function `on_pick_left()`
- Line 771: function `on_pick_right()`
- Line 777: function `loadFile()`
- Line 803: function `log()`
- Line 824: class `Source`
- Line 825: method `__init__()`
- Line 830: class `TimeSeriesSource`
- Line 831: method `__init__()`
- Line 837: class `World`
- Line 838: method `__init__()`
- Line 845: method `loadCsv()`
- Line 860: class `DataSourceManager`
- Line 861: method `__init__()`
- Line 877: method `loadCsv()`
- Line 883: method `updateUI()`
- Line 887: method `createTimeSeriesSource()`
- Line 892: class `Application`
- Line 893: method `__init__()`
- Line 981: function `showAppMaximized()`
- Line 985: function `log()`
- Line 988: function `_save_data()`
- Line 1039: function `load_plot_framework()`
- Line 1119: function `load_plot_framework_2()`
- Line 1178: function `on_add_static()`
- Line 1196: function `on_add_dynamic()`
- Line 1222: function `start_prediction_process()`
- Line 1261: function `_update_xydata()`
- Line 1290: function `_update_predictions_data()`
- Line 1387: function `_sync_threshold_spinbox()`
- Line 1401: function `_detect_anomalies()`
- Line 1575: function `_update_canvas()`
- Line 1589: function `on_add_new()`
- Line 1626: function `on_add_pred()`
- Line 1745: function `appWin()`
- Line 1748: function `initViews()`
- Line 1754: function `load_visualization_framework()`
- Line 1858: function `load_axes()`
- Line 1881: function `load_sphere_source()`
- Line 1898: function `load_world_grid()`
- Line 1922: function `load_map()`
- Line 1957: function `get_map_xy_from_latlon()`
- Line 1969: function `get_map_lat_lon_from_xy()`

## Line-by-line explanation

| Line | Code | Explanation |
|---:|---|---|
| 1001 | `                for anomaly_time in self.anomaly_times:` | Starts a `for` loop: iterates as described by `anomaly_time in self.anomaly_times`. |
| 1002 | `                    if isinstance(anomaly_time, (datetime, pd.Timestamp)):` | Starts an `if` block: conditional control flow based on `isinstance(anomaly_time, (datetime, pd.Timestamp))`. |
| 1003 | `                        anomaly_times_set.add(anomaly_time)` | Executes Python statement: `anomaly_times_set.add(anomaly_time)`. |
| 1004 | `                    else:` | Starts an `else` branch for the preceding conditional. |
| 1005 | `                        anomaly_times_set.add(pd.to_datetime(anomaly_time))` | Executes Python statement: `anomaly_times_set.add(pd.to_datetime(anomaly_time))`. |
| 1006 | `            ` | Blank line used to separate logical sections and improve readability. |
| 1007 | `            # Filter out data points that match anomaly timestamps` | Comment explaining intent/context: Filter out data points that match anomaly timestamps |
| 1008 | `            filtered_x_t = []` | Assigns/updates `filtered_x_t` with the expression on the right-hand side. |
| 1009 | `            filtered_y_t = []` | Assigns/updates `filtered_y_t` with the expression on the right-hand side. |
| 1010 | `            excluded_count = 0` | Assigns/updates `excluded_count` with the expression on the right-hand side. |
| 1011 | `            ` | Blank line used to separate logical sections and improve readability. |
| 1012 | `            for time_val, mag_val in zip(x_t, y_t):` | Starts a `for` loop: iterates as described by `time_val, mag_val in zip(x_t, y_t)`. |
| 1013 | `                # Convert time to datetime for comparison` | Comment explaining intent/context: Convert time to datetime for comparison |
| 1014 | `                time_dt = pd.to_datetime(time_val) if not isinstance(time_val, (datetime, pd.Timestamp)) else time_val` | Assigns/updates `time_dt` with the expression on the right-hand side. |
| 1015 | `                ` | Blank line used to separate logical sections and improve readability. |
| 1016 | `                # Check if this timestamp is an anomaly` | Comment explaining intent/context: Check if this timestamp is an anomaly |
| 1017 | `                if time_dt not in anomaly_times_set:` | Starts an `if` block: conditional control flow based on `time_dt not in anomaly_times_set`. |
| 1018 | `                    filtered_x_t.append(time_val)` | Executes Python statement: `filtered_x_t.append(time_val)`. |
| 1019 | `                    filtered_y_t.append(mag_val)` | Executes Python statement: `filtered_y_t.append(mag_val)`. |
| 1020 | `                else:` | Starts an `else` branch for the preceding conditional. |
| 1021 | `                    excluded_count += 1` | Updates `excluded_count` using augmented assignment `+=` (reads current value, applies operation, stores back). |
| 1022 | `            ` | Blank line used to separate logical sections and improve readability. |
| 1023 | `            # Create DataFrame with filtered data (anomalies excluded)` | Comment explaining intent/context: Create DataFrame with filtered data (anomalies excluded) |
| 1024 | `            df_save_inp = pd.DataFrame({'x': filtered_x_t, 'y': filtered_y_t})` | Assigns/updates `df_save_inp` with the expression on the right-hand side. |
| 1025 | `            inp_file = os.path.join(folder, filename)` | Assigns/updates `inp_file` with the expression on the right-hand side. |
| 1026 | `            df_save_inp.to_csv(inp_file, index=False)` | Executes Python statement: `df_save_inp.to_csv(inp_file, index=False)`. |
| 1027 | `            ` | Blank line used to separate logical sections and improve readability. |
| 1028 | `            if excluded_count > 0:` | Starts an `if` block: conditional control flow based on `excluded_count > 0`. |
| 1029 | `                self.log(f'Saved prediction input file: {inp_file} with {len(df_save_inp)} data points ({excluded_count} anomalies excluded from training)', level='Info')` | Executes Python statement: `self.log(f'Saved prediction input file: {inp_file} with {len(df_save_inp)} data points ({excluded_count} anomalies excluded from training)', level='Info')`. |
| 1030 | `            else:` | Starts an `else` branch for the preceding conditional. |
| 1031 | `                self.log(f'Saved prediction input file: {inp_file} with {len(df_save_inp)} data points', level='Info')` | Executes Python statement: `self.log(f'Saved prediction input file: {inp_file} with {len(df_save_inp)} data points', level='Info')`. |
| 1032 | `            ` | Blank line used to separate logical sections and improve readability. |
| 1033 | `            if not self.predict_app_started:` | Starts an `if` block: conditional control flow based on `not self.predict_app_started`. |
| 1034 | `                self.start_prediction_process(inp_file)` | Executes Python statement: `self.start_prediction_process(inp_file)`. |
| 1035 | `                self.predict_app_started = True` | Assigns/updates `self.predict_app_started` with the expression on the right-hand side. |
| 1036 | `            else:` | Starts an `else` branch for the preceding conditional. |
| 1037 | `                self.log('Prediction process already started, skipping new start', level='Debug')` | Executes Python statement: `self.log('Prediction process already started, skipping new start', level='Debug')`. |
| 1038 | `` | Blank line used to separate logical sections and improve readability. |
| 1039 | `    def load_plot_framework(self):` | Defines function/method `load_plot_framework` (entry point for reusable logic). (scope: def load_plot_framework) |
| 1040 | `        window = self.appWin` | Assigns/updates `window` with the expression on the right-hand side. (scope: def load_plot_framework) |
| 1041 | `        layout = Qt.QVBoxLayout()` | Assigns/updates `layout` with the expression on the right-hand side. (scope: def load_plot_framework) |
| 1042 | `        window.tab_5.setLayout(layout)` | Executes Python statement: `window.tab_5.setLayout(layout)`. (scope: def load_plot_framework) |
| 1043 | `        # Ensure tab widget has proper size policy` | Comment explaining intent/context: Ensure tab widget has proper size policy (scope: def load_plot_framework) |
| 1044 | `        window.tab_5.setSizePolicy(QSizePolicy.Expanding, QSizePolicy.Expanding)` | Executes Python statement: `window.tab_5.setSizePolicy(QSizePolicy.Expanding, QSizePolicy.Expanding)`. (scope: def load_plot_framework) |
| 1045 | `` | Blank line used to separate logical sections and improve readability. |
| 1046 | `        static_canvas = FigureCanvas(Figure(figsize=(5, 4)))` | Assigns/updates `static_canvas` with the expression on the right-hand side. Hint: Matplotlib plotting (Qt backend) usage. |
| 1047 | `        # Configure canvas to fit available space without scrolling` | Comment explaining intent/context: Configure canvas to fit available space without scrolling |
| 1048 | `        static_canvas.setSizePolicy(QSizePolicy.Expanding, QSizePolicy.Expanding)` | Executes Python statement: `static_canvas.setSizePolicy(QSizePolicy.Expanding, QSizePolicy.Expanding)`. |
| 1049 | `        static_canvas.setMinimumHeight(300)  # Reasonable minimum` | Executes Python statement: `static_canvas.setMinimumHeight(300)  # Reasonable minimum`. |
| 1050 | `        # Ideally one would use self.addToolBar here, but it is slightly` | Comment explaining intent/context: Ideally one would use self.addToolBar here, but it is slightly |
| 1051 | `        # incompatible between PyQt6 and other bindings, so we just add the` | Comment explaining intent/context: incompatible between PyQt6 and other bindings, so we just add the |
| 1052 | `        # toolbar as a plain widget instead.` | Comment explaining intent/context: toolbar as a plain widget instead. |
| 1053 | `        ` | Blank line used to separate logical sections and improve readability. |
| 1054 | `        layout.addWidget(NavigationToolbar(static_canvas, window))` | Executes statement related to: Matplotlib plotting (Qt backend) usage: `layout.addWidget(NavigationToolbar(static_canvas, window))`. |
| 1055 | `        layout.addWidget(static_canvas)` | Executes Python statement: `layout.addWidget(static_canvas)`. |
| 1056 | `        # verticalSpacer = QSpacerItem(20, 40, QSizePolicy.Minimum, QSizePolicy.Expanding)` | Comment explaining intent/context: verticalSpacer = QSpacerItem(20, 40, QSizePolicy.Minimum, QSizePolicy.Expanding) |
| 1057 | `        # layout.addItem(verticalSpacer)` | Comment explaining intent/context: layout.addItem(verticalSpacer) |
| 1058 | `` | Blank line used to separate logical sections and improve readability. |
| 1059 | `` | Blank line used to separate logical sections and improve readability. |
| 1060 | `        self._static_ax = static_canvas.figure.subplots()` | Assigns/updates `self._static_ax` with the expression on the right-hand side. |
| 1061 | `        # t = np.linspace(0, 10, 501)` | Comment explaining intent/context: t = np.linspace(0, 10, 501) |
| 1062 | `        # self._static_ax.plot(t, np.tan(t), ".")` | Comment explaining intent/context: self._static_ax.plot(t, np.tan(t), ".") |
| 1063 | `        # shapefile = r'data\\India_shapefile_with_Kashmir\\India_shape\\india_ds.shp' #does not contain crs data` | Comment explaining intent/context: shapefile = r'data\India_shapefile_with_Kashmir\India_shape\india_ds.shp' #does not contain crs data |
| 1064 | `        ` | Blank line used to separate logical sections and improve readability. |
| 1065 | `        # # Read Indian Cities population data` | Comment explaining intent/context: # Read Indian Cities population data |
| 1066 | `        # data_file_cities = os.path.join(APP_BASE, *r'data\\simplemaps_worldcities_basicv1.77\\worldcities.csv'.split('\\\\'))` | Comment explaining intent/context: data_file_cities = os.path.join(APP_BASE, *r'data\simplemaps_worldcities_basicv1.77\worldcities.csv'.split('\\')) |
| 1067 | `        # df = pd.read_csv(data_file_cities)` | Comment explaining intent/context: df = pd.read_csv(data_file_cities) |
| 1068 | `        # df = df[(df['country'] == 'India')]` | Comment explaining intent/context: df = df[(df['country'] == 'India')] |
| 1069 | `        # print(df.head())` | Comment explaining intent/context: print(df.head()) |
| 1070 | `        #` | Comment line used as a visual separator. |
| 1071 | `        # # import street map` | Comment explaining intent/context: # import street map |
| 1072 | `        # print(india_map.crs)` | Comment explaining intent/context: print(india_map.crs) |
| 1073 | `        # # designate coordinate system` | Comment explaining intent/context: # designate coordinate system |
| 1074 | `        # crs = 'EPSG:4326'` | Comment explaining intent/context: crs = 'EPSG:4326' |
| 1075 | `        #` | Comment line used as a visual separator. |
| 1076 | `        # # zip x and y coordinates into single feature` | Comment explaining intent/context: # zip x and y coordinates into single feature |
| 1077 | `        # geometry = [Point(xy) for xy in zip(df['lng'], df['lat'])]` | Comment explaining intent/context: geometry = [Point(xy) for xy in zip(df['lng'], df['lat'])] |
| 1078 | `        # # create GeoPandas dataframe` | Comment explaining intent/context: # create GeoPandas dataframe |
| 1079 | `        # geo_df = gpd.GeoDataFrame(df, crs=crs, geometry = geometry)` | Comment explaining intent/context: geo_df = gpd.GeoDataFrame(df, crs=crs, geometry = geometry) |
| 1080 | `        #` | Comment line used as a visual separator. |
| 1081 | `        # print(geo_df.head())` | Comment explaining intent/context: print(geo_df.head()) |
| 1082 | `        # print(geo_df.columns)` | Comment explaining intent/context: print(geo_df.columns) |
| 1083 | `        # print('indiamap',india_map, india_map.__class__, india_map.columns)` | Comment explaining intent/context: print('indiamap',india_map, india_map.__class__, india_map.columns) |
| 1084 | `        # print('geometry', india_map['geometry'].to_list()[0])` | Comment explaining intent/context: print('geometry', india_map['geometry'].to_list()[0]) |
| 1085 | `        # create figure and axes, assign to subplot` | Comment explaining intent/context: create figure and axes, assign to subplot |
| 1086 | `        # fig, ax = plt.subplots(figsize=(15,15))` | Comment explaining intent/context: fig, ax = plt.subplots(figsize=(15,15)) |
| 1087 | `        delta = 2.0` | Assigns/updates `delta` with the expression on the right-hand side. |
| 1088 | `        x = np.arange(65, 100.0, delta)` | Assigns/updates `x` with the expression on the right-hand side. |
| 1089 | `        y = np.arange(5, 40.0, delta)` | Assigns/updates `y` with the expression on the right-hand side. |
| 1090 | `        # x = np.arange(-3.0, 3.0, delta)` | Comment explaining intent/context: x = np.arange(-3.0, 3.0, delta) |
| 1091 | `        # y = np.arange(-2.0, 2.0, delta)` | Comment explaining intent/context: y = np.arange(-2.0, 2.0, delta) |
| 1092 | `        X, Y = np.meshgrid(x, y)` | Assigns/updates `X, Y` with the expression on the right-hand side. |
| 1093 | `        # Z1 = np.exp(-X**2 - Y**2)` | Comment explaining intent/context: Z1 = np.exp(-X**2 - Y**2) |
| 1094 | `        # Z2 = np.exp(-(X - 1)**2 - (Y - 1)**2)` | Comment explaining intent/context: Z2 = np.exp(-(X - 1)**2 - (Y - 1)**2) |
| 1095 | `        # Z = (Z1 - Z2) * 2` | Comment explaining intent/context: Z = (Z1 - Z2) * 2 |
| 1096 | `        Z = np.ones_like(X)` | Assigns/updates `Z` with the expression on the right-hand side. |
| 1097 | `        # geo_mag = GeoMag(coefficients_file=r"C:/Users/Admin/eclipse-workspace/magnavis/src/data/wmm/WMM_HighResolution_2025.COF", high_resolution=True) # obvious issue with module not loading absolute files, can be manually corrected though` | Comment explaining intent/context: geo_mag = GeoMag(coefficients_file=r"C:/Users/Admin/eclipse-workspace/magnavis/src/data/wmm/WMM_HighResolution_2025.COF", high_resolution=True) # obvious issue with module not loading absolute files, can be manually corrected though |
| 1098 | `        geo_mag = GeoMag(coefficients_file=r"wmm/WMMHR_2025.COF", high_resolution=True)` | Assigns/updates `geo_mag` with the expression on the right-hand side. Hint: World Magnetic Model (geomagnetic field) usage. |
| 1099 | `        for idx, x_ in np.ndenumerate(x):` | Starts a `for` loop: iterates as described by `idx, x_ in np.ndenumerate(x)`. |
| 1100 | `            for idy, y_ in np.ndenumerate(y):` | Starts a `for` loop: iterates as described by `idy, y_ in np.ndenumerate(y)`. |
| 1101 | `                result = geo_mag.calculate(glat=y_, glon=x_, alt=0, time=2025.25)` | Assigns/updates `result` with the expression on the right-hand side. |
| 1102 | `                Z[idx, idy] = float(result.f) # f is total intensity of magnetic field` | Assigns/updates `Z[idx, idy]` with the expression on the right-hand side. |
| 1103 | `            ` | Blank line used to separate logical sections and improve readability. |
| 1104 | `        CS = self._static_ax.contour(X, Y, Z)` | Assigns/updates `CS` with the expression on the right-hand side. |
| 1105 | `        self._static_ax.clabel(CS, fontsize=7, inline_spacing=-10)` | Executes Python statement: `self._static_ax.clabel(CS, fontsize=7, inline_spacing=-10)`. |
| 1106 | `        self._static_ax.set_title('India')` | Executes Python statement: `self._static_ax.set_title('India')`. |
| 1107 | `        ` | Blank line used to separate logical sections and improve readability. |
| 1108 | `        shapefile = os.path.join(APP_BASE, *r'data\\india_India_Country_Boundary_MAPOG\\india_India_Country_Boundary.shp'.split('\\\\')) # does contain crs data, ie EPSG:4326` | Assigns/updates `shapefile` with the expression on the right-hand side. |
| 1109 | `        india_map = gpd.read_file(shapefile)` | Assigns/updates `india_map` with the expression on the right-hand side. Hint: Geospatial libraries (GeoPandas/Shapely) usage. |
| 1110 | `        for poly in india_map['geometry'].to_list()[0].geoms:` | Starts a `for` loop: iterates as described by `poly in india_map['geometry'].to_list()[0].geoms`. |
| 1111 | `            # print('poly', poly.__class__)` | Comment explaining intent/context: print('poly', poly.__class__) |
| 1112 | `            x, y = poly.exterior.xy` | Assigns/updates `x, y` with the expression on the right-hand side. |
| 1113 | `            self._static_ax.plot(x, y, alpha=0.4,color='grey')` | Executes Python statement: `self._static_ax.plot(x, y, alpha=0.4,color='grey')`. |
| 1114 | `        ` | Blank line used to separate logical sections and improve readability. |
| 1115 | `        # add .shp mapfile to axes` | Comment explaining intent/context: add .shp mapfile to axes |
| 1116 | `        # india_map.plot(ax=ax, alpha=0.4,color='grey')` | Comment explaining intent/context: india_map.plot(ax=ax, alpha=0.4,color='grey') |
| 1117 | `        # plt.show()` | Comment explaining intent/context: plt.show() |
| 1118 | `        ` | Blank line used to separate logical sections and improve readability. |
| 1119 | `    def load_plot_framework_2(self):` | Defines function/method `load_plot_framework_2` (entry point for reusable logic). (scope: def load_plot_framework_2) |
| 1120 | `        try:` | Starts a `try` block to catch and handle exceptions. (scope: def load_plot_framework_2) |
| 1121 | `            window = self.appWin` | Assigns/updates `window` with the expression on the right-hand side. (scope: def load_plot_framework_2) |
| 1122 | `            ` | Blank line used to separate logical sections and improve readability. (scope: def load_plot_framework_2) |
| 1123 | `            # print(window.tab_2, window.tab_2.__class__)` | Comment explaining intent/context: print(window.tab_2, window.tab_2.__class__) (scope: def load_plot_framework_2) |
| 1124 | `            # Use flexible figure size that adapts to container` | Comment explaining intent/context: Use flexible figure size that adapts to container (scope: def load_plot_framework_2) |
| 1125 | `            static_canvas = FigureCanvas(Figure(figsize=(6, 3)))` | Assigns/updates `static_canvas` with the expression on the right-hand side. Hint: Matplotlib plotting (Qt backend) usage. (scope: def load_plot_framework_2) |
| 1126 | `            static_canvas.setSizePolicy(QSizePolicy.Expanding, QSizePolicy.Expanding)` | Executes Python statement: `static_canvas.setSizePolicy(QSizePolicy.Expanding, QSizePolicy.Expanding)`. (scope: def load_plot_framework_2) |
| 1127 | `            # Ideally one would use self.addToolBar here, but it is slightly` | Comment explaining intent/context: Ideally one would use self.addToolBar here, but it is slightly (scope: def load_plot_framework_2) |
| 1128 | `            # incompatible between PyQt6 and other bindings, so we just add the` | Comment explaining intent/context: incompatible between PyQt6 and other bindings, so we just add the (scope: def load_plot_framework_2) |
| 1129 | `            # toolbar as a plain widget instead.` | Comment explaining intent/context: toolbar as a plain widget instead. (scope: def load_plot_framework_2) |
| 1130 | `            ` | Blank line used to separate logical sections and improve readability. (scope: def load_plot_framework_2) |
| 1131 | `            timeSerWidget = MagTimeSeriesWidget(self, self.appWin)` | Assigns/updates `timeSerWidget` with the expression on the right-hand side. (scope: def load_plot_framework_2) |
| 1132 | `            layout_outer = Qt.QVBoxLayout()` | Assigns/updates `layout_outer` with the expression on the right-hand side. (scope: def load_plot_framework_2) |
| 1133 | `            window.tab_2.setLayout(layout_outer)` | Executes Python statement: `window.tab_2.setLayout(layout_outer)`. (scope: def load_plot_framework_2) |
| 1134 | `            # Ensure tab widget has proper size policy` | Comment explaining intent/context: Ensure tab widget has proper size policy (scope: def load_plot_framework_2) |
| 1135 | `            window.tab_2.setSizePolicy(QSizePolicy.Expanding, QSizePolicy.Expanding)` | Executes Python statement: `window.tab_2.setSizePolicy(QSizePolicy.Expanding, QSizePolicy.Expanding)`. (scope: def load_plot_framework_2) |
| 1136 | `            layout_outer.addWidget(timeSerWidget)` | Executes Python statement: `layout_outer.addWidget(timeSerWidget)`. (scope: def load_plot_framework_2) |
| 1137 | `            ` | Blank line used to separate logical sections and improve readability. (scope: def load_plot_framework_2) |
| 1138 | `            # Store reference to time series widget for access later` | Comment explaining intent/context: Store reference to time series widget for access later (scope: def load_plot_framework_2) |
| 1139 | `            self.timeSerWidget = timeSerWidget` | Assigns/updates `self.timeSerWidget` with the expression on the right-hand side. (scope: def load_plot_framework_2) |
| 1140 | `            ` | Blank line used to separate logical sections and improve readability. (scope: def load_plot_framework_2) |
| 1141 | `            # Sync threshold spinbox with anomaly detector value after widget is fully initialized` | Comment explaining intent/context: Sync threshold spinbox with anomaly detector value after widget is fully initialized (scope: def load_plot_framework_2) |
| 1142 | `            # Use QTimer to ensure widget is ready` | Comment explaining intent/context: Use QTimer to ensure widget is ready (scope: def load_plot_framework_2) |
| 1143 | `            QTimer.singleShot(100, lambda: self._sync_threshold_spinbox())` | Configures/starts a timer to periodically trigger updates without blocking the UI thread. (scope: def load_plot_framework_2) |
| 1144 | `            ` | Blank line used to separate logical sections and improve readability. (scope: def load_plot_framework_2) |
| 1145 | `            ` | Blank line used to separate logical sections and improve readability. (scope: def load_plot_framework_2) |
| 1146 | `            layout = timeSerWidget.verticalLayout_3 # ` | Assigns/updates `layout` with the expression on the right-hand side. (scope: def load_plot_framework_2) |
| 1147 | `            timeSerWidget.scrollArea.setWidgetResizable(True)` | Executes Python statement: `timeSerWidget.scrollArea.setWidgetResizable(True)`. (scope: def load_plot_framework_2) |
| 1148 | `            timeSerWidget.scrollArea.setMinimumHeight(500)` | Executes Python statement: `timeSerWidget.scrollArea.setMinimumHeight(500)`. (scope: def load_plot_framework_2) |
| 1149 | `            timeSerWidget.scrollArea.setMinimumWidth(550)` | Executes Python statement: `timeSerWidget.scrollArea.setMinimumWidth(550)`. (scope: def load_plot_framework_2) |
| 1150 | `            layout.addWidget(NavigationToolbar(static_canvas, window))` | Executes statement related to: Matplotlib plotting (Qt backend) usage: `layout.addWidget(NavigationToolbar(static_canvas, window))`. (scope: def load_plot_framework_2) |
| 1151 | `            layout.addWidget(static_canvas)` | Executes Python statement: `layout.addWidget(static_canvas)`. (scope: def load_plot_framework_2) |
| 1152 | `    ` | Blank line used to separate logical sections and improve readability. |
| 1153 | `            dynamic_canvas = FigureCanvas(Figure(figsize=(5, 3)))` | Assigns/updates `dynamic_canvas` with the expression on the right-hand side. Hint: Matplotlib plotting (Qt backend) usage. |
| 1154 | `            layout.addWidget(dynamic_canvas)` | Executes Python statement: `layout.addWidget(dynamic_canvas)`. |
| 1155 | `            layout.addWidget(NavigationToolbar(dynamic_canvas, window))` | Executes statement related to: Matplotlib plotting (Qt backend) usage: `layout.addWidget(NavigationToolbar(dynamic_canvas, window))`. |
| 1156 | `    ` | Blank line used to separate logical sections and improve readability. |
| 1157 | `            self._static_ax = static_canvas.figure.subplots()` | Assigns/updates `self._static_ax` with the expression on the right-hand side. |
| 1158 | `            # load usgs magnetic data` | Comment explaining intent/context: load usgs magnetic data |
| 1159 | `            ` | Blank line used to separate logical sections and improve readability. |
| 1160 | `            # t = np.linspace(0, 10, 501)` | Comment explaining intent/context: t = np.linspace(0, 10, 501) |
| 1161 | `            # y_t = np.tan(t)` | Comment explaining intent/context: y_t = np.tan(t) |
| 1162 | `            ` | Blank line used to separate logical sections and improve readability. |
| 1163 | `            mag_t_df = api_df #get_timeseries_magnetic_data(hours=1)` | Assigns/updates `mag_t_df` with the expression on the right-hand side. |
| 1164 | `            # print('assuming api_df is full', api_df)` | Comment explaining intent/context: print('assuming api_df is full', api_df) |
| 1165 | `            # print(mag_t_df)` | Comment explaining intent/context: print(mag_t_df) |
| 1166 | `            self.x_t = mag_t_df['time_H'].tolist()` | Assigns/updates `self.x_t` with the expression on the right-hand side. |
| 1167 | `            self.y_mag_t = mag_t_df['mag_H_nT'].tolist()` | Assigns/updates `self.y_mag_t` with the expression on the right-hand side. |
| 1168 | `            # xfmt = md.DateFormatter('%Y-%m-%d %H:%M:%S')` | Comment explaining intent/context: xfmt = md.DateFormatter('%Y-%m-%d %H:%M:%S') |
| 1169 | `            # self._static_ax.xaxis.set_major_formatter(xfmt)` | Comment explaining intent/context: self._static_ax.xaxis.set_major_formatter(xfmt) |
| 1170 | `            # static_canvas.figure.subplots_adjust(bottom=0.3)` | Comment explaining intent/context: static_canvas.figure.subplots_adjust(bottom=0.3) |
| 1171 | `            # self._static_ax.tick_params(rotation=15) # xticks(rotation=25)` | Comment explaining intent/context: self._static_ax.tick_params(rotation=15) # xticks(rotation=25) |
| 1172 | `            self._static_line, = self._static_ax.plot(self.x_t, self.y_mag_t, ".")` | Assigns/updates `self._static_line,` with the expression on the right-hand side. |
| 1173 | `            ` | Blank line used to separate logical sections and improve readability. |
| 1174 | `            # Add cursor tooltips for static plot` | Comment explaining intent/context: Add cursor tooltips for static plot |
| 1175 | `            if MPLCURSORS_AVAILABLE:` | Starts an `if` block: conditional control flow based on `MPLCURSORS_AVAILABLE`. |
| 1176 | `                cursor_static = mplcursors.cursor(self._static_line, hover=True)` | Assigns/updates `cursor_static` with the expression on the right-hand side. Hint: Interactive plot tooltips via mplcursors. |
| 1177 | `                @cursor_static.connect("add")` | Connects a Qt signal to a slot/callback so UI events trigger Python code. |
| 1178 | `                def on_add_static(sel):` | Defines function/method `on_add_static` (entry point for reusable logic). (scope: def on_add_static) |
| 1179 | `                    idx = sel.target.index` | Assigns/updates `idx` with the expression on the right-hand side. (scope: def on_add_static) |
| 1180 | `                    if idx < len(self.x_t) and idx < len(self.y_mag_t):` | Starts an `if` block: conditional control flow based on `idx < len(self.x_t) and idx < len(self.y_mag_t)`. (scope: def on_add_static) |
| 1181 | `                        time_str = pd.to_datetime(self.x_t[idx]).strftime('%Y-%m-%d %H:%M:%S') if isinstance(self.x_t[idx], (datetime, pd.Timestamp)) else str(self.x_t[idx])` | Assigns/updates `time_str` with the expression on the right-hand side. (scope: def on_add_static) |
| 1182 | `                        sel.annotation.set_text(f'Time: {time_str}\\nValue: {self.y_mag_t[idx]:.2f} nT')` | Executes Python statement: `sel.annotation.set_text(f'Time: {time_str}\nValue: {self.y_mag_t[idx]:.2f} nT')`. (scope: def on_add_static) |
| 1183 | `            ` | Blank line used to separate logical sections and improve readability. |
| 1184 | `            self._dynamic_ax = dynamic_canvas.figure.subplots()` | Assigns/updates `self._dynamic_ax` with the expression on the right-hand side. |
| 1185 | `            # Set up a Line2D.` | Comment explaining intent/context: Set up a Line2D. |
| 1186 | `            # self.xdata = np.linspace(0, 10, 101)` | Comment explaining intent/context: self.xdata = np.linspace(0, 10, 101) |
| 1187 | `            # self.xdata = x_t` | Comment explaining intent/context: self.xdata = x_t |
| 1188 | `            # self._update_xydata(force=True)` | Comment explaining intent/context: self._update_xydata(force=True) |
| 1189 | `            # self._line, = self._dynamic_ax.plot(self.xdata, self.ydata)` | Comment explaining intent/context: self._line, = self._dynamic_ax.plot(self.xdata, self.ydata) |
| 1190 | `            self._line, = self._dynamic_ax.plot(self.x_t, self.y_mag_t)` | Assigns/updates `self._line,` with the expression on the right-hand side. |
| 1191 | `            ` | Blank line used to separate logical sections and improve readability. |
| 1192 | `            # Add cursor tooltips for dynamic plot` | Comment explaining intent/context: Add cursor tooltips for dynamic plot |
| 1193 | `            if MPLCURSORS_AVAILABLE:` | Starts an `if` block: conditional control flow based on `MPLCURSORS_AVAILABLE`. |
| 1194 | `                cursor_dynamic = mplcursors.cursor(self._line, hover=True)` | Assigns/updates `cursor_dynamic` with the expression on the right-hand side. Hint: Interactive plot tooltips via mplcursors. |
| 1195 | `                @cursor_dynamic.connect("add")` | Connects a Qt signal to a slot/callback so UI events trigger Python code. |
| 1196 | `                def on_add_dynamic(sel):` | Defines function/method `on_add_dynamic` (entry point for reusable logic). (scope: def on_add_dynamic) |
| 1197 | `                    idx = sel.target.index` | Assigns/updates `idx` with the expression on the right-hand side. (scope: def on_add_dynamic) |
| 1198 | `                    if idx < len(self.x_t) and idx < len(self.y_mag_t):` | Starts an `if` block: conditional control flow based on `idx < len(self.x_t) and idx < len(self.y_mag_t)`. (scope: def on_add_dynamic) |
| 1199 | `                        time_str = pd.to_datetime(self.x_t[idx]).strftime('%Y-%m-%d %H:%M:%S') if isinstance(self.x_t[idx], (datetime, pd.Timestamp)) else str(self.x_t[idx])` | Assigns/updates `time_str` with the expression on the right-hand side. (scope: def on_add_dynamic) |
| 1200 | `                        sel.annotation.set_text(f'Time: {time_str}\\nValue: {self.y_mag_t[idx]:.2f} nT')` | Executes Python statement: `sel.annotation.set_text(f'Time: {time_str}\nValue: {self.y_mag_t[idx]:.2f} nT')`. (scope: def on_add_dynamic) |
