# Line-by-line documentation: `src/application.py`

- **File**: `src/application.py`
- **Lines covered in this document**: 1601–1800 (of 1983)

## Structure overview (file-level)

- Line 72: function `load_vtk_file()`
- Line 81: function `getFilePath()`
- Line 94: class `SessionDataManager`
- Line 98: function `update_api_df()`
- Line 124: class `Color`
- Line 126: function `__init__()`
- Line 134: class `DraggableLabel`
- Line 135: method `mousePressEvent()`
- Line 139: function `mouseMoveEvent()`
- Line 156: class `DropLabel`
- Line 157: method `__init__()`
- Line 161: function `dragEnterEvent()`
- Line 165: function `dropEvent()`
- Line 172: class `MapWidget`
- Line 173: method `__init__()`
- Line 177: function `initUI()`
- Line 206: class `PandasModel`
- Line 209: function `__init__()`
- Line 213: function `rowCount()`
- Line 223: function `columnCount()`
- Line 232: function `data()`
- Line 245: function `headerData()`
- Line 262: class `MagTimeSeriesWidget`
- Line 263: method `__init__()`
- Line 269: method `timerRefreshEvent()`
- Line 283: method `initWidget()`
- Line 350: function `on_threshold_changed()`
- Line 377: function `on_train_window_changed()`
- Line 390: function `update_train_window_status()`
- Line 403: class `ApplicationWindow`
- Line 406: method `__init__()`
- Line 432: method `connectSlots()`
- Line 436: method `createThread()`
- Line 447: method `updateData()`
- Line 455: method `startThreads()`
- Line 462: function `addTimeSeries()`
- Line 469: function `showCsvData()`
- Line 493: function `setTreeView()`
- Line 500: function `updateTreeView()`
- Line 507: function `createMailModel()`
- Line 514: function `addSource()`
- Line 521: function `update_visualisation()`
- Line 604: function `clear_point_actors()`
- Line 612: function `set_points()`
- Line 673: function `sphere_prop_to_vtkarray()`
- Line 691: function `build_scene()`
- Line 728: function `setup_point_picker_framework()`
- Line 746: function `on_pick_left()`
- Line 771: function `on_pick_right()`
- Line 777: function `loadFile()`
- Line 803: function `log()`
- Line 824: class `Source`
- Line 825: method `__init__()`
- Line 830: class `TimeSeriesSource`
- Line 831: method `__init__()`
- Line 837: class `World`
- Line 838: method `__init__()`
- Line 845: method `loadCsv()`
- Line 860: class `DataSourceManager`
- Line 861: method `__init__()`
- Line 877: method `loadCsv()`
- Line 883: method `updateUI()`
- Line 887: method `createTimeSeriesSource()`
- Line 892: class `Application`
- Line 893: method `__init__()`
- Line 981: function `showAppMaximized()`
- Line 985: function `log()`
- Line 988: function `_save_data()`
- Line 1039: function `load_plot_framework()`
- Line 1119: function `load_plot_framework_2()`
- Line 1178: function `on_add_static()`
- Line 1196: function `on_add_dynamic()`
- Line 1222: function `start_prediction_process()`
- Line 1261: function `_update_xydata()`
- Line 1290: function `_update_predictions_data()`
- Line 1387: function `_sync_threshold_spinbox()`
- Line 1401: function `_detect_anomalies()`
- Line 1575: function `_update_canvas()`
- Line 1589: function `on_add_new()`
- Line 1626: function `on_add_pred()`
- Line 1745: function `appWin()`
- Line 1748: function `initViews()`
- Line 1754: function `load_visualization_framework()`
- Line 1858: function `load_axes()`
- Line 1881: function `load_sphere_source()`
- Line 1898: function `load_world_grid()`
- Line 1922: function `load_map()`
- Line 1957: function `get_map_xy_from_latlon()`
- Line 1969: function `get_map_lat_lon_from_xy()`

## Line-by-line explanation

| Line | Code | Explanation |
|---:|---|---|
| 1601 | `                    _xrange = self.new_x_t[-1]-self.x_t[0]` | Assigns/updates `_xrange` with the expression on the right-hand side. |
| 1602 | `                    _ymax = max(max(self.new_y_mag_t), max(self.y_mag_t))` | Assigns/updates `_ymax` with the expression on the right-hand side. |
| 1603 | `                    _ymin = min(min(self.new_y_mag_t), min(self.y_mag_t))` | Assigns/updates `_ymin` with the expression on the right-hand side. |
| 1604 | `                    _yrange = _ymax - _ymin` | Assigns/updates `_yrange` with the expression on the right-hand side. |
| 1605 | `                    self._dynamic_ax.set_xlim(self.x_t[0] - 0.05*_xrange, self.new_x_t[-1] + 0.05*_xrange)` | Executes Python statement: `self._dynamic_ax.set_xlim(self.x_t[0] - 0.05*_xrange, self.new_x_t[-1] + 0.05*_xrange)`. |
| 1606 | `                    self._dynamic_ax.set_ylim(_ymin - 0.05*_yrange, _ymax + 0.05*_yrange)` | Executes Python statement: `self._dynamic_ax.set_ylim(_ymin - 0.05*_yrange, _ymax + 0.05*_yrange)`. |
| 1607 | `                    self._static_ax.set_xlim(self.x_t[0] - 0.05*_xrange, self.new_x_t[-1] + 0.05*_xrange)` | Executes Python statement: `self._static_ax.set_xlim(self.x_t[0] - 0.05*_xrange, self.new_x_t[-1] + 0.05*_xrange)`. |
| 1608 | `                    self._static_ax.set_ylim(_ymin - 0.05*_yrange, _ymax + 0.05*_yrange)` | Executes Python statement: `self._static_ax.set_ylim(_ymin - 0.05*_yrange, _ymax + 0.05*_yrange)`. |
| 1609 | `                    self.needs_update_lims = False` | Assigns/updates `self.needs_update_lims` with the expression on the right-hand side. |
| 1610 | `                # It should be safe to use the synchronous draw() method for most drawing` | Comment explaining intent/context: It should be safe to use the synchronous draw() method for most drawing |
| 1611 | `                # frequencies, but it is safer to use draw_idle().` | Comment explaining intent/context: frequencies, but it is safer to use draw_idle(). |
| 1612 | `                self._line.figure.canvas.draw_idle()` | Executes Python statement: `self._line.figure.canvas.draw_idle()`. |
| 1613 | `                self._line_new.figure.canvas.draw_idle()` | Executes Python statement: `self._line_new.figure.canvas.draw_idle()`. |
| 1614 | `                self._static_line.figure.canvas.draw_idle()` | Executes Python statement: `self._static_line.figure.canvas.draw_idle()`. |
| 1615 | `                # print('canvas updated')` | Comment explaining intent/context: print('canvas updated') |
| 1616 | `            ` | Blank line used to separate logical sections and improve readability. |
| 1617 | `            # Always (re)draw predictions from the merged series` | Comment explaining intent/context: Always (re)draw predictions from the merged series |
| 1618 | `            if self.predict_x_t and self.predict_y_t:` | Starts an `if` block: conditional control flow based on `self.predict_x_t and self.predict_y_t`. |
| 1619 | `                pred_times = self.predict_x_t[-1]` | Assigns/updates `pred_times` with the expression on the right-hand side. |
| 1620 | `                pred_values = self.predict_y_t[-1]` | Assigns/updates `pred_values` with the expression on the right-hand side. |
| 1621 | `                if not self._new_predictions_line:` | Starts an `if` block: conditional control flow based on `not self._new_predictions_line`. |
| 1622 | `                    self._new_predictions_line, = self._dynamic_ax.plot(pred_times, pred_values, color=[0.3, 0.1, 0.4])` | Assigns/updates `self._new_predictions_line,` with the expression on the right-hand side. |
| 1623 | `                    if MPLCURSORS_AVAILABLE:` | Starts an `if` block: conditional control flow based on `MPLCURSORS_AVAILABLE`. |
| 1624 | `                        cursor_pred = mplcursors.cursor(self._new_predictions_line, hover=True)` | Assigns/updates `cursor_pred` with the expression on the right-hand side. Hint: Interactive plot tooltips via mplcursors. |
| 1625 | `                        @cursor_pred.connect("add")` | Connects a Qt signal to a slot/callback so UI events trigger Python code. |
| 1626 | `                        def on_add_pred(sel):` | Defines function/method `on_add_pred` (entry point for reusable logic). (scope: def on_add_pred) |
| 1627 | `                            idx = sel.target.index` | Assigns/updates `idx` with the expression on the right-hand side. (scope: def on_add_pred) |
| 1628 | `                            if idx < len(pred_times) and idx < len(pred_values):` | Starts an `if` block: conditional control flow based on `idx < len(pred_times) and idx < len(pred_values)`. (scope: def on_add_pred) |
| 1629 | `                                time_str = pd.to_datetime(pred_times[idx]).strftime('%Y-%m-%d %H:%M:%S') if isinstance(pred_times[idx], (datetime, pd.Timestamp)) else str(pred_times[idx])` | Assigns/updates `time_str` with the expression on the right-hand side. (scope: def on_add_pred) |
| 1630 | `                                sel.annotation.set_text(f'Time: {time_str}\\nPredicted: {pred_values[idx]:.2f} nT')` | Executes Python statement: `sel.annotation.set_text(f'Time: {time_str}\nPredicted: {pred_values[idx]:.2f} nT')`. (scope: def on_add_pred) |
| 1631 | `                else:` | Starts an `else` branch for the preceding conditional. |
| 1632 | `                    self._new_predictions_line.set_data(pred_times, pred_values)` | Executes Python statement: `self._new_predictions_line.set_data(pred_times, pred_values)`. |
| 1633 | `                # Update last prediction endpoint` | Comment explaining intent/context: Update last prediction endpoint |
| 1634 | `                self.predict_last_t_in_plot = pred_times[-1]` | Assigns/updates `self.predict_last_t_in_plot` with the expression on the right-hand side. |
| 1635 | `            ` | Blank line used to separate logical sections and improve readability. |
| 1636 | `            # Plot anomalies if any are detected` | Comment explaining intent/context: Plot anomalies if any are detected |
| 1637 | `            if self.anomaly_times and self.anomaly_values and len(self.anomaly_times) > 0:` | Starts an `if` block: conditional control flow based on `self.anomaly_times and self.anomaly_values and len(self.anomaly_times) > 0`. |
| 1638 | `                self.log(f'Anomaly Visualization: Attempting to plot {len(self.anomaly_times)} anomalies', level='Info')` | Executes Python statement: `self.log(f'Anomaly Visualization: Attempting to plot {len(self.anomaly_times)} anomalies', level='Info')`. |
| 1639 | `                # Convert times to proper format if needed (handle datetime objects)` | Comment explaining intent/context: Convert times to proper format if needed (handle datetime objects) |
| 1640 | `                try:` | Starts a `try` block to catch and handle exceptions. |
| 1641 | `                    # Ensure times are in the right format for matplotlib` | Comment explaining intent/context: Ensure times are in the right format for matplotlib |
| 1642 | `                    if isinstance(self.anomaly_times[0], (datetime, pd.Timestamp)):` | Starts an `if` block: conditional control flow based on `isinstance(self.anomaly_times[0], (datetime, pd.Timestamp))`. |
| 1643 | `                        # Times are datetime objects - matplotlib can handle them directly` | Comment explaining intent/context: Times are datetime objects - matplotlib can handle them directly |
| 1644 | `                        anomaly_times_plot = self.anomaly_times` | Assigns/updates `anomaly_times_plot` with the expression on the right-hand side. |
| 1645 | `                    else:` | Starts an `else` branch for the preceding conditional. |
| 1646 | `                        # Try to convert if they're strings` | Comment explaining intent/context: Try to convert if they're strings |
| 1647 | `                        anomaly_times_plot = [pd.to_datetime(t) if isinstance(t, str) else t for t in self.anomaly_times]` | Assigns/updates `anomaly_times_plot` with the expression on the right-hand side. |
| 1648 | `                    ` | Blank line used to separate logical sections and improve readability. |
| 1649 | `                    # Debug: log first few anomaly times and values` | Comment explaining intent/context: Debug: log first few anomaly times and values |
| 1650 | `                    if len(anomaly_times_plot) > 0:` | Starts an `if` block: conditional control flow based on `len(anomaly_times_plot) > 0`. |
| 1651 | `                        self.log(f'Anomaly Visualization: First anomaly - Time: {anomaly_times_plot[0]}, Value: {self.anomaly_values[0]:.2f} nT', level='Info')` | Executes Python statement: `self.log(f'Anomaly Visualization: First anomaly - Time: {anomaly_times_plot[0]}, Value: {self.anomaly_values[0]:.2f} nT', level='Info')`. |
| 1652 | `                except Exception as e:` | Starts an `except` handler: `except Exception as e:`. |
| 1653 | `                    self.log(f'Anomaly Visualization: Error converting times: {e}', level='Error')` | Executes Python statement: `self.log(f'Anomaly Visualization: Error converting times: {e}', level='Error')`. |
| 1654 | `                    anomaly_times_plot = self.anomaly_times` | Assigns/updates `anomaly_times_plot` with the expression on the right-hand side. |
| 1655 | `                ` | Blank line used to separate logical sections and improve readability. |
| 1656 | `                # Remove old vertical lines if they exist` | Comment explaining intent/context: Remove old vertical lines if they exist |
| 1657 | `                for vline in self.anomaly_vertical_lines:` | Starts a `for` loop: iterates as described by `vline in self.anomaly_vertical_lines`. |
| 1658 | `                    try:` | Starts a `try` block to catch and handle exceptions. |
| 1659 | `                        vline.remove()` | Executes Python statement: `vline.remove()`. |
| 1660 | `                    except:` | Starts an `except` handler: `except:`. |
| 1661 | `                        pass` | No-op placeholder statement (keeps block syntactically valid). |
| 1662 | `                self.anomaly_vertical_lines = []` | Assigns/updates `self.anomaly_vertical_lines` with the expression on the right-hand side. |
| 1663 | `                ` | Blank line used to separate logical sections and improve readability. |
| 1664 | `                for vline in self.anomaly_vertical_lines_static:` | Starts a `for` loop: iterates as described by `vline in self.anomaly_vertical_lines_static`. |
| 1665 | `                    try:` | Starts a `try` block to catch and handle exceptions. |
| 1666 | `                        vline.remove()` | Executes Python statement: `vline.remove()`. |
| 1667 | `                    except:` | Starts an `except` handler: `except:`. |
| 1668 | `                        pass` | No-op placeholder statement (keeps block syntactically valid). |
| 1669 | `                self.anomaly_vertical_lines_static = []` | Assigns/updates `self.anomaly_vertical_lines_static` with the expression on the right-hand side. |
| 1670 | `                ` | Blank line used to separate logical sections and improve readability. |
| 1671 | `                # Get y-axis limits for vertical lines` | Comment explaining intent/context: Get y-axis limits for vertical lines |
| 1672 | `                ylim_dynamic = self._dynamic_ax.get_ylim()` | Assigns/updates `ylim_dynamic` with the expression on the right-hand side. |
| 1673 | `                ylim_static = self._static_ax.get_ylim()` | Assigns/updates `ylim_static` with the expression on the right-hand side. |
| 1674 | `                ` | Blank line used to separate logical sections and improve readability. |
| 1675 | `                # Create vertical lines at each anomaly time point (only vertical lines, no markers)` | Comment explaining intent/context: Create vertical lines at each anomaly time point (only vertical lines, no markers) |
| 1676 | `                for anomaly_time in anomaly_times_plot:` | Starts a `for` loop: iterates as described by `anomaly_time in anomaly_times_plot`. |
| 1677 | `                    # Transparent light red vertical line on dynamic plot` | Comment explaining intent/context: Transparent light red vertical line on dynamic plot |
| 1678 | `                    vline_dynamic = self._dynamic_ax.axvline(` | Assigns/updates `vline_dynamic` with the expression on the right-hand side. |
| 1679 | `                        x=anomaly_time,` | Assigns/updates `x` with the expression on the right-hand side. |
| 1680 | `                        color='lightcoral',  # Light red color` | Assigns/updates `color` with the expression on the right-hand side. |
| 1681 | `                        linestyle='-',  # Solid line instead of dashed` | Assigns/updates `linestyle` with the expression on the right-hand side. |
| 1682 | `                        linewidth=2,` | Assigns/updates `linewidth` with the expression on the right-hand side. |
| 1683 | `                        alpha=0.4,  # More transparent for subtle highlighting` | Assigns/updates `alpha` with the expression on the right-hand side. |
| 1684 | `                        zorder=6  # Above data lines but subtle` | Assigns/updates `zorder` with the expression on the right-hand side. |
| 1685 | `                    )` | Executes Python statement: `)`. |
| 1686 | `                    self.anomaly_vertical_lines.append(vline_dynamic)` | Executes Python statement: `self.anomaly_vertical_lines.append(vline_dynamic)`. |
| 1687 | `                    ` | Blank line used to separate logical sections and improve readability. |
| 1688 | `                    # Transparent light red vertical line on static plot` | Comment explaining intent/context: Transparent light red vertical line on static plot |
| 1689 | `                    vline_static = self._static_ax.axvline(` | Assigns/updates `vline_static` with the expression on the right-hand side. |
| 1690 | `                        x=anomaly_time,` | Assigns/updates `x` with the expression on the right-hand side. |
| 1691 | `                        color='lightcoral',  # Light red color` | Assigns/updates `color` with the expression on the right-hand side. |
| 1692 | `                        linestyle='-',  # Solid line instead of dashed` | Assigns/updates `linestyle` with the expression on the right-hand side. |
| 1693 | `                        linewidth=2,` | Assigns/updates `linewidth` with the expression on the right-hand side. |
| 1694 | `                        alpha=0.4,  # More transparent for subtle highlighting` | Assigns/updates `alpha` with the expression on the right-hand side. |
| 1695 | `                        zorder=6  # Above data lines but subtle` | Assigns/updates `zorder` with the expression on the right-hand side. |
| 1696 | `                    )` | Executes Python statement: `)`. |
| 1697 | `                    self.anomaly_vertical_lines_static.append(vline_static)` | Executes Python statement: `self.anomaly_vertical_lines_static.append(vline_static)`. |
| 1698 | `                ` | Blank line used to separate logical sections and improve readability. |
| 1699 | `                self.log(f'Anomaly Visualization: Successfully plotted {len(self.anomaly_times)} anomaly vertical lines', level='Info')` | Executes Python statement: `self.log(f'Anomaly Visualization: Successfully plotted {len(self.anomaly_times)} anomaly vertical lines', level='Info')`. |
| 1700 | `                ` | Blank line used to separate logical sections and improve readability. |
| 1701 | `                # Force redraw of both canvases` | Comment explaining intent/context: Force redraw of both canvases |
| 1702 | `                try:` | Starts a `try` block to catch and handle exceptions. |
| 1703 | `                    self._dynamic_ax.figure.canvas.draw_idle()` | Executes Python statement: `self._dynamic_ax.figure.canvas.draw_idle()`. |
| 1704 | `                    self._static_ax.figure.canvas.draw_idle()` | Executes Python statement: `self._static_ax.figure.canvas.draw_idle()`. |
| 1705 | `                except Exception as e:` | Starts an `except` handler: `except Exception as e:`. |
| 1706 | `                    self.log(f'Anomaly Visualization: Error redrawing canvas: {e}', level='Error')` | Executes Python statement: `self.log(f'Anomaly Visualization: Error redrawing canvas: {e}', level='Error')`. |
| 1707 | `            else:` | Starts an `else` branch for the preceding conditional. |
| 1708 | `                # Clear anomalies if none detected - remove vertical lines` | Comment explaining intent/context: Clear anomalies if none detected - remove vertical lines |
| 1709 | `                for vline in self.anomaly_vertical_lines:` | Starts a `for` loop: iterates as described by `vline in self.anomaly_vertical_lines`. |
| 1710 | `                    try:` | Starts a `try` block to catch and handle exceptions. |
| 1711 | `                        vline.remove()` | Executes Python statement: `vline.remove()`. |
| 1712 | `                    except:` | Starts an `except` handler: `except:`. |
| 1713 | `                        pass` | No-op placeholder statement (keeps block syntactically valid). |
| 1714 | `                self.anomaly_vertical_lines = []` | Assigns/updates `self.anomaly_vertical_lines` with the expression on the right-hand side. |
| 1715 | `                ` | Blank line used to separate logical sections and improve readability. |
| 1716 | `                for vline in self.anomaly_vertical_lines_static:` | Starts a `for` loop: iterates as described by `vline in self.anomaly_vertical_lines_static`. |
| 1717 | `                    try:` | Starts a `try` block to catch and handle exceptions. |
| 1718 | `                        vline.remove()` | Executes Python statement: `vline.remove()`. |
| 1719 | `                    except:` | Starts an `except` handler: `except:`. |
| 1720 | `                        pass` | No-op placeholder statement (keeps block syntactically valid). |
| 1721 | `                self.anomaly_vertical_lines_static = []` | Assigns/updates `self.anomaly_vertical_lines_static` with the expression on the right-hand side. |
| 1722 | `                ` | Blank line used to separate logical sections and improve readability. |
| 1723 | `                # Clear any old scatter markers if they exist (shouldn't be created anymore)` | Comment explaining intent/context: Clear any old scatter markers if they exist (shouldn't be created anymore) |
| 1724 | `                if self.anomaly_line:` | Starts an `if` block: conditional control flow based on `self.anomaly_line`. |
| 1725 | `                    try:` | Starts a `try` block to catch and handle exceptions. |
| 1726 | `                        self.anomaly_line.remove()` | Executes Python statement: `self.anomaly_line.remove()`. |
| 1727 | `                    except:` | Starts an `except` handler: `except:`. |
| 1728 | `                        pass` | No-op placeholder statement (keeps block syntactically valid). |
| 1729 | `                    self.anomaly_line = None` | Assigns/updates `self.anomaly_line` with the expression on the right-hand side. |
| 1730 | `                if self.anomaly_line_static:` | Starts an `if` block: conditional control flow based on `self.anomaly_line_static`. |
| 1731 | `                    try:` | Starts a `try` block to catch and handle exceptions. |
| 1732 | `                        self.anomaly_line_static.remove()` | Executes Python statement: `self.anomaly_line_static.remove()`. |
| 1733 | `                    except:` | Starts an `except` handler: `except:`. |
| 1734 | `                        pass` | No-op placeholder statement (keeps block syntactically valid). |
| 1735 | `                    self.anomaly_line_static = None` | Assigns/updates `self.anomaly_line_static` with the expression on the right-hand side. |
| 1736 | `                ` | Blank line used to separate logical sections and improve readability. |
| 1737 | `                # Force redraw` | Comment explaining intent/context: Force redraw |
| 1738 | `                self._dynamic_ax.figure.canvas.draw_idle()` | Executes Python statement: `self._dynamic_ax.figure.canvas.draw_idle()`. |
| 1739 | `                self._static_ax.figure.canvas.draw_idle()` | Executes Python statement: `self._static_ax.figure.canvas.draw_idle()`. |
| 1740 | `                ` | Blank line used to separate logical sections and improve readability. |
| 1741 | `        except Exception as e:` | Starts an `except` handler: `except Exception as e:`. |
| 1742 | `            self.log('Error updating canvas', error=e)` | Executes Python statement: `self.log('Error updating canvas', error=e)`. |
| 1743 | `                ` | Blank line used to separate logical sections and improve readability. |
| 1744 | `    @property` | Executes Python statement: `@property`. |
| 1745 | `    def appWin(self):` | Defines function/method `appWin` (entry point for reusable logic). (scope: def appWin) |
| 1746 | `        return self._app_window` | Returns value(s) from the current function: `self._app_window`. (scope: def appWin) |
| 1747 | `` | Blank line used to separate logical sections and improve readability. |
| 1748 | `    def initViews(self):` | Defines function/method `initViews` (entry point for reusable logic). (scope: def initViews) |
| 1749 | `        """Initializes the different views (aka tabs) in our application."""` | Single-line triple-quoted string (docstring or multiline literal expressed on one line). (scope: def initViews) |
| 1750 | `        wnd = ApplicationWindow(self)` | Assigns/updates `wnd` with the expression on the right-hand side. (scope: def initViews) |
| 1751 | `        # wnd.showMaximized()` | Comment explaining intent/context: wnd.showMaximized() (scope: def initViews) |
| 1752 | `        return wnd` | Returns value(s) from the current function: `wnd`. (scope: def initViews) |
| 1753 | `` | Blank line used to separate logical sections and improve readability. |
| 1754 | `    def load_visualization_framework(self):` | Defines function/method `load_visualization_framework` (entry point for reusable logic). (scope: def load_visualization_framework) |
| 1755 | `        self.frame = Qt.QFrame()` | Assigns/updates `self.frame` with the expression on the right-hand side. (scope: def load_visualization_framework) |
| 1756 | `        window = self.appWin` | Assigns/updates `window` with the expression on the right-hand side. (scope: def load_visualization_framework) |
| 1757 | `        self.vl = Qt.QVBoxLayout()` | Assigns/updates `self.vl` with the expression on the right-hand side. (scope: def load_visualization_framework) |
| 1758 | `        window.tab_3.setLayout(self.vl)` | Executes Python statement: `window.tab_3.setLayout(self.vl)`. (scope: def load_visualization_framework) |
| 1759 | `        self.vtkWidget = QVTKRenderWindowInteractor(self.frame)` | Assigns/updates `self.vtkWidget` with the expression on the right-hand side. Hint: VTK (3D visualization) usage. (scope: def load_visualization_framework) |
| 1760 | `        self.vl.addWidget(self.vtkWidget)` | Executes statement related to: VTK (3D visualization) usage: `self.vl.addWidget(self.vtkWidget)`. (scope: def load_visualization_framework) |
| 1761 | `        self.ren = vtk.vtkRenderer()` | Assigns/updates `self.ren` with the expression on the right-hand side. Hint: VTK (3D visualization) usage. (scope: def load_visualization_framework) |
| 1762 | `        # todo - everything related to ren, renwin, iren, cam  .... (anything related to visualisation, and assign it to a tab manager)` | Comment explaining intent/context: todo - everything related to ren, renwin, iren, cam  .... (anything related to visualisation, and assign it to a tab manager) (scope: def load_visualization_framework) |
| 1763 | `        self.renwin = self.vtkWidget.GetRenderWindow()` | Assigns/updates `self.renwin` with the expression on the right-hand side. Hint: VTK (3D visualization) usage. (scope: def load_visualization_framework) |
| 1764 | `        self.iren = self.renwin.GetInteractor()` | Assigns/updates `self.iren` with the expression on the right-hand side. (scope: def load_visualization_framework) |
| 1765 | `        ` | Blank line used to separate logical sections and improve readability. (scope: def load_visualization_framework) |
| 1766 | `        style = vtk.vtkInteractorStyleTrackballCamera()` | Assigns/updates `style` with the expression on the right-hand side. Hint: VTK (3D visualization) usage. (scope: def load_visualization_framework) |
| 1767 | `        self.style = style` | Assigns/updates `self.style` with the expression on the right-hand side. (scope: def load_visualization_framework) |
| 1768 | `        self.iren.SetInteractorStyle(style)` | Executes Python statement: `self.iren.SetInteractorStyle(style)`. (scope: def load_visualization_framework) |
| 1769 | `        ` | Blank line used to separate logical sections and improve readability. (scope: def load_visualization_framework) |
| 1770 | `  #       vtkNew<InteractorStyleMoveVertex> style;` | Comment explaining intent/context: vtkNew<InteractorStyleMoveVertex> style; |
| 1771 | `  #         renderWindowInteractor->SetInteractorStyle(style);` | Comment explaining intent/context: renderWindowInteractor->SetInteractorStyle(style); |
| 1772 | `  #         style->Data = input;` | Comment explaining intent/context: style->Data = input; |
| 1773 | `  # style->GlyphData = glyphFilter->GetOutput();` | Comment explaining intent/context: style->GlyphData = glyphFilter->GetOutput(); |
| 1774 | `` | Blank line used to separate logical sections and improve readability. |
| 1775 | `        self.vtkWidget.GetRenderWindow().AddRenderer(self.ren)` | Executes statement related to: VTK (3D visualization) usage: `self.vtkWidget.GetRenderWindow().AddRenderer(self.ren)`. |
| 1776 | `        ` | Blank line used to separate logical sections and improve readability. |
| 1777 | `        self.cam = self.ren.GetActiveCamera()` | Assigns/updates `self.cam` with the expression on the right-hand side. |
| 1778 | `        ` | Blank line used to separate logical sections and improve readability. |
| 1779 | `        colors = vtkNamedColors()` | Assigns/updates `colors` with the expression on the right-hand side. Hint: VTK (3D visualization) usage. |
| 1780 | `        self.ren.SetBackground(colors.GetColor3d('SlateGray'))` | Executes Python statement: `self.ren.SetBackground(colors.GetColor3d('SlateGray'))`. |
| 1781 | `        ` | Blank line used to separate logical sections and improve readability. |
| 1782 | `        # ######## TRIAL HOVER` | Comment explaining intent/context: ######## TRIAL HOVER |
| 1783 | `        # colors = vtkNamedColors()` | Comment explaining intent/context: colors = vtkNamedColors() |
| 1784 | `        #` | Comment line used as a visual separator. |
| 1785 | `        # sphere_source = vtk.vtkSphereSource()` | Comment explaining intent/context: sphere_source = vtk.vtkSphereSource() |
| 1786 | `        #` | Comment line used as a visual separator. |
| 1787 | `        # # Create a mapper and actor` | Comment explaining intent/context: # Create a mapper and actor |
| 1788 | `        # mapper = vtkPolyDataMapper()` | Comment explaining intent/context: mapper = vtkPolyDataMapper() |
| 1789 | `        # # sphere_source >> mapper` | Comment explaining intent/context: # sphere_source >> mapper |
| 1790 | `        # actor = vtkActor()` | Comment explaining intent/context: actor = vtkActor() |
| 1791 | `        # actor.SetMapper(mapper)` | Comment explaining intent/context: actor.SetMapper(mapper) |
| 1792 | `        # actor.GetProperty().SetColor(colors.GetColor3d('MistyRose'))` | Comment explaining intent/context: actor.GetProperty().SetColor(colors.GetColor3d('MistyRose')) |
| 1793 | `        # self.ren.AddActor(actor)` | Comment explaining intent/context: self.ren.AddActor(actor) |
| 1794 | `        # # Create the widget` | Comment explaining intent/context: # Create the widget |
| 1795 | `        # from vtkmodules.vtkInteractionWidgets import vtkHoverWidget` | Comment explaining intent/context: from vtkmodules.vtkInteractionWidgets import vtkHoverWidget |
| 1796 | `        # hover_widget = vtkHoverWidget()` | Comment explaining intent/context: hover_widget = vtkHoverWidget() |
| 1797 | `        # hover_widget.SetInteractor(self.iren)` | Comment explaining intent/context: hover_widget.SetInteractor(self.iren) |
| 1798 | `        # hover_widget.timer_duration = 1000` | Comment explaining intent/context: hover_widget.timer_duration = 1000 |
| 1799 | `        # appself = self` | Comment explaining intent/context: appself = self |
| 1800 | `        # # Create a callback to listen to the widget's two VTK events.` | Comment explaining intent/context: # Create a callback to listen to the widget's two VTK events. |
